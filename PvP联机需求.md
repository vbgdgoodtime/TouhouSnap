# 东方逆转（Touhou2）· 联网 PvP 需求与交接文档

> 用途：把「把本游戏改造成线上联机 PvP」这件事完整交接给一个新对话 / 新协作者。
> 本文件是**规划性文档**，不属于 `docs/` 下的游戏机制文档体系（不参与「只写当前已实装内容」的约定）。
> 生成时间：基于仓库当前状态（最新提交 `8d5fb12`）整理。

---

## 0. 一句话目标

让两个真人玩家通过网络进行一局完整对战（6 回合 / 3 地形 / 双倍下注 / 暗牌揭示），
在**不放弃 GitHub Pages 静态托管**的前提下，尽量少引入需要自己运维的服务端。

---

## 1. 项目现状（交接必读）

### 1.1 技术形态

| 项目 | 现状 |
| --- | --- |
| 类型 | 纯前端静态网页游戏（Marvel Snap 玩法 + 东方 Project 主题） |
| 构建 | **无构建工具**、无打包、无 ES Module；全部是普通 `<script>` 标签 |
| 依赖 | 运行时零依赖；`jsdom` 仅用于测试 |
| 网络 | **完全没有网络请求**（无 `fetch` / `XMLHttpRequest` / `WebSocket` / `sendBeacon`） |
| 存储 | 仅 `localStorage`（卡组存档，键 `touhou2.decks.v1`） |
| 托管 | GitHub Pages，**Deploy from a branch：`main` / `(root)`** |
| 仓库 | `https://github.com/vbgdgoodtime/TouhouSnap`（从 `touhou2` 改名而来） |
| 线上地址 | `https://vbgdgoodtime.github.io/TouhouSnap/` |
| 体量 | 147 个跟踪文件，约 1.5 MB（含 56 张卡图） |

### 1.2 文件结构（与 `README.md` 一致）

```
index.html            入口（静态标记 + 按顺序加载脚本）
style.css             全部样式（单文件）
js/game.js            引擎主文件：对局流程/规则结算/渲染（约 203 KB，6400 行，最大）
js/ai.js              对手 AI：四档（简单/普通/困难/月狂），束搜索 + 效果估值（约 47 KB）
js/card-browser.js    图鉴与「指定卡牌」弹窗
js/deck-page.js       卡组设置页面（window.DeckBuilder）
js/deck-storage.js    卡组本地持久化（localStorage）
js/home.js            主页面（window.Home）
js/dev-page.js        开发调试模式（window.DevTools）
data/cards.js         人物卡 POOL + 特殊卡 SPECIAL
data/locations.js     地形池 POOL
assets/cards/         运行时卡图（ASCII 文件名）
assets/source-art/    原始立绘素材
docs/                 游戏机制文档（现有机制/地形/卡牌/开发规则…）
tests/smoke-test.js   jsdom 冒烟测试
```

### 1.3 关键约束（改动时必须遵守）

1. **脚本加载顺序 = 依赖顺序**，全部靠全局对象互调（`window.Game` / `AI` / `Home` / `DeckBuilder` / `DeckStorage` / `CardBrowser` / `DevTools`）。**不要调整 `index.html` 里的脚本次序**，也不要引入 ES Module / 打包器（会破坏 `file://` 直开与现有约定）。
2. **必须遵守 `docs/开发规则.md`**，其中最重要的是：
   - 规则 1：只写当前内容、一切改动先精简；
   - 规则 6：**验证由用户负责，AI 不自行跑测试/不自行验证**；
   - 规则 7：不确定先问用户；
   - 规则 3：机制改动只写对应专项文档；`docs/历史版本.md` 已停用（不再追加记录、不取版本号）。
3. 数据与逻辑分离：改卡/改地形只动 `data/`。

---

## 2. 当前架构中与联机直接相关的 6 个事实

### 2.1 所有玩家操作都集中在 `window.Game.ui.*`（重要）

`js/game.js` 末尾导出：

```js
window.Game = {
  restart,
  ui: {
    onPick, onPickConfirm,        // 选中手牌 / 确认
    onPickLoc, onPickLocConfirm,  // 选区域 / 确认落子
    onSnap,                       // 双倍下注
    onPass,                       // 结束回合
    onRetreat,                    // 认输
    onEnergyReset, confirmEnergyReset, cancelEnergyReset,  // 重置本回合暗牌
    onAddStone, onSwitchSide, onAiSpy, onPiles, ...
  },
  _dbg: () => ({ ... })           // 状态探针（回合/能量/手牌/区域…）
}
```

**含义**：UI 回调目前是"直接改状态"。要做联机，需要把它们改造成"产生动作对象"，由统一的动作应用函数消费——这是整个改造的枢纽。

### 2.2 本局是"同时行动 + 先后揭示"结构（对联机极其有利）

流程：每回合双方各自**暗出**若干张牌（消耗能量）→ 双方都结束回合 → 翻牌结算揭示效果
（首回合先后随机，其后**当前领先方先翻**，持平则随机）。

**含义**：天然适合**回合级"提交—结算"（lockstep / commit-reveal）**模型——双方各自提交本回合动作集，收齐后本地各自按同一规则结算，无需逐帧同步。

### 2.3 随机性全部走全局 `Math.random`，且**逻辑随机与表现随机混用**

- 影响**状态**的随机：洗牌、抽地形、翻牌先后、摧毁并列取谁、`gamble`/`dice` 掷骰、`spawn` 生成、`morph` 变身目标、`roam` 漂移区域、`gather` 分配……
- 纯**表现**的随机：粒子特效、飘字、主页面漂浮图标、地形装饰（`js/game.js`、`js/home.js` 内多处）。

**关键坑**：如果直接把全局 `Math.random` 换成种子流，两端会因动画帧数/触发时机不同而**消耗掉不同数量的随机数 → 随机流错位 → 状态不同步**。
所以联机前**必须先把"逻辑随机"与"表现随机"分离**（逻辑用 `Game.rng()`，表现继续用原生 `Math.random`）。

### 2.4 确定性已被间接验证（重要利好）

`tests/smoke-test.js` 的做法：在 jsdom 的 `beforeParse` 里把 `window.Math.random` 整体替换为种子化 PRNG（mulberry32），
使"抽卡 / 地形 / 翻牌先后 / AI"全部由 `SEED` 决定，**整局可复现**（`SEED=3 node tests/smoke-test.js`）。

**含义**：这套代码的确定性是可以做到的，lockstep 方案在技术上成立，不需要重写引擎。

### 2.5 AI 已经是一个"动作生产者"，可复用于联机

`window.AI` 导出：

```js
window.AI = {
  LEVELS, ORDER, DEFAULT_LEVEL, ALIAS,
  getLevel, setLevel, levelInfo, policyText,
  hypotheticScore,          // 单张落子估值
  _eval, _plan, _model, _delta,  // 探针：_plan() 直接产出本回合行动方案
}
```

**含义（白捡的三件事）**：
1. **掉线托管**：对手掉线立刻由 AI 接管；
2. **异步影子对战**：把"种子 + 操作序列"发给朋友，AI 或录像回放即可复现你的打法；
3. **单人模式不受影响**（AI 继续走本地路径）。

### 2.6 卡组存档与序列化口径

`js/deck-storage.js`：`localStorage['touhou2.decks.v1'] = { version, deckSeq, activeDeckId, decks:[{id,name,cards:[卡名…]}] }`
——最多 20 套、每套 12 张；**卡组里的牌以"卡名字符串"保存**，读回时由 `deck-page.js` 的 `defByName()` 解析。

**联机时必须注意**：
- 名字解析不到就**静默丢弃**该卡 → 两侧卡牌数据版本不一致时，同一套卡组会在两端解析出不同的 12 张牌 → **必须做数据版本校验**（建议把卡池版本/哈希放进握手包，不一致直接拒绝开局并提示）。
- 传输卡组时建议沿用同样的"卡名数组"口径，便于与 `DeckStorage` 互通。

---

## 3. 三条候选技术路线（含推荐）

| 路线 | 需要什么 | 服务器运维 | 防作弊 | 适用 |
| --- | --- | --- | --- | --- |
| **A. 实时托管服务（BaaS）** | Supabase Realtime / Firebase / Cloudflare Durable Objects / PartyKit / Ably 等免费服务；页面仍留在 GitHub Pages | ❌ 不需要 | 中～强（可把裁判放进 Durable Objects / Workers） | 陌生人匹配、开房间实时对战 |
| **B. WebRTC P2P 直连** | 信令交换（可用"复制连接码"零服务，或用免费信令）+ STUN（免费公用）；TURN 中继需服务器或付费 | ❌ 基本不需要 | **最弱**（房主可作弊、可看对手暗牌） | 熟人开黑 |
| **C. 确定性重放 / 异步对战** | 只要"随机种子化 + 操作序列化"；通道可以是任意（含 BaaS 的一张表，甚至复制粘贴链接） | ❌ 不需要 | 弱 | **本项目最低成本的起步方案** |

**推荐路径**：先 C（验证同步模型与动作抽象），再 A（把 C 的通道换成实时房间），最后才考虑"权威服务端"（防作弊）。

### 3.1 关键洞察：本游戏的同步量极小

- 一局 6 回合，每回合每位玩家只提交"暗出哪几张牌到哪个区域（+ 是否加倍/认输/重置）"；
- 一条动作 ≈ 几十字节，一局总计几十条 ≈ 几 KB。
- **结论**：不需要逐帧同步、不需要 WebSocket 也能玩；轮询（1 次/秒）都绰绰有余。

---

## 4. 必须完成的改造清单（按依赖顺序）

### 阶段 0：可联机化的地基（无网络，纯重构）

| # | 改造 | 说明 |
| --- | --- | --- |
| 0.1 | **随机源分离** | 引入 `Game.rng`（种子化 PRNG）；把影响状态的所有 `Math.random` 调用改用 `Game.rng()`；表现层保留原生 `Math.random`。约 40 处，需逐个判定归类 |
| 0.2 | **动作模型** | 定义动作对象（如 `{type:'play', card, zone}` / `{type:'snap'}` / `{type:'pass'}` / `{type:'retreat'}` / `{type:'undo'}`），UI 只产生动作，统一 `applyAction(action)` 消费 |
| 0.3 | **本局状态可导出/校验** | 提供"状态指纹"（关键字段哈希）便于两端对账、断线重连、录像回放 |
| 0.4 | **卡池版本标识** | 生成 `data/` 的版本号/哈希，用于握手校验（见 2.6） |
| 0.5 | **规则引擎与渲染解耦（可选但强烈建议）** | 现在 `game.js` 与 DOM 深度耦合；若未来要"服务端权威"，必须能无 DOM 运行规则。可先做到"渲染只读状态、不写状态" |

**验收**：同一 `SEED` + 同一动作序列在两端产生完全一致的对局结果与状态指纹（可用现有冒烟测试的种子机制验证）。

### 阶段 1：异步"挑战链接 / 影子对战"（零后端）

- 把 `种子 + 双方卡组 + 动作序列` 编码进 URL（压缩 + base64）或存到免费表；
- 朋友打开链接 → 本地复现你的对局 / 或与你留下的"影子"（AI 按你的动作序列出牌）对战；
- **不需要账号、不需要服务器**，也天然规避合规问题。

**验收**：A 打完一局生成链接，B 打开后看到与 A 完全一致的复盘；B 能接着打下一局并回传。

### 阶段 2：熟人实时对战（BaaS 房间）

- 6 位房间码加入房间；房主开局时下发 `种子 + 卡组 + 卡池版本`；
- 每回合双方提交动作集 → 收齐后各自结算；节点：`snap` 是**回合中互动**，需要定义"谁先确认"的顺序与超时；
- **掉线**：超时未提交 → 由 `window.AI` 托管该方；
- 协议建议：小消息 + 幂等（同一动作重复到达不重复应用），带序号防乱序。

**验收**：两台设备（含手机）能完成一整局；任一方刷新/掉线后可恢复。

### 阶段 3：陌生人匹配 + 防作弊（工程量最大，按需再做）

- 服务端权威：把规则引擎部署到 Workers / Node，客户端只上报动作、只接收可见信息；
- 需要解决**暗牌信息泄漏**：现在所有状态都在客户端（AI 甚至支持 `includeHidden` 读取暗牌战力），P2P 下对手可直接在控制台看到你的暗牌；
- 配套：账号体系、匹配队列、战绩、（可选）排行榜。

---

## 5. 待用户决策的问题（新对话先问这些）

1. **对战形态**：异步为主（挑战链接/影子）还是实时房间？还是两者都要？
2. **对手来源**：只跟朋友（房间码/链接）还是要陌生人随机匹配？
3. **是否需要账号**：不登录（设备本地身份）能否接受？还是必须有云端账号？
4. **卡组如何同步**：沿用本地 `localStorage` 各自构筑（推荐，改造小），还是要求云端同步？（后者需要账号 + 存储）
5. **防作弊要求**：娱乐局够用就行，还是必须防止改代码/改数据/偷看暗牌？
6. **目标平台**：手机为主还是桌面为主？（影响 UI：房间码输入、触屏交互）
7. **国内可访问性**：玩家主要在国内吗？——这直接决定能不能用 Firebase（国内基本不可用），也决定要不要考虑国内节点服务与备案问题。
8. **成本意愿**：只接受完全免费额度，还是可以承担少量费用（影响是否能用 TURN 中继、付费数据库）？
9. **可以接受引入第三方 BaaS 吗**？（Supabase / Cloudflare 等；如果坚持"绝不引入第三方"，就只能走阶段 1 的纯前端方案）
10. **UI 需要新增哪些界面**：创建房间/加入房间/等待对手/断线重连提示/对手离开提示…

---

## 6. 风险与坑清单（务必提前知道）

| 风险 | 说明 |
| --- | --- |
| **随机流错位** | 逻辑/表现随机未分离就上 lockstep，会出现"两端结果不同"且极难排查（务必先做 0.1） |
| **卡池版本不一致** | 一方改了 `data/cards.js`，另一方还是旧版 → 同名卡效果/数值不同 → 对局结果分叉（必须握手校验） |
| **暗牌信息泄漏** | 客户端持有全部状态；P2P 下"看对手暗牌"是零成本的（阶段 3 才能根治） |
| **作弊** | 纯前端可改代码、改数值、重放动作；只有服务端权威能解决 |
| **GitHub Pages 限制** | 只能静态托管；另有软限制（站点约 1 GB / 月流量约 100 GB / **每小时约 10 次构建**）——推送过于频繁会导致 Pages 不更新，容易误判成"部署坏了" |
| **国内访问** | GitHub Pages 与部分海外 BaaS 在国内可能慢/不稳；Firebase 基本不可用 |
| **合规** | 一旦引入账号与用户数据，就有隐私政策/实名等责任；纯前端方案零数据责任 |
| **免费额度** | Supabase 免费项目长期不活跃会被暂停；Cloudflare 免费额度（Workers 10 万请求/天、D1 5 GB、KV 1 GB）对本游戏量级足够 |
| **密钥安全** | 前端代码里的 key 全部公开：只能用允许公开的 key（如 Supabase `anon` + RLS），**绝不能**把 `service_role`、GitHub Token 放进前端 |
| **部署环境健康度** | 当前线上曾出现"仓库是新版、站点落后两个版本"的现象，怀疑 Pages 构建失败或**每小时构建次数超限**；建议先加 `.nojekyll` 并确认 Actions 状态，再开始联机改造（否则联机联调阶段会被部署问题干扰） |

---

## 7. 可复用资产（别重复造）

| 资产 | 复用于联机场景 |
| --- | --- |
| `window.AI._plan()` / 四档 AI | 掉线托管、影子对战、异步对手、联机调试时的"假对手" |
| `js/dev-page.js`（`window.DevTools`） | 联机调试：固定地形/空牌库/指定卡牌，构造可复现的测试局面 |
| `tests/smoke-test.js` 的种子机制 | lockstep 一致性验证的工具原型（同种子 → 同结果） |
| `js/deck-storage.js` | 卡组序列化口径（卡名数组）可直接复用为传输格式 |
| `Game._dbg()` | 状态指纹/对账探针的现成基础 |
| `docs/现有机制.md` | 规则权威口径（结算管线、时机效果、区域字段），写服务端裁判时的事实来源 |

---

## 8. 建议的下一步（新对话开场即可执行）

1. 先确认本文档第 5 节的 10 个决策问题；
2. 落定路线（建议：阶段 0 → 阶段 1 → 阶段 2，阶段 3 按需）；
3. 从 **0.1 随机源分离** 开始动工（这是所有方案的前置条件，且不影响现有单机玩法）；
4. 每步改动遵守 `docs/开发规则.md`（只写当前内容、先精简、验证由用户负责、不确定先问）。
