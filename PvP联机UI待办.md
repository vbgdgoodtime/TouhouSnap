# 东方逆转 · 联机 UI 待办（执行清单）

> **本文件是执行清单**：联机对战已上线可用（房间服务 `pvp.2houvv.xyz`，国内直连、不需要梯子；
> 当前状态见 `PvP联机现状.md`），下面是把**联机相关界面**与**对局页 / 主页面**打磨到位的待办。
> 开工前先读两份：`docs/开发规则.md`（**★规则 1 只写当前内容、先精简**；**★规则 6 验证由用户负责、不跑测试不开浏览器、不读 `js/ai.js` 与 `tests/`**）、
> `docs/联机对战.md`（联机实现口径，以它为准）。
> **做完一项就把那一项从本文件删掉**（本文件只留还没做的；不要写"已完成""原为…"这类历史）。

## 0. 三条红线（每项都适用）

1. **纯显示层改动**（元素 / 文案 / 样式）不影响"两端算同一局"，放心改；
   **凡是动到状态或消息的**都要同步 `docs/联机对战.md` §5 与 `worker/README.md` 的消息表，并**请用户两台设备实测**。
2. **`index.html` 的脚本加载次序不能动**（data → game → ai → card-browser → home → deck-storage → deck-page → dev-page → **net**）；`js/net.js` 必须仍在最后。
3. 措辞沿用现有口径：本机恒坐 `p`、对手恒坐 `a`；"提交包""对账一致""超时判负""撤退价"。别自造新词。

## 1. 对手动作的即时提示

- **现状**：对手加倍 / 认输 / 交牌只有对局信息里的一行字（`Game.net.applyPeerSnap` 里的 `log('snap', …)`；认输已即时弹结算弹窗）。
  对手"交了没有"已由侧栏对手信息区（`#oppTurnTag`）与顶栏状态条显示，见 `js/net.js` 的 `syncOpponent` / `renderBar`。
- **要做**（纯显示层）：
  - 对手**加倍**时给一个看得见的反馈（状态条变色 / 侧栏标记闪一下）；
  - 音效：项目里**没有音频资源**，要做先按规则 7 问用户要不要加、加在哪几个时机。
- **验收**：对局中不盯日志也能察觉对手的动作。

## 2. 对局页整体布局

- **手机端已收口**（用户口径：手机 / 窄窗口太挤，截图实测）：
  - **顶栏＝两行网格**（`≤700px`：`grid-template-columns: 1fr auto 1fr`；`.hud` 用 `display: contents` 让回合 / 赌注直接成为网格项）：
    第一行＝品牌（左，「⚡ 东方逆转」点它回主页面）· `回合：x/N`（居中，靠 `.hud-turn-label` 保留文字，手机上其余 HUD 标签仍隐藏）·
    右侧一组 `.hud-right`＝赌注 + **「加倍」**（加倍在手机上**重新可见**，原先 ≤900px 是隐藏的）；
    第二行＝`.top-actions`（**整行靠右**）：**图鉴 / 重新开始带文字**（不再图标化）+ 开发调试按钮（仅开发调试出现）。
    「重新开始」在**联机对局里由 `renderControls()` 加 `.hidden` 收掉**（联机不许就地重开，`uiOnRestart` 也会挡），单人局才显示；
  - 手机状态条 `.mob-status` **默认只占 1 行**（`-webkit-line-clamp: 1`），文案被截断时挂 `.clamped` 出现 ▾、
    点一下 `.open` 展开 —— 判定与切换在 `js/game.js` 的 `setStatus()` / `syncMobStatusClamp()`；
  - 能量点行 `.energy-pips` 在 ≤700px **不再单独占一行**（能量由底部的「能量：x/N」块显示，它同时是重置暗牌的入口）；
  - **回合开始的规则说明不再写状态条**：`roundStartStage()` 只发一句「第 N 回合：选择手牌，点击区域打出。」
    （那段规则本来就在侧栏「玩法速览」与日志里，手机上却要占两行）；
  - **底部中央**＝「能量：x/N ↺」（金黄色文字、可点重置）+「手牌：x/N」；顶栏已**没有**能量框与「← 主页」按钮。
- **还没做**（都等用户拍板）：
  - `#netBar`（`index.html` 里紧跟 `</header>` 的一整条，手机上占 1 行 / 桌面占 ≈30px）：
    并进 `header` 内 / 做成可折叠 / 保持现状 —— **不用改 JS**（`js/net.js` 只按 id 取值）；
  - 手牌区底部让位（`.hand-wrap` 的 `48px + --sa-bottom`）与角落按钮合并成同一水平带：手机可再省 ≈48px，
    代价是按钮贴着手牌两端、**误触风险**；
  - 场地列数：≤640px 现为「三列并排」，每列 ≈110px ⇒ 每张缩略卡只有 ≈53px 宽（立绘还是"高度撑满、宽度居中裁掉"，
    加高场地不会让牌变宽）。改两列 / 单列堆叠能显著放大卡面，但一屏看不全三块地形。
- **用户已明确不动**：侧栏 `#sidePanel`（含「玩法速览」折叠）。
- **验收**：桌面端场地不被顶栏压缩；手机端场地可视高度明显增加（顶栏一行 + 状态条一行 + 能量点行省下 ≈70px）。

## 3. 主页面与各入口的视觉

- **现状**：`#homeScreen` 的主入口是 开始对战 / **联机对战** / 开发调试 / 卡组设置 / 设置 / 挑战码（`#homeMenu` 的 DOM 顺序；「图鉴」那一格当前带 `hidden` 类不显示），
  联机入口已排在「开始对战」下方，样式 `.home-btn.net`＝淡蓝渐变底 + 深蓝字（与「开始对战」同款写法：无边款 + 渐变 + 光晕投影）；**新手引导 `#guideMask` 的 8 小节里没有"怎么联机"这一节**，
  连「🧭 主页面各入口怎么用」那节也只列了 开始对战 / 卡组设置 / 设置 / 开发调试，**没有联机对战**（文案全在 `index.html`，交互在 `js/home.js`）。
- **要做**（先问用户想要的风格）：
  - 入口分组（单机 / 联机 / 工具）；
  - 新手引导补一节「怎么和朋友联机」（房间码、双方各带卡组、要同版本、超时判负、刷新即离开；
    顺带提一句「设置 → 玩家资料」里填的昵称与头像会显示给对手）。
- **验收**：新玩家能在引导里搞清联机流程；主页面能一眼看到联机入口。

## 4. 交付约定（每项都照这个来）

- **不跑测试、不开浏览器、不起本地服务器、不读 `js/ai.js` 与 `tests/`**（规则 6）；改完只做静态自查（`node --check`、grep 复核自己声称改过的地方）。
- 交付时列**改动文件清单** + **建议用户验证的步骤与现象**；由用户实测后回报。
- 只写当前内容（规则 1）：不加版本号、不写"以前是怎样"。
- 联机相关的改动若影响到"两端算同一局"，必须在交付说明里**明确点名**，请用户两台设备实测一次完整对局。
