/* =========================================================
   东方逆转 · 区域（场地）数据（从 game.js 拆出，便于扩展）
   =========================================================
   字段说明：
     id   = 内部标识（决定配色 class，如 double/slot2）
     n    = 区域名（中文显示名）
     icon = 顶部/中间带显示的小图标
     wt   = 赢得该区域计入的区域数权重（当前统一为 1）
     dbl  = 终局结算时该区双方总战力的倍率（1=不变, 2=×2）
     max  = 每方在该区域最多可放的卡数（4 或 2）
     eff  = 效果说明文字
     spawn= 可选：本区域“出现时”每方生成的卡牌。两种写法：
            ① { card: SPECIAL 键名, n: 张数 }——生成指定特殊卡（如虹龙洞 → 石块、
               幽灵洋馆 → 幽灵）；
            ② { cost: 费用, n: 张数 }——**从 POOL[费用] 随机抽人物卡**生成（v165，妖精神社：
               双方各 1 张随机 1 费卡；**每张独立随机抽取**，故双方可能拿到不同的卡）。
            生成方式统一走 placeToken：**落地即翻开、占格位、进入场上放置顺序队列**；
            是否触发被生成卡的「揭示」效果由 `reveal` 决定：
              · 缺省（不写）= **不触发揭示**（虹龙洞石块/黄瓜田河童/幽灵洋馆幽灵等 token 口径），
                但其持续 `og` / 时机 `fx` / 防摧毁 `surv` 等实时机制照常生效；
              · `reveal: true`（v166，妖精神社）= 生成后**立即结算这些卡自身的揭示效果**
                （如抽到「大妖精」当场给同区双方已翻开卡 +1；按落场顺序、先你方后敌方，
                同步结算无翻牌停顿）；注意像 `dw` 这类摧毁型揭示也会当场生效。
            ⚠️ 结算时机（v74/v151/v150）：该效果在**地形被揭晓时**（locationRevealStage）、
            **区域被「区域变形 xform」变成该地形时**（v151）、以及**开发者「🗻 指定地形」
            替换该列时**（v150，可勾选关闭）结算——即“该地形在本区出现”的三种时机。
     aff = 可选：区域-阵营加成 { group: GROUPS 键名, add: 威力 }，
           给位于本区域、所属该阵营的卡牌常驻加威力（实时生效，见 game.js 的
           locRoleBonus/cardPowerIn；如红魔馆对 scarlet 阵营 +2）。
     fill= 可选：某一方在本区放满 max 张（实际卡数，含暗牌）时，该方总战力
           额外 +N（常驻实时判定；因摧毁/撤回导致不足 max 张则立即不生效）。
     cb  = 可选：费用加成 { c: 费用, add: 威力 }，位于本区域、费用恰为该值的
           卡牌（双方，含特殊卡）威力 +N（常驻实时，见 game.js locCostBonus）。
     inv = 可选：true 表示“反转区域”——本区域总战力更低的一方获胜。
           比较口径按负值参与（区域胜负/领先判定/总点数决胜/AI，见 game.js zoneEff）。
     minTurn= 可选：本区域到第 minTurn 回合起双方才可放牌（之前为锁定，
           见 game.js locOpen；如七夕坂 minTurn: 5）。
     all = 可选：区域全体修正（可为负，如冥界 -2）——本区域所有卡牌
           （双方、特殊卡）威力 +N（常驻实时，见 game.js locAllBonus）。
     purge= 可选：true 表示回合结束摧毁——每回合翻牌结算后，摧毁本区域
           “全场”（敌我混比）战力最低的卡牌，并列最低一并摧毁（见 game.js reactorPurge）。
    decay= 可选：回合结束战力衰减（如间歇泉 decay: 1）——每回合翻牌结算后，本区域
           **双方所有已翻开**的卡牌永久 −N 战力（见 game.js locTurnEndPowerEffects）。
           只作用于结算那一刻已翻开的卡（暗牌不吃、翻面后从下一个回合末起才被衰减）；
           永久生效、可叠加；含落场 token（石块/厄运/分身等）；un 免疫；带 surv/phx 的卡
           照常被减（这不是“摧毁”，不触发任何摧毁相关机制）。
     grow= 可选：回合结束战力成长（如寺子屋 grow: 1）——decay 的正向镜像：每回合翻牌
           结算后，本区域**双方所有已翻开**的卡牌永久 +N 战力（见 game.js
           locTurnEndPowerEffects，与 decay 共用同一套口径与结算点）。同样只作用于已翻开
           的卡、永久可叠加、含落场 token、un 免疫；这不是“增益揭示”，与 bf/ba 等
           一次性揭示无关，也不触发任何摧毁类机制。
     gamble= 可选：翻开时博彩（如驹草赌场 gamble: 1）——**在该区域翻开的卡牌**（暗牌
            翻面那一刻）**永久随机 +N 或 −N 战力**（各 50%），见 game.js
             runLocRevealEffects。结算点在翻牌流程内、该卡自身揭示效果**之前**
             （故 bl/oc 等按博彩后的威力判定）；只作用于“在本区被翻开”的卡——落地即
             翻开的落场 token（石块/厄运/分身等）与已翻开后被移入本区的卡不参与，
             地形出现前就已翻开的旧卡不追溯；双方一视同仁；永久生效（走 applyPermBuff：
             ±N 演出 + 战力影响历史按来源记录地形名）；被摧毁回手后再打出会重新博彩。
     dice= 可选：定时掷骰 { turn, n }（如骰子赌桌 dice: { turn: 4, n: 1 }）——在
           **第 turn 回合的回合结束时**（阶段 ⑤-0 地形回合结束效果内，先于场上
           「回合结束」卡牌效果），把本区域**所有卡牌**（双方，含落地即翻开的落场
           token）**各自**随机永久 +n 或 −n 战力（各 50%），见 game.js locDiceEffects。
           与 gamble 的区别：gamble 是“卡牌在本区被翻开时”逐张博彩、每张都会轮到；
           dice 只在指定的那一个回合末结算一次，且作用于**当时在本区的所有卡**
           （含此前就已翻开的旧卡、也含 token），一次性结算完就不再触发。
     rally= 可选：定时加成 { turn, add }（如演唱会 rally: { turn: 5, add: 2 }）——在
            **第 turn 回合结束时**（阶段 ⑤-0，先于场上「回合结束」卡牌效果），把本区域
            **双方所有已翻开卡牌**永久 +add 战力（add 可为负），见 game.js
            locRallyEffects。与 dice（定时掷骰）同类“定时一次性”机制，区别：rally 是
            **固定值**、dice 是**每张卡各自随机 ±n**；与 grow（每回合成长）的区别：
            grow 每个回合末都触发、rally 只在指定的那一个回合末触发一次。
     gust= 可选：揭示后吹飞（如魔力风暴 gust: true）——**在该区域翻开的卡牌**（走翻牌流程
           的那张暗牌）在**自身揭示效果结算完之后**「若可能」就移动到另外一个随机区域，
           见 game.js runLocAfterRevealEffects（移动核心与 roam 共用）。与 gamble 的区别：
           gamble 在翻面瞬间、自身揭示效果**之前**结算（会改 bl/oc 等判定），gust 在自身
           揭示效果**之后**结算（不改本次揭示结果）；只作用于“本回合在该区被翻开”的卡——
           已翻开的旧卡、落地即翻开的 token、被移入本区的卡都不受影响，地形出现前的旧卡
           不追溯；若自身效果已把该卡挪出本区（如八云紫 shift）则不再吹。
     collapse= 可选：回合结束崩塌 { cards, to }（如幽明结界 collapse: { cards: 6,
           to: 'underworld' }）——每回合翻牌结算后（阶段 ⑤-0 地形回合结束效果的**最后**
           一步）检查：若本区域**双方总卡牌数** ≥ `cards`（按“张数”计，大体积卡也算 1 张、
           暗牌与 token 都算），则把本区域地形**整体换成 `to` 指定的地形**（如冥界），
           见 game.js locCollapseEffects。口径与 xform 对齐（v151）：变形＝该地形在本区
           “出现”，换上后立刻结算目标地形的「出现时」效果（无 spawn 时为空操作）；
           已放卡不移动、不增删；换掉后本区不再带 collapse 字段，故只会崩塌一次；
           崩塌后的新地形**从下一回合起**按其规则参与（同一回合不再触发新地形的回合结束类效果）。
     xformTurn= 可选：定时变形 { turn }（v199，如秘封俱乐部 xformTurn: { turn: 5 }）——
            在**第 turn 回合的回合开始时**（阶段 ①-0b，紧接「地形揭晓」之后、场上
            「回合开始」卡牌效果之前），把本区域地形**整体换成地形池里随机另一个地形**，
            并立刻结算目标地形的「出现时」效果（spawn，同 xform / collapse 的 v151 口径），
            见 game.js locXformTurnEffects。口径（用户确认，v199）：
              ① 候选 = `POOL` 里**除自身以外**的任意一块地形（**允许与另外两列当前地形
                 重复**；EXTRA 非随机地形不入候选）；
              ② 抽中「上限放不下本区已放卡」的地形（如迷途竹林 max 2 而某侧已放 3 张）
                 **照常变形**——口径同「地形揭晓」的超限处理：已放卡不移动、不增删、不摧毁，
                 隙间只铺在“空置”的不可用格（见 buildZoneChildren），该侧此后按已满处理；
              ③ 换掉后本区不再带 xformTurn 字段，故**只会变形一次**；
              ④ 新地形**从变形那一刻起完全生效**，含**同一回合末**的 grow/decay/dice/
                 rally/purge 等回合结束类效果；
              ⑤ 若该地形是在第 turn 回合开始**之后**才落到本区的（xform 区域变形 /
                 开发者「🗻 指定地形」），该时机已过、**不补结算**（口径同 dice / rally）。
     gap= 可选：回合结束封格（v200，如八云紫的家 gap: 1）——每回合翻牌结算后（阶段 ⑤-0
            地形类回合结束效果的最后一步，排在 collapse 之后），**双方分别判定**：某侧在本区
            **还有空位**（已占格 < 该侧当前可用格数）就继续封 `gap` 张（缺省 1）「隙间」；
            该侧“剩下的空间已经被牌放满”则本次不加，之后只要有牌被摧毁/移走腾出空位，
            下一个回合末就继续封。见 game.js locGapEffects / locGaps / locSideMax。口径（用户确认，v200）：
              ① **从后往前**：封的是该侧**最靠后**的格位——实现上把「已封格数」记在本列
                 （state.locs[j].gaps，**双方各一份**），该侧**可用格数** = `max` − 已封数，
                 渲染层照旧只把隙间铺在“空置的不可用格”，故表现就是最后一格先变灰、逐回合往前推；
              ② **双方分别判定**（如上），故两侧被封的格数可以不同；
              ③ **下限 0**：可以封到该侧一格不剩（此后该侧不能在本区放牌/生成 token，
                 已有卡保留、照常计分）；
              ④ **换地形即清空**：xform / collapse / 开发者「指定地形」/ 秘封俱乐部的定时变形
                 都会重置该列两侧的隙间（隙间属于这块地形）；
              ⑤ 只改「能放几张」：隙间不占 `zones` 数组、不进战力/`powerLog`/`fieldQueue`，
                 不触发 `surv`/`phx`/`prot`/`ind`；放牌、落场生成、移动、大体积卡上限、
                 `fill` 放满加成一律按封格后的**可用格数**算。
     noDown= 可选：区域免减攻（v201，如蓬莱药局 noDown: true）——本区域**所有卡牌**
            （双方、含暗牌与落场 token）**无法被减少战力**：一切“负的战力增量”在生效前被拦下
            （卡牌战力原封不动、不写战力影响历史、不播 −N 演出，只记一条日志）。
            见 game.js 的 applyPermBuff 拦截点、locNoDown / cardNoDown。口径（用户确认，v201）：
              ① 拦的是**所有会让本区卡牌战力下降的效果**：卡牌揭示 `de`/`deAll`、地形
                 `decay` 衰减、`dice` 掷出负、`gamble` 掷出负、`rally` 负值、`surv` 防摧毁的
                 “替代降攻”、以及分身/衍生物与本体战力对齐的负差；
              ② **实时负加成一并抹平**：本区地形若带负的 `all`/`cb`/`aff`（或己方持续 `og` 为负），
                 对本区卡牌按 0 计；
              ③ **不追溯、不恢复**：地形出现前已吃到的减攻留在卡上（`buff` 里的负值照常计入），
                 地形被换掉后也不再受保护；只对“结算那一刻人在本区域”的卡生效；
              ④ **不是“摧毁”类**：与 `surv`/`phx`/`prot`/`ind` 无耦合——但 `surv` 的替代降攻
                 被拦下时，该卡**不离场、也不降攻**（两个防护叠加）；
              ⑤ 卡的**印刷负战力不算“被减攻”**：厄运 −3 / 水银 −1 进本区照常是负战力，
                 对方往本区塞这类 token 也照常；法术恒为 0、本就免疫一切增减。
      mute= 可选：区域「抹除文本」（**v202**，如静海 mute: true）——本区域**双方所有卡牌**
            （含暗牌、落场 token 与法术）**失去卡牌文字**：卡面上写的效果一律视为不存在、
            **任何时机都不发动**。判定＝**实时读法、零状态**（`locMuted(locIdx)` / `cardMuted(card)`，
            按 `fieldLocOf` 实时查），因此**静海被换掉、或卡被移出本区就自动恢复**，不需要任何收尾代码。
            失效范围（用户确认，v202）：揭示 `k`（含 shift / gather / reviveDiscard 三个走分步演出的键）、
            持续 `og`（**源卡**在静海 → 它的光环整条失效；被加成的卡在静海不受影响，只要源卡在静海外）、
            时机效果 `fx`（turnStart / turnEnd / gameEnd，错过的时机**不补结算**）、
            防护/替代（`surv` / `phx` / `prot`（蕾蒂的区域免摧毁）/ `ind`——它们也是“卡面文字”，一并失效）、
            `fly`（每回合移动一次的能力）；法术的揭示不发动，但**照常消散**
            （消散是 v170 的法术规则、不是卡面文字，仍进放逐池）。
            **不影响**（用户口径：“只有在静海里的牌才会被影响”）：`costDown`（纯狐，在手牌/牌库里就生效）、
            `gs`（哆来咪，开局在卡组即触发）、`playReq`（大鲶鱼，只在“手牌→场上”那一刻校验）、
            卡的数值与物理属性（印刷费用 `def.c` / 基础威力 `def.p` / 阵营 `g` / **`occ` 大体积占格**），
            以及全部**区域类效果**（`aff`/`cb`/`all`/`fill`/`inv`/`purge`/`grow`/`decay`/`dice`/`rally`/
            `gamble`/`gust`/`gap`/`noDown`/`prot`）——那些是地形写的，不是卡面文字。
            卡照常能被增益/削弱/摧毁/移动/换边（它只是“哑巴”，不是 `un` 占位卡）；**不追溯**：
            静海出现前已经结算过的永久效果留在卡上（不回滚），静海期间错过的揭示/时机**不补结算**。
            日志：每张牌**首次**被拦截时记一条（避免每回合刷屏）。
     extraRound= 可选：本局延长回合（**v203**，如虚假之月 `extraRound: 1`）——只要**本局场上**
            （＝当前三列里的任一列）有带本字段的地形，**本局总回合数就是 6 + 1 = 7**，否则 6；
            见 game.js 的 `fakeMoonOnField` / `roundsTotal` / `syncRoundTotal`。口径（用户确认，v203）：
              ① **实时判定、零状态**：判据就是**当前三列的地形**（读 `def.extraRound`，未揭示列是
                 `unreveal` 占位地形、不带本字段 ⇒ 天然不算）——地形因揭晓 / 定时变形 / 卡牌 `xform` /
                 `collapse` 崩塌 / 开发者「🗻 指定地形」而带上或失去本字段时，本局总回合数**立即**跟着变
                 （顶栏「回合 N / 总数」实时同步，并在变化的那一刻写一条日志）；
              ② **进入第 7 回合后锁定**：一旦本局已打到第 7 回合，此后即使把本地形换掉也**仍算 7 回合**
                 （第 7 回合照常打完、第 7 回合末才终局结算，绝不会中途结束）；该锁定也保证
                 **永远不会出现第 8 回合**；
              ③ **不叠加**：上限恒为 **7**——同时存在两块带本字段的地形也只延长 1 回合；
              ④ 只改「打几回合」，**不改变能量曲线的形状**：每回合基数 = `min(回合数, 本局总回合数)`
                 ⇒ 第 7 回合双方各 **7 点**（哆来咪 `gs.energyAdd` / 斯塔萨菲雅 `energyNext` 照常叠加）；
              ⑤ 与「第 4~7 回合的回合开始」无关：地形揭晓只在第 1/2/3 回合各一次（`locationRevealStage`），
                 第 4 回合起 ①-0 为空操作；`dice` / `rally` / `xformTurn` / `minTurn` 这类“指定回合”
                 字段**不会**因本延长而在第 7 回合二次触发（它们仍只在自己指定的那一个回合结算）；
              ⑥ 结算链整体顺延一回合：第 7 回合的 ⑤-0 地形类回合末效果 / ⑤ `fx.turnEnd` /
                 ⑥ `fx.handEnd` / ⑦ `fx.gameEnd` + `leave` 终局离场照常执行，之后才走终局演出与胜负结算；
              ⑦ 非“出现时”效果、非摧毁/非增减类：不改任何卡牌战力与格位、不触发
                 `surv`/`phx`/`prot`/`ind`、不进 `powerLog`/`fieldQueue`。
     prot= 可选：区域免摧毁（如睡鼠神祠 prot: true）——本区域**双方所有卡牌**都不会被
           摧毁：针对本区域的任何“摧毁”指向（卡牌 `dw`/`dwh`、地形 `purge` 等）一律失效，
           既不选目标，也不会触发防摧毁 `surv` / 凤凰重生 `phx`；保护含双方人物卡与
           落场 token、也含本区所有后续落下的卡。见 game.js locNoDestroy（与卡级 prot
           「蕾蒂」同一判定入口：地形带 prot 恒为真，随地形存亡——被 `xform`/`collapse`
           换掉后立即失效）。非“摧毁”类效果（增减/移动/换边等）照常生效。
     shatter= 可选：出现时「天界降临」摧毁另外两块地形（**v205**，如天界 `shatter: true`）——
             在本区**「出现」的那一刻**（与 `spawn` 完全同一套时机：① 地形揭晓；② 卡牌 `xform`；
             ③ 地形 `xformTurn` 定时变形；④ 地形 `collapse` 崩塌；⑤ 开发者「🗻 指定地形」），
             把**另外两块地形**连同其上的**所有卡牌**一并摧毁，本局此后只剩本区域可用。
             口径（用户确认，v205）：
               ① **逐块处理、左→右**（按列下标升序，跳过本列与**已经破碎**的列），每块两阶段：
                  ㈠ **依次摧毁该区的所有卡**（顺序＝场上放置顺序队列 `fieldQueue`；每张间隔 **0.3s**，
                     每张播既有「分崩离析」演出 `playShatter`）→ ㈡ 等 **0.3s** → **摧毁该地形本身**
                     （该列变「已破碎」）；两块之间再等 **0.3s**。
                  即整条节奏 = 卡A →0.3s→ 地形A →0.3s→ 卡B →0.3s→ 地形B。
                  ⚠️ 三处等待原为 1000 / 500 / 800 ms，**用户复测后要求「所有的间隔都改成 0.3 秒」**
                     （对应 `SHATTER_CARD_MS` / `SHATTER_TERRAIN_MS` / `SHATTER_NEXT_LOC_MS`）。
                     因 `SHATTER_CARD_MS`(300) < `playShatter` 动画时长(约 1000ms)，相邻几张的碎裂
                     演出会**重叠播放**（观感＝连续爆裂）；分崩离析是**全局共用**的摧毁演出
                     （`dw`/`dwh`/`dwb`/`purge` 等都用它），**不为一处机制单独改短** ——
                     若想恢复“每张完整播完”，把 `SHATTER_CARD_MS` 调回 1000 即可。
               ② **一切保护一律无视**：区域级 `prot`（睡鼠神祠）/「蕾蒂」与卡级 `surv`（灵乌路空）
                  /`phx`（藤原妹红）/`ind`（佛体金刚石）**全部不生效、不播任何替代演出**——
                  整块区域都不存在了。被摧毁的卡**照常走 `recordDestroy`**：进归属方的摧毁池、
                  计入「纯狐」`costDown` 的摧毁计数；这与 `purge`/`dw` 等“会被防护拦下”的摧毁
                  **刻意不同**，是本字段的专属口径。
               ③ **含暗牌与落场 token**（石块/厄运/分身/河童…）、以及场上的法术，一视同仁。
               ④ **「已破碎」区域**：整列被一块损坏面板占据——**看不出原本是哪块地形、没有双方
                  总点数的数字、没有格位**，只写「已破碎」；地形换成 `EXTRA.shattered`
                  （`max: 0`、`wt: 0`、无任何效果），因此 `locSideMax`/`sideRoom` 恒 0、
                  `locOpen` 恒 false ⇒ **双方都不能再在该区放牌 / 移动 / 落场生成 / 复活**；
                  `zoneTotals` 恒 0，终局**不计入赢下的区域数与三区总点数**（只由剩下的区域判定）。
               ⑤ **永久锁定**：已破碎的列**不会被任何路径再换地形**——卡牌 `xform`、地形
                  `xformTurn`、`collapse` 崩塌、开发者「🗻 指定地形」一律跳过（开发者工具在弹窗内红字提示）。
               ⑥ **不重复摧毁**：再出现第二块带 `shatter` 的地形时，已破碎的列直接跳过；
                  另外两列都已破碎则本次「降临」无事发生（只记一条日志）。
               ⑦ **演出是异步的**：整条链会被 `await`（揭晓 / 定时变形 / 崩塌 / 卡牌 `xform` 四条路径
                  都会等它播完才继续，因此**不会与后续的「回合开始 / 翻牌 / 回合结束」效果抢时序**）；
                  **开发者「🗻 指定地形」例外** —— 它只启动链条便**立刻关窗**，演出在后台继续播
                  （避免弹窗一直挂到两块地形都拆完）。
               ⑧ 若被摧毁的那一列正是「虚假之月」，本局总回合数会**当场退回 6**（走 `syncRoundTotal`）。
               ⑨ `shatter` 自身**不改任何卡牌战力**、不写 `powerLog`、不触发 `surv`/`phx`/`prot`/`ind`；
                  「时机已过就不补结算」的口径同 `dice`/`rally`/`xformTurn`（例：第 5 回合开始之后
                  才被 `xform` 换上天界的 `xformTurn` 不再补触发）。
   ⚠️ 回合结束结算顺序（v153）：每回合翻牌结算后**先结算地形类效果**
           （grow 成长 / decay 衰减 / dice 掷骰 / rally 定时加成 → purge 摧毁 →
           collapse 崩塌，见 game.js runLocTurnEndEffects），**再结算场上「回合结束」
           卡牌效果**（卡牌 def.fx.turnEnd，见 runTurnEndEffects），最后是手牌回合结束
           挂点（runHandEndEffects）。
   每局会从该池中按 pick 权重不放回抽 3 块互不相同的区域（池中种类不足 3 时才可能出现重复）。
   新增场地：直接向下方数组追加一条即可，无需改 game.js。
   ========================================================= */
window.DS_LOCATIONS = {
  POOL: [
    { id: 'plain',  n: '无名之丘', icon: '⛰️', wt: 1, dbl: 1, max: 4, eff: '无特殊效果' },
    { id: 'mansion', n: '红魔馆',   icon: '🏰', wt: 1, dbl: 1, max: 4, aff: { group: 'scarlet', add: 2 }, eff: '此区域的「红魔馆」角色 威力 +2' },
    { id: 'slot2',  n: '迷途竹林', icon: '🎋', wt: 1, dbl: 1, max: 2, eff: '双方在此区域最多各放 2 张牌' },
    { id: 'rainbow', n: '虹龙洞', icon: '🕳️', wt: 1, dbl: 1, max: 4, eff: '区域出现时：双方各生成 1 张「石块」', spawn: { card: 'stone', n: 1 } },
    { id: 'cucumber', n: '黄瓜田', icon: '🥒', wt: 1, dbl: 1, max: 4, eff: '区域出现时：双方各生成 1 张「河童」', spawn: { card: 'kappa', n: 1 } },
    { id: 'dragon',  n: '龙神像', icon: '🐉', wt: 1, dbl: 1, max: 4, fill: 5, eff: '放满 4 张的一方：本区战力额外 +5' },
    { id: 'lake',    n: '雾之湖', icon: '🌅', wt: 1, dbl: 1, max: 4, cb: { c: 1, add: 2 }, eff: '此区域所有 1 费卡牌 威力 +2' },
    { id: 'needle',  n: '辉针城', icon: '🏯', wt: 1, dbl: 1, max: 4, inv: true, pick: 0.28, eff: '本区域战力更低的一方获胜' },
    { id: 'tanabata', n: '七夕坂', icon: '🌠', wt: 1, dbl: 1, max: 4, minTurn: 5, eff: '第 5 回合起双方才能在此放牌' },
    { id: 'underworld', n: '冥界', icon: '🪦', wt: 1, dbl: 1, max: 4, all: -2, eff: '此区域所有卡牌 威力 -2' },
    { id: 'reactor', n: '聚变反应炉', icon: '🔥', wt: 1, dbl: 1, max: 4, purge: true, eff: '每回合结束：摧毁本区全场战力最低的牌（并列全删）' },
    { id: 'geyser',  n: '间歇泉', icon: '♨️', wt: 1, dbl: 1, max: 4, decay: 1, eff: '每回合结束：此区域双方所有已翻开卡牌 威力 -1（永久）' },
    { id: 'terakoya', n: '寺子屋', icon: '🏫', wt: 1, dbl: 1, max: 4, grow: 1, eff: '每回合结束：此区域双方所有已翻开卡牌 威力 +1（永久）' },
    { id: 'komakusa', n: '驹草赌场', icon: '🎰', wt: 1, dbl: 1, max: 4, gamble: 1, eff: '在此区域翻开的卡牌：永久随机 +1 或 -1 战力（各半）' },
    { id: 'dicetable', n: '骰子赌桌', icon: '🎲', wt: 1, dbl: 1, max: 4, dice: { turn: 4, n: 1 }, eff: '第 4 回合结束：此区域所有卡牌各自随机永久 +1 或 -1 战力' },
    { id: 'ghostmansion', n: '幽灵洋馆', icon: '🏚️', wt: 1, dbl: 1, max: 4, spawn: { card: 'ghost', n: 1 }, eff: '区域出现时：双方各生成 1 张「幽灵」' },
    { id: 'concert', n: '演唱会', icon: '🎤', wt: 1, dbl: 1, max: 4, rally: { turn: 5, add: 2 }, eff: '第 5 回合结束：此区域双方所有已翻开卡牌 威力 +2（永久）' },
    { id: 'magicstorm', n: '魔力风暴', icon: '🌪️', wt: 1, dbl: 1, max: 4, gust: true, eff: '在此区域翻开的卡牌：揭示效果结算后，若可能就移动到另一个随机区域' },
    { id: 'barrier', n: '幽明结界', icon: '⛩️', wt: 1, dbl: 1, max: 4, collapse: { cards: 6, to: 'underworld' }, eff: '每回合结束：若此区域双方总卡牌数 ≥ 6，则本区域变成「冥界」' },
    { id: 'dormouse', n: '睡鼠神祠', icon: '🐭', wt: 1, dbl: 1, max: 4, prot: true, eff: '此区域双方卡牌都不会被摧毁' },
    { id: 'fairyshrine', n: '妖精神社', icon: '🏮', wt: 1, dbl: 1, max: 4, spawn: { cost: 1, n: 1, reveal: true }, eff: '区域出现时：双方各生成 1 张随机 1 费卡牌（并结算其揭示）' },
    { id: 'sealingclub', n: '秘封俱乐部', icon: '📿', wt: 1, dbl: 1, max: 4, xformTurn: { turn: 5 }, eff: '第 5 回合开始时：本区域变成随机另一个地形（并结算其「出现时」效果）' },
    { id: 'yukarihouse', n: '八云紫的家', icon: '🌌', wt: 1, dbl: 1, max: 4, gap: 1, eff: '回合结束时：双方各从后往前添加 1 张「隙间」（该侧放满则本次不加）' },
    { id: 'houraipharmacy', n: '蓬莱药局', icon: '💊', wt: 1, dbl: 1, max: 4, noDown: true, eff: '此区域双方所有卡牌都不会被减攻（不会获得负战力）' },
    // 🌊 v202：静海（mute: true）——**区域「抹除文本」**的首个（也是当前唯一）使用者：
    //   本区域**双方所有卡牌**（含暗牌、落场 token 与法术）**失去卡牌文字**，卡面效果一律不发动。
    //   口径见上方 `mute=` 字段说明（实时判定、零状态；静海被换掉或卡被移出即恢复；
    //   揭示/持续/时机/防护/`fly` 全部失效，而 `costDown`/`gs`/`playReq`/数值与区域类效果不受影响）。
    //   引擎侧只有 `locMuted` / `cardMuted` / `muteSkipLog` 三个收口 + 若干守卫点（js/game.js）。
    { id: 'calmsea', n: '静海', icon: '🌊', wt: 1, dbl: 1, max: 4, mute: true, eff: '此区域双方所有卡牌失去卡牌文字（卡面效果一律不发动）' },
    // 🌕 v203：虚假之月（`extraRound: 1`）——**本局延长回合**的首个（也是当前唯一）使用者：
    //   只要本局场上（当前三列）有它，本局就是 **7 回合**，且第 7 回合双方能量 **7 点**。
    //   口径见上方 `extraRound=` 字段说明（实时判定 + 进入第 7 回合后锁定 + 不叠加）；
    //   引擎侧只有 `fakeMoonOnField` / `roundsTotal` / `syncRoundTotal` 三个收口 + 四个消费点
    //   （终局判定 / 能量基数 / `energyNext` 终局边界 / 顶栏「回合 N / 总数」）。
    //   pick 显式写 1（与其它地形等概率，用户口径）：池内地形每局抽 3 块 ⇒ 约 3/池内种数 一遇
    //   （v205 加入「天界」后池内共 27 块 ⇒ 约 3/27 ≒ 11%）。
    //   ⚠️ 实战中**主动变出**它的唯一手段＝**v204 的 3 费法术「天文密葬法」**（`k:'xform'` + `xf:'falsemoon'`）；
    //      变走它可用鬼人正邪（→辉针城）/ 灵乌路空（→聚变反应炉）/ 随机变形 / 开发者「🗻 指定地形」。
    { id: 'falsemoon', n: '虚假之月', icon: '🌕', wt: 1, dbl: 1, max: 4, pick: 1, extraRound: 1, eff: '此地形在场时：本局共有第 7 回合（第 7 回合能量 7）' },
    // ☁️ v205：天界（`shatter: true`）——**「出现时摧毁另外两块地形」**的首个（也是当前唯一）使用者：
    //   天界在本区「出现」的那一刻，把另外两块地形连同其上的**所有卡牌**一并摧毁（无视一切防护），
    //   本局此后**只剩本区域可用**；被摧毁的两列变成「已破碎」（看不出原地形、无双方总点数、不能放牌）。
    //   口径见上方 `shatter=` 字段说明与 docs/现有地形.md 的「天界」条。
    //   pick 显式写 0.28（与辉针城同款稀有度）：避免「一局只剩一个可用区」这种剧变机制太常见。
    //   ⚠️ 它自己是“存活”的那一块；被它换掉/变走（xform / xformTurn / collapse / 开发者工具）后，
    //      新地形照常生效，而**已破碎的两列永久保持破碎**（不会被二次摧毁、也不能被再换地形）。
    { id: 'heaven', n: '天界', icon: '☁️', wt: 1, dbl: 1, max: 4, pick: 0.28, shatter: true, eff: '出现时：摧毁另外两块地形（连同其上的所有卡牌），此后只剩本区域可用' },
  ],

  /* ---- 非随机地形（EXTRA）----
     不放进 POOL → 绝不会被每局开局的三区随机抽选抽到；仅作为“可按 id 引用的地形”存在。
     现仅「未揭示」：v74 起作为**每局开局三列的初始未揭示态**，由地形揭晓系统
     （game.js locationRevealStage）在第 1/2/3 回合开始时依次揭晓为真实地形。
     样式 = 基础面板（与「无名之丘」同款）。 */
  EXTRA: {
    // 未揭示：max 4 / 无特殊效果 / 样式与无名之丘相同 / 效果文案（中间带备注）显示「未揭示地形」。
    // ⚠️ id 不要用 'hidden'——全局 UI 有 .hidden{display:none} 类，列 class 直接取 def.id 会整列隐藏。
    unreveal: { id: 'unreveal', n: '未揭示', icon: '❓', wt: 1, dbl: 1, max: 4, eff: '未揭示地形' },

    // 「已破碎」（v205）：天界 `shatter` 摧毁后的占位地形 —— 被摧毁的两列都换成它。
    //   · `max: 0`   ⇒ `locSideMax` / `sideRoom` 恒为 0：双方都不能再在该区放牌 / 移动 /
    //                  落场生成 / 复活；`locOpen` 另有显式守卫恒为 false。
    //   · `wt: 0`    ⇒ 即使漏掉显式排除，也不会计入“赢下的区域数”（终局另有显式跳过）。
    //   · 外观由渲染层**整列替换**（`.location.shattered-loc` + `.shatter-block`）：看不出原地形、
    //     没有双方总点数的数字、也没有格位，只写「已破碎」。
    //   ⚠️ 它**不进 POOL**，故绝不会被开局随机抽到，也不会成为 `xformTurn` / `collapse` 的目标
    //      （两者都只从 POOL 取候选）；`id` 也不要用 'hidden'（与全局 .hidden 隐藏类冲突）。
    shattered: { id: 'shattered', n: '已破碎', icon: '💥', wt: 0, dbl: 1, max: 0, eff: '此区域已被摧毁：不能放牌、不计分' },
  },
};
