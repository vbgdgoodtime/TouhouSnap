# 东方逆转 · 联机现状（实时房间 / 挑战码 / 重放）

> **联机对战已上线可用**：房间服务绑了自定义域名 **`pvp.2houvv.xyz`**，国内可直接连、**不需要梯子**。
> 读文档的顺序：本文件（当前状态与交接）→ `docs/联机对战.md`（联机口径的权威，**改动以它为准**）→ `PvP联机UI待办.md`（下一步的界面工作）。
> 开工前先读 `docs/开发规则.md`：**★规则 1** 只写当前内容、一切改动先精简；**★规则 6** 验证由用户负责
> （不跑测试 / 不开浏览器 / 不起本地服务器 / 不读 `js/ai.js` 与 `tests/`）。

## 1. 现在是什么形态

- **实时房间**（主页面「🌐 联机对战」，客户端 `js/net.js` 的 `window.Net`）：云上一台**只转发消息**的房间
  （`worker/`，一个房间一个 Durable Object，绑定名 `ROOMS`），**对局完全跑在两端浏览器里**，每回合各交一手。
  服务端不是裁判：不校验合法性、不算分、不判胜负。
- **挑战码**（`TH2P2:`；导出在结算弹窗「📤 生成挑战码」，导入在主页面「🔗 挑战码」）：把一局烘焙成一行文本码，
  另一端凭码把整局重算出来（零后端）；重放分**复盘**与**同一局面挑战**两种模式。
- 两条路共用同一套地基：**一局的完整输入 = 种子 + 双方卡组 + 建牌路径 + 数据版本 + 双方每回合的动作**。
- **明确不做**：断线重连、掉线托管（等不到提交包就判那一方认输）、防作弊（两端各算一遍，改内存就能作弊；
  对账只能发现"算不到一起"，不能证明是谁改的）、联机对局生成挑战码。

### 部署事实

| 项 | 当前值 |
| --- | --- |
| 房间服务地址 | `js/net.js` 顶部 `SERVER = 'https://pvp.2houvv.xyz'`（自定义域名，代码自动换成 `wss://`） |
| 连通性 | 国内**直接连得上、不需要梯子**；`*.workers.dev` 那个备用地址在国内常不稳，只作备用 |
| worker 部署 | 已按 `worker/src/index.js` 的当前代码部署（`start` / `rematch` 到达时清空去重表那一条已生效） |
| 卡牌数据哈希 | `Game.dataHash()` → `2fa1d6d2`（数据文件未动就应仍是这个；握手时两端必须相等） |

## 2. 架构事实（读联机代码前先知道这些）

- **两条互相独立的随机流**：`rng()`＝主逻辑流（洗牌 / 抽地形 / 翻牌先后 / 掷骰 / 并列取谁 / 随机取牌）；
  `aiRng()`＝只给 AI 决策用。**必须分开**：AI 的随机不属于"一局的输入"——同一个种子 + 同一串动作必须算出同一局
  （挑战码重放、联机两端对账都靠这条）；AI 若吃走主逻辑流，随机流的位置会随它思考几次而漂移。
  阶段③ 把全局 `Math.random` 临时换成 `aiRng`（**只在已播种时替换**），`NATIVE_RANDOM` 是脚本加载时捕获的原生引用
  ——回退必须指向它，否则 `aiRng` 内部再调 `Math.random()` 就是自己调自己。
- **表现随机**（粒子 / 飘字 / 装饰）仍走原生 `Math.random`，不参与复现。
- **每局自动播种**：`restart(opts)` 用 `opts.seed`，不传就取一个新随机种子；`Game.seed(n)` **已移除**，要指定种子用 `Game._newGame(n)`。
- **对局主循环** `playRound(gen)`：① 回合开始 → ② `await waitPlayer()` → ③ 对手 → ④ 翻牌揭示 → ⑤⑥ 回合末 → 终局判定。
  阶段③ 依次判断：**重放**（取记录）→ **联机**（`Net.awaitRemoteTurn` 等对手提交包，等不到就判他认输）→
  **开发调试**（AI 不出牌）→ 否则 `aiThink()`。
- **联机对账点**在阶段④ 之前：双方动作都灌完、翻牌还没开始，两端各报一次状态指纹（`Net.reconcileRound`）；
  不一致立刻停下并报出回合号与两个指纹。**取指纹之前必须先 `canonicalizeFieldQueue()`**（见 §4）。
- **记录的两个提交点**：`recordTurnInputs('p')`（`waitPlayer()` 返回之后）、`recordTurnInputs('a')`（阶段③ 之后）；
  移动（`fly`）不在 `playerMoves`/`aiMoves` 里，是从 `flyMovedFrom` + 该方场上所在区域**反推**的。
- **重放 / 联机状态由 `restart` 每局显式设定或清空**（`opts.replay` / `opts.netPvp` / `state.netRole`），不靠"记得手动清"。
- **房间座位序 `state.seatOrder`**：联机两端各自把本地玩家当 `p`、状态互为镜像，字面座位在两端指向不同的人；
  凡是"按座位遍历、消耗随机数或决定先后"的地方都必须走它（**房主在先**），否则同一颗随机数会派给不同的人。
  单机恒为 `['p','a']` ⇒ 单机行为与从前一字不差。覆盖清单见 `docs/联机对战.md` §5。
- **联机每次开局都是"双方各带 12 张自建卡组"**（`opts.netPvp`），建牌顺序一律**房主在先**（卡牌 id 与卡组顺序两端才会一致）。

## 3. 已实装的接口与探针

`window.Game`（联机相关）：

| 接口 | 用途 |
| --- | --- |
| `dataHash()` | 数据内容哈希（卡池 / 特殊卡 / 阵营分组 / 地形；排除 `img`/`i`），握手校验用 |
| `_fingerprint()` | 状态指纹：回合 / 阶段 / 赌注 / 加倍状态 / 三块地形 / 放置队列 / 双方（**按座位序取、键名 `s0`/`s1`**）能量·手牌·牌库·三区·三个特殊牌池 |
| `_inputs()` | 这一局由哪些输入决定（双方卡组顺序 / 地形 / 起手 / 能量）——指纹不一致时先比它 |
| `_newGame(seed, opts)` | 设种子重开并等到「停在等你操作」，返回该时刻指纹；`opts.freshDeck` 强制默认曲线卡组、`opts.autoPass` 自动打完并返回终局指纹 |
| `_record()` / `_code()` / `_replayInfo(code)` | 本局录制内容 / 一行挑战码 / 把码解回可读结构（与 `_record()` 同构，可 `JSON.stringify` 往返比对） |
| `_replay(code, { mode })` | `review` 返回终局指纹；`shadow` 等到「轮到你」时交还控制权并返回那一刻指纹 |
| `net.restart / localPackage / applyPackage / applyPeerSnap / applyPeerRetreat / forfeitPeer / forfeitSelf / fingerprint / matchOver / netReopenResult` | 联机引擎侧挂钩（`js/net.js` 调用）：按握手结果开局 / 打提交包 / 灌包 / 加倍 / 认输 / 超时判负的两端 / 一局是否已结束（`phase === 'over'`）/ 重开结算弹窗 |

`window.Net`（`js/net.js`）：

| 接口 | 用途 |
| --- | --- |
| `active / awaitRemoteTurn / localTurnStarted / submitLocalTurn` | 是否在联机局中 / 等对手提交包（`null`＝超时）/ 轮到你了 / 交本机这一手 |
| `sendSnap / sendRetreat / sendTimeout / reconcileRound / abort` | 即时广播加倍与认输 / 通知对手"你被判超时" / 每回合对账 / 中止本局 |
| `room() / role() / peerOnline() / peerName() / myName() / timeoutSeconds()` | 房间码 / 本机角色（`host`/`guest`）/ 对手在线 / 双方昵称 / 超时秒数 |
| `ui.*` | 房间弹窗、顶栏状态条、对手信息区、房间报错提示、`onMatchEnd` / `rematch` / `kick`（房主专用「🚫 踢出对手」，**当前关闭**）/ `closePeerGone`（"房间没法继续"那层最靠前的提示） |
| `Home.playerName() / Home.playerAvatar() / Home.cleanName() / Home.knownAvatar()` | 本机玩家资料（设置里填的昵称 / 选的卡图头像，存 `localStorage: touhou2.player.v1`）：握手时随 `hello` 发给对手；后两个用于**对手数据的清洗与白名单核对** |

## 4. 容易踩回去的坑（动联机代码前先看）

1. **建牌路径是输入的一部分**：自建 12 张（`buildDeckFromDefs`，洗 1 次）与费用曲线随机组（`buildDeckCards`，每档各洗一次 + 再洗一次曲线）**消耗的随机数个数不同**——走错一条，后面整条随机流错位，对手牌组与三块地形全都会变。
2. **卡组不能再洗一遍**：记录里存的是**洗完之后**的顺序，而 `buildDeckFromDefs` 的洗牌输入是玩家选的顺序 ⇒ 重放必须走 `buildDeckInOrder`（不洗，但照样消耗那 11 次随机数，并按记录的倒序建牌以还原卡牌 id）。症状：**对手牌组一致、我方"牌一样但顺序不同"**。
3. **静态托管 ≠ 同一版本**：朋友可能跑着缓存里的旧页面。所以握手比两项——**数据哈希**（抓数据改动）与**页面引擎指纹**（本页所有 `<script src>` 源码的哈希，抓旧页面）；文案里都带"先按 Ctrl+F5"。
4. **AI 的随机不能进主逻辑流**，也不能用未播种的 `Math.random`；阶段③ 替换全局 `Math.random` 时，任何"回退到 `Math.random`"的代码都会变成无限递归（回退只能指向 `NATIVE_RANDOM`）。
5. **弹窗层级**：`.modal-mask` 统一 `z-index: 50`，同层时 DOM 靠后的盖住靠前的；主页面态由 `body.in-home …:not(.hidden)` 各自抬高（`#netMask` 212，与挑战码同层）。
6. **核对卡组的时机**：必须在两副牌都还是**完整 12 张**时比（`startRecord` 内）；晚一步就被抽掉 3 张，永远比不相等。
7. **`decksOk` 里真正有判断力的是"对手牌组"**：我方牌组是按记录建的（必然相等），对手牌组仍由种子派生——随机流一错位就先在它上面暴露。
8. **不符时只拒绝、不硬跑**：宁可中止并说明，也不要跑出一局"看起来正常"的假结果；**中止要保留现场**（把"本机算出来的"和"码里记的"整份打到控制台）。
9. **镜像座位不是免费的**：两端各自把本地玩家当 `p` 之后，凡按字面座位遍历的地方（取牌 / 掷骰 / 翻牌先后 / 放置队列插序）都会把同一颗随机数派给不同的人 ⇒ 必须走 `state.seatOrder`。**漏掉一处就在那一处开始分叉**，所以每回合都有一次对账：分叉会在**当回合**被报出来。已有的覆盖点还包括**发卡 id 的顺序**（`newCard` 的 `cardSeq++`，地形"出现时"给双方各生成 token 时写死 `'p'` 再 `'a'`，会让同一批 id 落在不同的人身上）。
10. **放置队列跨座位插序**：两端的本回合入队先后天然相反，`canonicalizeFieldQueue` 先把队列排成同一顺序，**再**取对账指纹 —— 顺序反了会必然对不上（那是假警报）。
11. **提交包里的 `side` 是"发送方的本地座位"**，不是包的归属：`p` ＝ 他自己那一手。所以房间的去重键必须是 **`(回合, 发送方角色)`**，用 `msg.side` 会让两个人的包互相顶掉（症状：一方进下一回合，另一方一直卡在"等对手出牌"）。
12. **房间只转发、不保存 ⇒ 先到的人发的消息是"发给空气"的**：房主连上时发的 `hello` 那时房间里还没人，等加入者进来它早没了。**必须在对手进来时把 `hello` / `ready` 再报一次**（`js/net.js` 收到 `peer: online` 时重发）。症状：**房主能开局、加入者卡在"开局信息不全"**。
13. **新一局的回合号从 1 重新数**：worker 在 `start` / `rematch` 到达时**必须清空去重表**，否则第二局的第 1 回合会被当成重复包**静默丢掉**（症状：两端都卡在"等对手出牌"）。
14. **时间线的两处硬约束**：`uiRetreat` 之后主循环要在 `state.phase === 'over'` 时立刻收摊（对手认输可能发生在你正等操作时）；加倍要**当场**广播（对手要在本回合内决定撤退），随后的提交包带同一标记、两端幂等。
15. **不做托管就少了两个漂移源**：本机不替对手出牌、不广播别人的包，确定性只依赖"双方各交一手 + 座位序"。
16. **只认当前那一根连接的事件**：`js/net.js` 的三个 ws 处理器都先判身份（`sock !== ws` ⇒ 丢弃），`connect()` 也**先摘掉 `ws` 再关**上一根连接。
    漏了这个，重试 / 退出房间再进留下的旧连接一旦触发 `onclose`，就会把当前房间的 `peerOnline` 写成 false
    —— **对局照常进行、只有状态条骗人**，而且对方没断线时不会有任何事件把它纠正回来（现由 `peerHeard()` 在收到对方消息时自愈）。
    判断是不是真掉线看这一条：**房间真丢了对方的连接时，本机发给对方的包会被直接丢掉**（`sockets[role]` 已置空），对方的提交包也不会再来。
17. **对手的昵称是"别人给的字符串"，落进 HTML 之前必须清洗**：结算弹窗走 `showModal(...)` 的 innerHTML，而昵称来自对手的 `hello`。
    `js/home.js` 的 `cleanName`（去 `<>&"'` 与控制字符、最多 12 字）**输入时与收到时各过一遍**（`js/net.js` 的 `cleanPeerName`）；
    头像只认**本机真有的那张卡图文件名**（`knownAvatar` 白名单，对不上退回 👤）—— 既防注入，也防两端卡图不一致时的裂图。
    与卡组不同，**昵称 / 头像随时可以改、改完当场生效**：还在房间里时本机会把 `hello` 就地重报一次（`Net.ui.refreshHello`），
    对手那边随即更新；它们不进任何状态指纹，改了不影响"两端算同一局"。
    侧栏那个头像格还有一处要留意：单机口径（`js/game.js` 的 `renderSide`）每次渲染都会把它写成 🤖，所以**联机时改成由 `js/net.js` 独占**
    （`renderSide` 在 `netOn()` 时不碰它），且 `setAv` 的去重既比标记也比内容 —— 否则会被"值没变"骗过去、头像停在 🤖。
18. **"让服务端做事"的指令不能套 `{t:'msg'}` 那层壳**：房间只认**顶层**的 `t`（`bye` / `kick` 都是这样），套进 `msg` 里会被当成普通消息转给对手、什么也不会发生。
    踢人还必须由服务端**关连接**（`{"t":"kick"}` → 先给对方发 `{"t":"kicked"}` 再 `close`）：对手连接半死时（人掉了、服务端还没察觉）
    只有关掉它才会触发 `gone`、把位置与 token 一起释放 —— 房主看到"对手在线"却等不到他，就是这种情形。
19. **初始发牌与 ⓪ 的顺序是"一局的输入"的一部分**：`gs.shuffleN`（7 费哆来咪）重洗牌库用的随机数个数 = **牌库张数 − 1**，
    所以双方初始 3 张必须都在 ⓪ 之后发。曾经对手的 3 张先发（牌库 9 张）、本机的后发（12 张）⇒ 两端洗同一副牌库时消耗的随机数差 30 个、
    洗出来的顺序也不同 ⇒ **第 1 回合对账必不一致**（症状：第 1 回合报"盘面不一致"，而地形、桌面上的一切看起来都很正常——
    地形抽取在 ⓪ 之前，两端当然一样；带 `gs` 的卡开局还会先播一段「登场」演出，看到它就说明这一局有哆来咪）。

## 5. 已定口径（用户拍板）

1. **赌注 / 加倍**（单机与联机同一套，AI 永不加倍）：结算值 `2 × 2^(本局加倍人数)`（无人 2 / 一人 4 / 两人 8）；
   双方**各一次**、整局限用；宣布当回合侧栏就显示新值，**下一回合开始生效**；从宣布到生效回合结束之前撤退按**加倍前的值**；
   **末回合宣布**则直接按新值结算。挑战码为 `TH2P2`（每回合 9 字段），**旧 `TH2P1` 码一律拒绝**。
2. **房间服务放本仓库的 `worker/` 子目录**（跟随版本管理；GitHub Pages 会把它当静态文件一并发布，里面没有密钥）。
3. **不做超时 AI 托管**：等对手的提交包超过 `TURN_TIMEOUT`（90 秒）就**直接判他认输**（走认输那条路，按撤退价结算），
   本机不替他出牌、不接管他的回合；同时发 `timeout` 告诉对手（他真掉线就自然发不到，两端结论一致）。
4. **不做断线重连**：刷新页面＝离开房间，对面会在超时后判你认输；房间因此不保存任何消息日志。
   **手机切到后台同理**（系统掐掉后台连接，回来接不上）—— 这是已确认的现象，改法见 §6 第 5 条。
5. **「手动转发提交包」这条退路不做**：只做实时房间（真要应急，挑战码那条通道可临时顶替）。
6. **房主掉线 / 踢人**：房主掉线 ⇒ 加入者当场弹「房主已掉线，房间已解散」、退房、回主页面（房间没人管了）；
   加入者掉线 ⇒ 房主**不受影响**、留在房间里（可以等他回来）。
   **对局进行中不抢先散房**：仍按"等提交包超时判负"走（谁没交包谁输），判完由 `onMatchEnd` 走到房主掉线那条提示 ——
   这样加入者不会白白丢掉一个"判对方超时"的赢局。
   「🚫 踢出对手」（房主把半死连接的对手移出、腾出位置）**代码与协议都已就位但当前关闭**（`js/net.js` 的 `KICK_ENABLED = false`）：
   它要重新部署 `worker/` 才生效，要用就改那个开关 + `cd worker && npx wrangler deploy`。

## 6. 待办（都不阻塞）

1. **回归基线重新采集**（指纹口径与结算值改过，旧值已失效）：在浏览器控制台跑下面几条，把结果填回来：
   - `await Game._newGame(3, { freshDeck: true })` → （待填）
   - `await Game._newGame(3, { freshDeck: true, autoPass: true })` → （待填）
   - `Game.dataHash()` → `2fa1d6d2`（数据文件未动，应仍是这个）
   - `JSON.stringify(Game._replayInfo(Game._code())) === JSON.stringify(Game._record())` → `true`（往返验证，应仍成立）
2. **赌注细则逐项确认**：撤退保值（加倍生效前认输只输 2）、末回合加倍直接结算、双方各一次。
3. **下一步的界面优化三项**：对手动作的即时提示 / 对局页整体布局 / 主页面与各入口的视觉 —— 逐项要求与红线见 `PvP联机UI待办.md`。
4. **一处注释与实现相反**：`worker/src/index.js` 里 `this.tokens = {}` 那行的行内注释写"断开也让位不了"，
   而 `gone()` 实际是断开就释放该角色（`worker/README.md` 的写法与实现一致）。下次改 `worker/` 时顺手对齐注释。
5. **手机切到后台就断线**（**已确认现象，暂不修**：先只记录，等以后动手）：系统会掐掉后台页面的 WebSocket
   （iOS 基本是切走就断，Android 短时间常能撑一会儿），本机又没有重连逻辑 ⇒ 切去看 QQ / 锁屏回来**接不上这一局**，
   对面在 `TURN_TIMEOUT`（90 秒）后判你超时；后台放久了页面被系统回收，连引擎状态一起丢。四个候选改法（按工作量排）：
   - **C 只改提示**（零风险）：切到后台 / 回到前台时明确提示"切走会断开、超时判你输"；
   - **B 断线时拉长等待**：对面明确掉线时不马上数 90 秒，改成 3~5 分钟，一直不回来才判负（只救"连接侥幸没断"的情形，iOS 救不回）；
   - **A 轻量重连**：回到前台 / 网络恢复时用**同一个 token** 连回原房间、补发握手与**本回合那手包**
     （同一 `(回合, 发送方角色)` 的包房间会去重，补发是安全的）；配套要改 `worker/`：断开后给 token 一个宽限窗口，
     否则极端情况两端会**互换角色**、座位镜像错乱；只在"页面还活着、只是连接断了"时有效；
   - **D 房间日志补跑**：`worker` 存整局消息、重新入房补状态，页面被系统回收也能救 —— 工作量最大（当初明确砍掉的那一套）。

   推荐顺序：**C → B → A**（A 要和 B 一起做才有意义），D 视需要。A / B 会改到 `docs/联机对战.md` §5 的
   「超时判负」与「不做断线重连」两条口径，验收必须两台设备（含手机）实测。
   **同源的一处没修**：本机自己掉线时主循环并不知道（连接断了引擎照旧等对方的提交包），90 秒后本机也会判"对手超时认输"
   —— 对面同时也在判你超时，两端的结果会各说各话。做 A / B 时一并收口（`js/net.js` 已经把 `selfGone` 这个信号记下来了，
   目前只用于状态条文案与"别把断线错认成对手掉线"）。
