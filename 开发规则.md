# 项目开发全局规则（东方逆转 · 纯前端卡牌游戏）

> 本文件是**全局开发规则**，所有改动（加卡、加机制、改数据、改文档）都必须遵守。
> 涉及新卡/新机制的“当前状态”说明，与 `data/现有机制.md`、`data/现有人物卡牌.md`、
> `data/现有特殊卡牌.md`、`data/现有地形.md`、`GAME_REFERENCE.md`（含文末更新记录）保持同步。

## 规则 1：新卡图片的获取（优先自 /图片素材 取图）

添加新人物卡（或给既有卡补图）时，**优先从项目根目录的 `图片素材/` 文件夹寻找该角色的图片**：

1. 在 `图片素材/` 中查找对应角色的立绘，命名通常形如
   `100px-角色名（Q版立绘）.png`（按角色名匹配，留意简体/繁体写法差异）；
2. **找到后**：复制该图片到 `cards-image/` 目录，并**改名**为英文/ASCII 文件名
   （惯例用角色常用罗马音/昵称，如 `hina.png`、`okuu.png`），然后在
   `data/cards.js` 对应卡条目标记 `img: '<文件名>'`；
3. **没有找到**对应角色的图片：**不要用其它图片顶替，也不要静默跳过**——
   必须**提醒用户**（报告缺哪张卡、缺哪个文件、我检查过哪些候选），
   由用户重新下载图片放入 `图片素材/` 后，**再次检查图片是否存在**，确认接入后再完成该卡；
4. 图片规格约定见 `cards-image/README.md`（512×512 正方形为佳；`图片素材/` 内为
   100px Q 版立绘时可直接复制使用，放大视图会偏糊，日后可替换大图）。
5. 特殊卡（token，如石块/厄运/废弃列车）一般**没有图片**，用卡面 emoji 兜底即可，
   无需套用本条；除非用户另行提供了 token 图片。

## 规则 2：改动必须带文档同步

- 新增/修改人物卡 → 同步 `data/现有人物卡牌.md` 与 `GAME_REFERENCE.md` §5（含总数）；
- 新增/修改特殊卡 → 同步 `data/现有特殊卡牌.md` 与 `GAME_REFERENCE.md` token 表；
- 新增/修改区域机制字段 → 同步 `data/现有地形.md` / `data/现有机制.md` / `GAME_REFERENCE.md` §3；
- 新增引擎机制/术语 → 同步 `data/现有机制.md` 与 `GAME_REFERENCE.md`（规则正文 + 更新记录表）；
- `cards-image/README.md` 的文件名对照表与新素材状态也要同步。

## 规则 3：机制术语口径（v58/v59/v60 起）

- **揭示**（原「现身」）= 此牌翻开时立刻执行的一次性效果，后续不再触发；
- **持续**（原「在场光环」）= 此牌翻开后持续存在的效果（如天子→己方石块）；
- **防摧毁**（`surv`）= 被摧毁时不会离场、取而代之永久 −N 战力；
- 历史更新记录行保留当时的旧称，不做追溯改写。
