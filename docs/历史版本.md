# 历史版本记录（东方逆转）

> ⚠️ **本文件暂时停用**：改动**不再**追加到这里、也**不再取版本号**（现行口径见 `docs/开发规则.md` 规则 1 与规则 3）；
> 恢复后再按下面的追加口径继续。以下为 v1 起的历史记录（新→旧）。
> - **只记录「哪个版本改了什么」**：每条一行，不写实现细节、演出与 CSS 数值；某项机制的**现行完整口径**看对应专项文档（`docs/现有机制.md` / `现有地形.md` / `现有人物卡牌.md` / `现有特殊卡牌.md` / `card-image-map.md`）。
> - 专项文档只写当前口径（不标版本号、不保留旧写法），改机制时直接替换，见 `docs/开发规则.md` 规则 1 与规则 3。
> - 缩略记录按**当时的**目录结构、卡名与术语书写（旧称「现身」「在场光环」等保留原样、不追溯改写）；
>   历史路径对照：`cards-image/` → `assets/cards/`、`图片素材/` → `assets/source-art/`、根目录 `game.js` 等 → `js/`、`data/现有*.md` 与 `GAME_REFERENCE.md` → `docs/`。
> - 版本号：全局共用一条递增序列，新增改动取下一个号（本文件停用期间不取号）。
> - 索引与跨模块总纲见 `docs/GAME_REFERENCE.md`。

## 索引

| 版本段 | 主题概览 |
| --- | --- |
| v1 – v10 | 玩法成型：能量预算多张出牌 / 2×2 场地展示 / 数据分层（`data/*.js`）/ 东方换皮 |
| v11 – v20 | 地形机制：`aff` 阵营加成、`fill` 放满加成、`cb` 费用加成、`inv` 反转、`minTurn` 限回合、`all` 全区修正、`purge` 回合末摧毁；token（石块） |
| v21 – v40 | 效果键与卡牌重做：`xform`/`mv`/`give`/`dwh`/`oc`/`fly` 等；战力影响历史、`spawn` 生成、`og` 持续效果 |
| v41 – v60 | 术语统一（「揭示」「持续」）、放置顺序队列 `fieldQueue` + `fx` 时机效果、`switch`/`clone`/`morph`/`gift`/`surv`、防摧毁与 `prot` 雏形 |
| v61 – v80 | 演出系统（±N 气泡 / 碎裂 / 飞行）、`phx` 凤凰重生、`leave` 终局离场、`occ` 大体积、地形揭晓 `locPlan` |
| v81 – v100 | 演出打磨：抽牌入场、生成/加手演出、飞行与换边演出、翻牌缩放、地形换场淡出 |
| v101 – v120 | 页面层：主页面（首页）、卡组设置页面（含本地存档），游戏主逻辑基本不动 |
| v121 – v140 | 卡组页面与手机端 UI、`deck-storage.js` 持久化、首页选出战卡组、AI 独立组牌 |
| v141 – v155 | 开发调试页面与工具、能量分边、地形回合末类机制（`decay`/`grow`/`gamble`/`dice`/`rally`）、v153 结算顺序口径 |
| v156 – v167 | 地形「翻开时/揭示后」类机制（`gamble`/`gust`/`collapse`/`prot`）、`spawn.cost` 与 `reveal`、`fieldTurn` 口径、数值平衡 |
| v168 – v219 | 卡池与机制扩充期：法术与 `spell`/`discard`/`reviveDiscard`/`retrigger`/`gather`/`shuffleIn`/`dwb`/`dwc`/`gs`/`costDown`/`playReq`/`mute`（封印）等效果键、AI 四档与设置页、特殊牌池；地形 `xformTurn`/`gap`/`noDown`/`mute`/`noReveal`/`extraRound`/`shatter`/`repeatReveal`（守矢神社 / 天界 / 静海 / 法界 / 虚假之月等）；卡图与文档口径调整，v214 把演出与间隔口径拆到 `docs/动画演出与间隔.md`，v215 规则 6 升为重要性第二 |

## 逐版本记录（v231 → v1，新→旧）

| 版本 | 变更 |
| --- | --- |
| v231 | 重做「藤原妹红」（2 费 / 3 战力 → 2 费 / 2 战力）：卡级字段 `phx` 由 `phx: 2`（回手并永久 +2）改为 `phx: true`——被摧毁时回手并把**本体战力**翻倍（与揭示键 `x2` 共用收口 `doubleCardPower`，逐次 2 → 4 → 8；本体战力 0 ⇒ 不变、为负 ⇒ 更负），手牌满 7 张仍重生失败；顺带修正 `docs/开发规则.md` 规则 5 的数值示例（「2-3」→「2-2」） |
| v230 | 新增 4 费人物「茨木华扇之臂」（4 费 / 10 战力，🦾，配图 `kasen_arm.png`，素材 `240px-茨木华扇（Q版立绘）.png`，与日后「茨木华扇」本人的 `kasen.png` 分文件）与效果键 `dwOwn`（现作 `fx.turnEnd` 用）：回合结束时摧毁本区域自己一侧一张**随机**卡，候选＝已翻开且**实时战力严格低于此牌**（`cardPowerIn`，含区域加成 / 持续 `og` / 永久增益）、排除自己 / 法术 / `un` 占位卡（含落场 token）；本区免摧毁 `locNoDestroy` ⇒ 整条失效，选中 `ind` ＝摧毁失败、判定结束，`phx`/`surv` 按各自机制结算且都不算被摧毁，真被摧毁才 `recordDestroy` 进摧毁池（喂纯狐 `costDown`）；候选为空则无事发生。卡池 65 → 66 张、4 费组 7 → 8 张，文档同步人物卡总数、效果键表、摧毁点清单与卡图计数（`assets/cards/` 55 → 56 张、有图 52 → 53 张） |
| v229 | 新增 6 费人物「星熊勇仪」（6 费 / 10 战力，🍷，配图 `yuugi.png`，素材 `240px-星熊勇仪（Q版立绘）.png`）与卡级持续机制 `costDownP`（`costDownP: { min: 10, n: 2 }`）：自己场上每有一张**已翻开**、**实时战力 ≥ 10** 的卡，此牌能量消耗 −2（下限 0 费），在手牌 / 牌库里即生效；战力按 `cardPowerIn` 判定（区域加成 / 持续 `og` / 永久增益都算），暗牌、法术与 `un` 占位卡不计（收口 `fieldPowerCount(side, min)`，接进 `cardCost` ⇒ `max(0, def.c + costMod − costDown × destroyCount() − costDownP.n × fieldPowerCount())`）；与 `costUp`/`costDown` 叠加、不改 `def.c`、无改费演出。牌库实例一律补写 `card.side`（`buildDeckCards(curve, side)` / `buildDeckFromDefs(defs, side)`）以便牌库侧判己方。卡池 64 → 65 张、6 费组 9 → 10 张，文档同步人物卡总数、卡级机制表与卡图计数（`assets/cards/` 54 → 55 张、有图 51 → 52 张；顺带修正人物卡总数行 3 费 14 → 15、4 费 8 → 7） |
| v228 | 新增 5 费人物「丰聪耳神子」（5 费 / 0 战力，👂，配图 `miko.png`，素材 `240px-丰聪耳神子（Q版立绘）.png`）与卡级持续机制 `mul`（区域倍率，`mul: 2`）：源卡已翻开且仍在己方场上期间，把它**所在区域自己一侧**的结算总点数整体 ×2（`zoneTotals` ＝ 该侧已翻开卡战力之和 + 放满加成 `fill`，故 `fill` 一起翻倍；只乘总点数、不改任何单张卡的战力数字）；只乘源卡所属的一侧（换边后按新归属方），与地形 `dbl`（双方同乘）可叠加；多个来源各乘一次（两张＝×4）；离场或被封印 / 静海抹除文本即失效；带「持续」标记（`cardHasOngoing` 收 `mul`），横幅悬停与终局结算行标注倍率。卡池 63 → 64 张、5 费组 8 → 9 张，文档同步人物卡总数、卡级机制表与卡图计数（`assets/cards/` 53 → 54 张、有图 50 → 51 张） |
| v227 | 「雾雨魔理沙」数值改为 3 费 / 4 战力（落位 `POOL[4]` → `POOL[3]`，效果不变仍为 `give` 加入「极限火花」）；法术「极限火花」改用新增效果键 `dwh2`：揭示摧毁此区域敌方已翻开、战力最高的**两张**——最高一档 ≥2 张时在档内随机挑 2 张、只有 1 张时连次高一档随机 1 张、候选只剩 1 张就只删 1 张、无候选则跳过；`ind` 只拦下它自己那一张（不改选、不顺位补第 3 名） |
| v226 | 「芙兰朵露·斯卡蕾特」效果键 `dw` → `dwh`（数值不变，仍 4 费 / 3 战力）：揭示摧毁此区域敌方已翻开的战力最高的一张卡（并列最高时随机挑一张）；`dwh` 使用者加入该人物卡 |
| v225 | 新增 2 费人物「少名针妙丸」（2 费 / 2 战力，🔨，配图 `shinmyoumaru.png`，素材 `240px-少名针妙丸（Q版立绘）.png`）与效果键 `reset`（战力归位）：揭示把本区域**双方已翻开**的卡牌（排除法术与 `un` 隙间，含落场 token）战力恢复至**印刷战力** `def.p`——差额 `def.p − cardPower` 作为一次永久修正走 `applyPermBuff` 收口（该牌永久修正补正回 0、台账记本卡、播 ±N 演出）；区域加成 `aff`/`cb`/`all` 与持续 `og` 不受影响、暗牌不参与翻面不补；带 `noDown`（蓬莱药局）的区域“往下修”的差额被拦下。卡池 62 → 63 张、2 费组 11 → 12 张，文档同步人物卡总数、效果键表与卡图计数（`assets/source-art/` 65 → 66 个 PNG） |
| v224 | 新增 6 费人物「绵月丰姬」（6 费 / 0 战力，🌜，配图 `toyohime.png`，素材 `240px-绵月丰姬（Q版立绘）.png`）与效果键 `delCopy`：揭示在本区域己方**已翻开**卡里（候选排除自己 / 法术 / `un` 占位卡，含落场 token）**随机摧毁一张**，只有它**真被摧毁**才把它的**复制体**放到另外两个区域自己一侧各 1 张并立即重结算其揭示；摧毁失败（本区免摧毁 / 目标的 `ind`/`phx`/`surv`）则不生成任何复制体，真被摧毁才 `recordDestroy` 进摧毁池（照常喂纯狐 `costDown`）。复制体＝完整副本（`def` ＋ `buff`/`powerLog`/`muteP`/`costMod` 照抄）⇒ 与「赫卡提亚的分身」的白板 token 刻意不同；不走 `revealRound`（不吃 `gamble`/`gust`）但走 `resolveRevealInZone`（目标区是守矢神社时照常结算两次），分步版摧毁后停 0.3s 收口 `delCopyCore`/`applyDelCopyReveal`。卡池 61 → 62 张、6 费组 8 → 9 张，文档同步人物卡总数与效果键表（顺带修正 5 费组 7 → 8 的漏记与合计 60 → 61）、分步演出键「四个 → 五个」及卡图计数（`assets/source-art/` 64 → 65 个 PNG） |
| v223 | 新增 5 费人物「绵月依姬」（5 费 / 4 战力，🌛，配图 `yorihime.png`，素材 `240px-绵月依姬（Q版立绘）.png`）与效果键 `x2`（自身战力翻倍）：基准＝**本体战力**（印刷威力 + 永久增益，**不含**区域加成与持续光环），增量作为一次永久增益写入 ⇒ 翻倍结果照常叠加地形/光环；按字面严格翻倍（本体 0 → 无变化、为负 → 更负，负增量在 `noDown` 区被拦下），反复触发逐次 4 → 8 → 16；卡池 60 → 61 张、5 费组 7 → 8 张，文档同步人物卡总数、效果键表与卡图计数（顺带修正 `card-image-map.md` 的 `assets/source-art/` 素材数 61 → 64） |
| v222 | 新增 4 费人物「稀神探女」（−1 战力，🌘，配图 `sagume.png`，素材 `240px-稀神探女（Q版立绘）.png`）与效果键 `swapDeck`：揭示把**牌库剩余的牌**（结算当刻快照）的实际费用与实时战力互换——按当前生效值换（含已获加费/减费与永久 `buff`）、写实例级 `costMod`/`buff` 故印刷值不变；新费用 = clamp(当前战力, 0, 6)、新战力＝原费用（不封顶）（纯狐 8/16 → 6/8、依神紫苑 5/−7 → 0/5、法术只换到 0 费）；日志只写张数、不列牌名。卡池 59 → 60 张、4 费组 7 → 8 张，文档同步人物卡总数与卡图计数 |
| v221 | 卡组页新增**导出 / 导入卡组**：一套卡组 ⇄ 一行文本码 `TH2D1:` + **明文卡组名** + `:` + base64(UTF-8 JSON `{n, c:[卡名…]}`)——名字明文前置，一屏多个码时肉眼可辨；卡组名含空白或 `:` 时省略明文名（退回不带名的两段形式），解析两种形式都接受；顶栏「📤 导出 / 📥 导入」共用弹窗 `#deckImportMask`（导出只读展示 + 📋 复制，剪贴板不可用时提示手动复制；导入先校验再弹确认预览，报出码里的卡组名、被跳过的卡名与「超过 12 张只取前 12」），导入只**新建**一套卡组（重名自动加后缀、空名兜底「导入卡组」），不改存档格式 |
| v220 | 规则 1 清理：`index.html` 注释里 34 处版本号（含 `vNNN：` 前缀与「vA→vB」「vNNN 起」等写法）全部删除，只留当前口径；顺带补齐主页面入口清单与「对局使用自建卡组」的表述 |
| v219 | 新增 3 费法术「法界之火」（🔥，无配图走 emoji 兜底，`k:'xform'` + `xf:'dharma'`；纯数据、无引擎改动）：揭示把本区域替换为「法界」（`noReveal` 封锁揭示唯一的主动变出手段）；卡池 58 → 59 张，文档同步「使用者五位」与无图法术清单 |
| v218 | 新增地形「法界」（🌑 `dharma`，wt 1 / dbl 1 / max 4）与区域字段 `noReveal`（封锁揭示）：本区双方所有卡牌无法触发揭示效果（实时零状态，离开本区 / 换地形即恢复；只封揭示——持续 / 时机 / 防护 / 移动与全部地形类效果照常，法术揭示不发动但照常消散；被拦下的那一次不补结算）；判定 `locNoReveal`/`revealBlocked`，拦截点 `applyEffect` 揭示分支 + `resolveRevealInZone` + `revealEffectWillChange`，日志 `revealSkipLog`、放大视图提示 `revealBlockNoteHTML`，墨黑地形配色 + 地名纯白 |
| v217 | 新增 3 费法术「生神停止」（⏳，无配图走 emoji 兜底）与 `mute` 键的三个子句：`side`（`'opp'`/`'own'`/`'both'`）、`n:'all'`（候选全部、一次性全封无逐张停顿）、`has:'ongoing'`（只筛卡面带「持续」标记的卡＝`og`/卡级 `prot`/卡级 `ind`，新判定收口 `cardHasOngoing`）；该卡＝`{ side:'both', n:'all', has:'ongoing' }`，双方所有持续牌整张文本被封印；`KIND_LABEL.mute` 因多使用者改为通用文案 |
| v216 | 新增 2 费法术「阴阳玉」（配图 `yinyang.png`，素材 `240px-THBWiki-LOGO-阴阳玉.png`）与效果键 `mute`（游戏内称「封印」）：揭示把本区敌方**已翻开**卡里实时战力最低的一张**永久**失去卡牌文字（并列随机；标记写在卡实例 `card.muteP` 上、随卡整局有效）；「失去卡牌文字」由此拆成两来源——区域抹除（静海 `mute`，实时恢复）与卡级抹除（封印，永久、离开静海不恢复），判定合并为 `cardMuted` + 新增渲染用 `cardSealed`，`cardAuraBonus` / `powerHistoryRows` / `locNoDestroy` 三处由“查区域”改为“查卡”；封印牌在场上 / 手牌 / 三池 / 放大视图叠 ❗ 并置灰；有图卡由 45 张增至 46 张 |
| v215 | 规则 6 升为重要性第二（仅次于规则 1）：改为「验证由用户负责 + AI 不自行测试 / 不做烟雾测试 / 不开浏览器 + 不读 AI 对战脚本代码与 `tests/smoke-test.js`」 |
| v214 | 文档拆分：演出、动画与间隔口径迁入新增的 `docs/动画演出与间隔.md`，`docs/现有机制.md` 只留演出名与指针 |
| v213 | 新增人物「梅莉」（1 费 / 2 战力）与效果键 `xformR`：揭示把本区换成随机另一地形并结算其「出现时」，候选同 v199、不做上限防御 |
| v212 | 新增 4 费法术「星之仪式」（无战力）：揭示把本区域替换为「守矢神社」（`xform` + `xf`）；也是唯一能主动变出守矢神社的手段 |
| v211 | 「东风谷早苗」的 `retrigger` 新增可见演出：她卡面播一圈淡绿光环扩散、被再触发的每张卡各闪一下 |
| v210 | 修订「守矢神社」`repeatReveal` 的触发面：本区发生的揭示都执行两次（停 400ms 后重复），早苗再触发出来的揭示也计入 |
| v209 | 修：被再触发的每张揭示改走标准揭示流程，故 `shift`/`gather`/`reviveDiscard`（乃至另一张早苗）都保留各自分步间隔 |
| v208 | 新增地形「守矢神社」（🌾 `moriyashrine`，max 4，`pick` 1）与区域字段 `repeatReveal`：在本区翻开的卡其揭示执行两次（第 2 次在 400ms 后） |
| v207 | 新增人物「东风谷早苗」（5 费 / 6 战力，🌾）与效果键 `retrigger`：把同区己方已翻开的每张卡各自再触发一遍；「静海」里的卡照常不发动 |
| v206 | 开发调试新增「🪨 添加石块」工具（仅调试显示）：按地形「出现时」口径给指定区域双方各生成若干张「石块」 |
| v205 | 新增地形「天界」（☁️ `heaven`，`shatter`）：本区「出现」时摧毁另外两块地形及其上所有卡，此后只剩本区域可用 |
| v204 | 新增 3 费法术「天文密葬法」（🌑，无战力）：揭示把本区域替换为「虚假之月」（`xform` + `xf`），纯数据改动 |
| v203 | 新增延长回合机制 `extraRound`（「虚假之月」）：在场时本局由 6 回合变 7 回合、第 7 回合能量 7；进入第 7 回合后锁定、不叠加 |
| v202 | 新增地形「静海」（🌊 `calmsea`，max 4）与区域字段 `mute`（抹除文本）：本区双方所有卡失去卡面文字、任何时机都不发动效果，数值与 `occ` 占格照常 |
| v201 | 新增区域字段 `noDown`（「蓬莱药局」免减攻）：本区双方所有卡无法被减少战力，负增量在生效前被拦下、不写 `powerLog` |
| v200 | 新增区域字段 `gap`（「八云紫的家」回合结束封格）：每回合末双方各从后往前封 N 格（缺省 1、至多封到刚好放满），可用格数随之减少、下限 0 |
| v199 | 新增区域字段 `xformTurn`（「秘封俱乐部」定时变形）：第 N 回合开始时把本列地形换成随机另一地形，候选＝除自身外全部地形、允许与另两列重复 |
| v198 | 新增人物「姬虫百百世」（2 费 / 2 战力）与 `discard` 的 `give` 子句：弃牌后每弃 1 张即加入 1 张「石块」到手牌，战力＝被弃牌的印刷费用 |
| v197 | 新增时机键 `fx.handEnd`（手牌回合结束）；「稗田阿求」重做为 1 费 / 4 战力，回合结束若仍在手牌则自动丢弃，`leave` 自此无使用者 |
| v196 | 新增人物「大鲶鱼」（6 费 / 16 战力）与卡级机制 `playReq`（`{ tk:'rock', n:4 }`）：只在己方场上已翻开的「石块」≥4 张时才打得出来 |
| v195 | 新增效果键 `dwc`（3 费法术「妖精大战争」）：揭示摧毁双方场上所有印刷费用为 1 的卡牌，逐区逐张 |
| v194 | 新增卡级机制 `costDown`（8 费「纯狐」）：双方每有一张牌被摧毁此牌费用 −1、下限 0，在手牌/牌库即生效；洗入池放宽到 8 费 |
| v193 | 「四季映姬」的复活弃牌池改为「逐张」分步演出：每张复活后立即结算其揭示、停 500ms 再推进，避免一次复活太多 |
| v192 | 新增人物「四季映姬」（6 费 / 6 战力）与效果键 `reviveDiscard`：把自己弃牌池里所有角色卡牌随机复活到随机区域 |
| v191 | `discard` 的 `pick` 新增 `'maxCost'`（取印刷费用最高者、并列随机弃一张），新人物「小野塚小町」（3 费 / 5 战力）用它弃最贵的手牌 |
| v190 | `discard` 新增 `pick` 取牌位置（`'random'` / `'right'` / `'left'`）；新人物「莉莉黑」（1 费 / 3 战力）弃自己最右侧 1 张手牌 |
| v189 | 新增效果键 `discard`（弃牌）：把牌从手牌移出、以其本体放进归属方弃牌池（先进先出），并有双方可见的弃牌演出 |
| v188 | 「西行寺幽幽子」数值下调：揭示 `de` 由 −3 降为 −2（仍 4 费 / 4 战力） |
| v187 | 新增三个牌池与侧栏「特殊牌池」弹窗（摧毁池 / 弃牌池 / 放逐池），入池的是牌本体、先进先出；只有真正离场的摧毁才进摧毁池 |
| v186 | 手牌上方的能量条只画本回合当前可用能量（`energyLeft`）且全为橙色，不再补灰色占位 |
| v185 | 新增卡级机制 `gs`（现仅 7 费「哆来咪」）：开局在自己牌库/起手中即触发，洗入 10 张随机牌、每回合最大能量 +1 |
| v184 | 新增效果键 `shuffleIn`（洗入卡组）；「琪露诺」重做为 1 费 / 2 战力并向对手牌库洗入 1 张「冰块」，界面补「牌库」计数 |
| v183 | 新增人物「莉莉霍瓦特」（1 费 / 1 战力，无阵营）：揭示 `bf` +1，同区己方其他已翻开卡牌 +1，与当时的「琪露诺」同款 |
| v182 | 法术「三妖精集结」配图接入（`fairies.png`，448×488，非 240×240 立绘、原样不裁切），有图卡由 38 张增至 39 张 |
| v181 | 主页面新增「📚 图鉴」入口（列在「卡组设置」与「设置」之间），只打开既有图鉴弹窗；卡池排序补「卡名」 |
| v180 | `give` 新增 `pool` 写法（从池里不放回随机抽 n 张），「蓬莱山辉夜」抽 2 张神宝；`ind` 卡本版起照常参与最强/最弱判定 |
| v179 | `spawnS` 增 `fill: true` 写法（「镇守大地之石」填满该侧空位、法术先消散让格）；「蕾米莉亚·斯卡蕾特」每回合末落后即永久 +3 |
| v178 | 法术卡框的战力位改为只画 ✦ 星标、不再写「法术」二字；衍生卡单列为第二层卡池，弹窗筛选改为两层 |
| v177 | 「雾雨魔理沙」重做为 4 费 / 6 战力 `give`，揭示时把新法术「极限火花」（`dwh`）加入手牌；`dwh` 并列改为随机挑一张 |
| v176 | AI 档位文案「去算法化」：`desc`/`tip` 与设置弹窗「关于对手」只写中性难度感受，不透露搜索方式与算法深浅 |
| v175 | AI 难度扩为四档并调整默认值：简单（默认，原样恢复 v117~v172 的贪心 AI，不接受「不出牌」）/ 普通 / 困难 / 月狂 |
| v174 | 主页面新增「⚙️ 设置」入口（紧接「图鉴」下方）：弹窗切换对手 AI 强度，点选即生效并记住（写入 `localStorage`） |
| v173 | AI 策略重做：法术估值改为按效果推演（不再一律滞留）、`gather` 按成员平均战力推演，困难/噩梦档也会移动；AI 不再加倍 |
| v172 | 新增主页面「📖 新手引导」入口与机制速览弹窗；效果键新增 `dwb`（摧毁双方最弱随机一张）、本区自己一侧生成与 `drawSpell` |
| v171 | 新增 `gather`（「三妖精集结」，`{ group:'light', add:1 }`）：逐区生成 1 张该阵营妖精并立即结算其揭示，翻牌流程每区停 0.5s |
| v170 | 新增卡级机制 `spell`（法术）：只有能量花费与「揭示」效果、无战力，结算完自行消散并进放逐池 |
| v169 | 新增 `costUp`（「桑尼米尔克」让对方手牌随机 1 张费用 +1）与 `energyNext`（「斯塔萨菲雅」让自己下回合多 N 点能量） |
| v168 | 文档口径：机制与地形改由各专项文档作唯一权威口径，`GAME_REFERENCE.md` 不再收录细节与历史记录，v1~v167 记录精简迁入 `docs/历史版本.md` |
| v167 | 数值平衡：藤原妹红 1费/1战力 → 2费/3战力（退出「妖精神社」池）；鬼人正邪 10→6、灵乌路空 6→4、伊吹萃香 12→10 |
| v166 | `spawn` 新增 `reveal: true`（妖精神社）：出现的卡立即结算自身揭示；缺省不触发 |
| v165 | 新增地形「妖精神社」（🏮 `fairyshrine`）：`spawn` 支持 `{ cost, n }`，从 `POOL[cost]` 随机抽人物卡（排除 `un`/`occ>1`） |
| v164 | 新增地形「睡鼠神祠」（🐭 `dormouse`，`prot: true`）：本区卡牌免摧毁，`dw`/`dwh`/`purge` 失效、不触发 `surv`/`phx` |
| v163 | 新增地形「幽明结界」（⛩️ `barrier`）与 `collapse: { cards, to }`：回合末本区总卡数达标即整体换 `to`（冥界），只崩塌一次 |
| v162 | 新增地形「魔力风暴」（🌪️ `magicstorm`，`gust: true`）：本区本回合翻开的卡揭示后移到另一随机区域 |
| v161 | 新增地形「演唱会」（🎤 `concert`，`rally: { turn: 5, add: 2 }`）：第 5 回合末本区双方已翻开卡牌永久 +2（含 token） |
| v160 | 「回合开始」时机只结算本回合开始前已在场上的卡（`fieldTurn`）；洋馆当回合生成的幽灵下回合起才游走 |
| v159 | 新增地形「幽灵洋馆」（🏚️ `ghostmansion`）：出现时双方各生成 1 张「幽灵」；「幽灵」费用 1 费 → 0 费（战力仍 1） |
| v158 | 新增衍生卡「幽灵」（👻 `SPECIAL.ghost`）与效果键 `roam`（自身移到另两区随机一处）；以 `fx: { turnStart: { k: 'roam' } }` 注册 |
| v157 | 新增地形「骰子赌桌」（🎲 `dicetable`，`dice: { turn: 4, n: 1 }`）：第 4 回合末本区所有卡各自随机永久 ±1（含 token） |
| v156 | 新增地形「驹草赌场」（🎰 `komakusa`，`gamble: 1`）：区域「翻开时」效果——本区翻开的卡永久随机 ±1（各 50%） |
| v155 | 新增地形「寺子屋」（🏫 `terakoya`，`grow: 1`）：每回合末本区双方已翻开卡牌永久 +1（含 token） |
| v154 | UI：出战卡组选择弹窗卡组位只显示卡组名，12 张信息移到悬停提示 |
| v153 | 新增地形「间歇泉」（♨️ `geyser`，`decay: 1`）：每回合末本区双方已翻开卡牌永久 −1（含 token、非摧毁）；回合末先地形、后「回合结束」卡牌效果 |
| v152 | 新增衍生卡「河童」（🥒 `SPECIAL.kappa`，3费/4战力，白板）与地形「黄瓜田」（`cucumber`）：出现时双方各生成 1 张河童 |
| v151 | `xform` 变形也结算目标地形的「出现时」效果 |
| v150 | 开发调试「🗻 指定地形」改为默认结算「出现时」效果，可勾选关闭 |
| v149 | 开发调试「🗻 指定地形」修复区域 2/3 点不到：区域选择条移到标题下方三等分 |
| v148 | 开发调试新增「🗻 指定地形」：选地形 + 区域 1/2/3 立即替换该列（同步 `locPlan`） |
| v147 | 开发调试：侧栏对手信息新增当前能量（剩余 / 本回合上限） |
| v146 | UI：正常对局隐藏顶栏「修改能量」「指定卡牌」与侧栏「👁️ 查看」 |
| v145 | 能量分边：`players.p` / `players.a` 各持 `energyTotal` + `energyLeft`，出牌与重置只动己方 |
| v144 | 开发调试新增「⇄ 切换立场」：暗出的牌落到对手区域且 `card.side` 为对手 |
| v143 | 开发调试：能量固定 10；三块地形固定「无名之丘」；AI 不出牌（仍抽牌） |
| v142 | 新增开发调试页面（`dev-page.js`）：不校验卡组直接进战斗页，玩家牌库为空 |
| v141 | UI：对手侧栏新增「👁️ 查看」，弹窗展示 AI 手牌 + 牌库卡面 |
| v140 | UI：顶栏 HUD 回合数左侧新增「← 主页」按钮 |
| v139 | 抽牌去掉起手友好拒绝采样与兜底序列，一律纯随机洗牌 |
| v138 | AI 开局按 `1×2, 2×3, 3×3, 4×1, 5×1, 6×2` 的费用结构随机组牌 |
| v137 | 「开始对战」先弹出出战卡组选择（满 12 张的卡组），玩家牌库改用该组卡牌 |
| v136 | 卡组设置：卡序改为编辑中即时整理（费用→战力重排），「💾 保存」只写入本地 |
| v135 | 卡组设置：经 `deck-storage.js`（`localStorage` 键 `touhou2.decks.v1`）持久化 |
| v134 | 卡组设置手机端编辑态：「卡组名 N/12」并入顶部栏同一行 |
| v133 | 卡组设置卡池：卡面高度统一（无图卡补占位、效果文字固定两行） |
| v132 | 卡组设置卡池：筛选后卡少时不再被纵向拉长 |
| v131 | 卡组设置卡牌区新增费用筛选条 `#deckFilter`（含「衍生卡牌」档，token 仅可查看） |
| v130 | 卡组设置编辑态标题去掉「正在修改：」前缀 |
| v129 | 卡组设置：点卡组位直接进入编辑态；新增卡组改名（非空 / ≤12 字 / 不重名） |
| v128 | 卡组设置手机端卡池：最小列宽 84→74px、每行 ≥4 张 |
| v127 | 卡组设置编辑态：空卡槽改为与卡面同构的「幽灵卡」 |
| v126 | 卡组设置编辑态：卡池中已在卡组的卡变灰不可点；卡槽改自适应网格（宽屏 1 行 12 格） |
| v125 | 卡组设置实装修改卡组（编辑态）：12 卡槽 + 「🧹 清空卡组」「✓ 完成修改」，左键加入/移出 |
| v124 | 卡组设置：卡组位悬浮 ✏️ 修改 / 🗑️ 删除 按钮（本版只实装删除） |
| v123 | 卡组设置：卡组栏顺序改为创建时间倒序（「＋」固定最左） |
| v122 | 卡组设置：卡组栏改为动态列表（「＋ 新建卡组」+ 已建卡组位，上限 20 套） |
| v121 | 新增卡组设置页面（`#deckScreen` + `deck-page.js`）：上 1/3 卡组栏（20 套 × 12 张）+ 下 2/3 卡牌区；仅框架 |
| v120 | 新增主页面（首页）：先显示 `#homeScreen`，交互在新文件 `home.js`；「开始对战」调 `Game.restart()` |
| v119 | 演出微调：开场抽完 3 张到第 1 回合开始的间隔 1s → 500ms |
| v118 | UI：「认输」与「结束回合」之间新增手牌数提示（`N/7`） |
| v117 | 代码拆分：`hypotheticScore` / `aiThink` 移到独立文件 `ai.js` |
| v116 | AI：不再主动往反转区（辉针城 `inv`）放牌 |
| v115 | UI（手机端）：手牌与能量点左右内边距 84px → 14px |
| v113 – v114 | UI（手机端）：手牌高度略减（角标遮住立绘顶部），随后压入程度减半 |
| v112 | UI（手机端）：卡牌详情弹窗整体收小（按 `vw` 缩放） |
| v110 – v111 | UI（手机端）：图鉴/「指定卡牌」弹窗只隐藏效果文字（保留卡名/费用/战力/图） |
| v109 | UI（手机端）：手牌区整体上移，避免被角落按钮遮住 |
| v108 | UI 更正（手机端）：区域效果说明保留在中间带原位（撤销 v106） |
| v107 | UI（手机端）：状态条下移到地形区之后、手牌之前 |
| v106 | UI（手机端）：地形效果说明移到列底（后由 v108 撤销） |
| v105 | UI（手机端）：≤900px 隐藏顶栏「双倍下注」按钮（AI 仍可加倍） |
| v104 | UI（手机端）：场地三列等分占满整宽，去掉横向滚动，三列等高 |
| v103 | UI：手牌尺寸统一——无图卡新增 `.no-img`，效果文字区预留两行 |
| v102 | UI：没有衍生卡的卡牌详情弹窗收窄 |
| v100 – v101 | 演出：暗牌翻开改为 CSS 缩放入场（`flipIn` 0.5s）；修复仅效果确有变化时才在结算后重建 |
| v99 | 演出微调：地形揭晓换场淡出 700ms → 500ms |
| v98 | 演出：地形揭晓换场——未揭示外观整列克隆并 700ms 淡出 |
| v97 | UI：图鉴与「指定卡牌」筛选条新增「衍生卡牌」档（token；隙间不列入） |
| v96 | 演出：换边效果（`switch` / `gift`）也使用滑行+缩放克隆飞行 |
| v95 | 抽牌入场回退为原生 CSS 滑入动画（`.hand-drawn` 0.65s） |
| v94 | 抽牌节奏：3 张开场逐张滑入（每张约 0.72s），间隔 1s 进第 1 回合；第 1 回合也各抓 1 张（起手 4 张） |
| v93 | 修复：抽牌滑入改为 body 固定克隆演出，不用 transform 平移手牌 |
| v92 | UI 重构：图鉴与「指定卡牌」拆到 `card-browser.js`；两页新增费用筛选条（0-1 / 2 / 3 / 4 / 5 / 6 费） |
| v91 | 演出：每回合开始抽到的卡从屏幕最右侧滑入 |
| v90 | 演出：落场生成卡凝聚显形、加入手牌底部滑入（1s） |
| v89 | 演出：射命丸文 `fly` 移动改用与 `shift` 同款的滑行+缩放飞行克隆 |
| v88 | 演出：被摧毁的卡新增分崩离析动画（3×2 碎片 + 渐隐 1s） |
| v87 | 演出微调：终局胜区横幅区域间隔 300ms → 150ms |
| v86 | 演出微调：终局胜区横幅动画 1s → 700ms、区域间隔 500ms → 300ms |
| v85 | 演出：终局新增胜区点数放大演出（左→右高亮后才弹结算窗） |
| v84 | 演出扩展：新增红色「-N」版本（永久降战力） |
| v83 | 演出修复：+N 气泡改为「冲大 → 停留 → 末段渐隐」 |
| v82 | 演出微调：+N 动画 700ms → 1s |
| v81 | 演出微调：+N 动画 300ms → 700ms |
| v80 | 演出：永久 +N 战力播放约 300ms 绿色小动画，覆盖 `bf`/`ba`/`bl`/`oc`/`phx` |
| v79 | 演出：八云紫 `shift` 逐张搬运改为滑行+缩放飞行演出 |
| v78 | 演出：八云紫 `shift` 改为逐张搬运动画（每张间隔 0.3s） |
| v77 | 新增人物「蕾蒂」（🌨️ 2费/3战力）：卡级免摧毁 `prot: true`——在场期间该区（双方）卡牌都无法被摧毁 |
| v76 | 重做「八云紫」（→ 6费/10战力）：新增效果键 `shift`（左区已翻开卡按序右移到最右区至填满）；不再生成「废弃列车」 |
| v75 | 视觉：未揭示地形列改为随机未知主题（淡紫灰底 + 虚线边框 + 图标漂浮） |
| v74 | 地形揭晓系统：开局按 `pick` 权重抽定 3 块地形存 `state.locPlan`，第 1/2/3 回合开始依次揭晓左/中/右（含「出现时」效果）；揭晓成 `max<4` 超限不摧毁不移走 |
| v73 | 新增「未揭示」地形（❓ `unreveal`）：放入 `data/locations.js` 的 `EXTRA` 表，不进随机池，只作按 id 引用 |
| v72 | `morph` 补充大体积限制：目标为大体积卡（如 `occ4`）时，仅当本区 `max` 等于其占格数且己方只剩二岩才可变身 |
| v71 | 占格口径修复：`mv` 移走目标卡时目标侧空余需 ≥ 该卡占格数；换边落对方侧同样按占格判定 |
| v70 | 新增人物「伊吹萃香」（🍶 5费/12战力）：卡级大体积占格 `occ: 4`——占满 4 格、只能放入 max=4 且己方该区全空 |
| v69 | 新增人物「稗田阿求」（📜 0费/6战力）：卡级 `leave`——游戏结束时从场上消失（非摧毁） |
| v68 | UI 修复：手牌卡面与详情弹窗显示当前战力（基础 + 永久增益） |
| v67 | 重做「藤原妹红」（→ 1费/1战力）：卡级 `phx: 2` 凤凰重生——被摧毁时返回手牌并永久 +2 战力（手牌满 7 则失败） |
| v66 | 新增人物「因幡帝」（🐇 2费/3战力）与效果键 `gift`：本区己方战力最低的已翻开卡换边到对方该区 |
| v65 | UI：侧栏对手信息改为显示对方当前手牌剩余张数 |
| v64 | 新增人物「二岩猯藏」（🦝 3费/0战力）与效果键 `morph`：随机复制对方手牌一张卡并重新触发其效果 |
| v63 | 新增人物「依神紫苑」（📉 5费/−7战力）与效果键 `switch`：翻开后转到对方该区一侧（−7 计入对方） |
| v62 | UI 修复：卡牌详情弹窗桌面加宽至 `min(880px, 96vw)`；衍生小卡尺寸自适应 |
| v61 | 新增人物「赫卡提亚」（💠 6费/5战力）与效果键 `clone`、特殊卡「赫卡提亚的分身」：另两区自己一侧各放 1 张，战力按揭示时快照固定 |
| v60 | 新增人物「灵乌路空」（☀️ 3费/6战力）：揭示 `xform` 把本区变为「聚变反应炉」；卡级防摧毁 `surv: 2`（改为永久 −2 战力） |
| v59 | 术语统一：「在场光环」→「持续效果」（简称「持续」）——翻开后持续存在、源卡离场即失效 |
| v58 | 术语统一：「现身」→「揭示」（On-Reveal）——翻开时立刻执行的一次性效果 |
| v57 | 新增人物「键山雏」（🎎 3费/1战力）与效果键 `spawnO`、特殊卡「厄运」（🌀 3费/−3，落对方一侧、计入对方） |
| v56 | 新增战力影响历史：卡实例维护 `powerLog`，面板分列基础/永久 buff/持续/区域加成（+N 绿、−N 红） |
| v55 | 新增放置顺序队列 `state.fieldQueue` 与时机效果字段 `fx: { turnStart?, turnEnd?, gameEnd? }`，按队列先后结算 |
| v54 | 引擎流程重构（行为不变）：主循环拆为显式阶段管线 |
| v53 | 区域抽选改为按 `pick` 权重不放回抽取（默认 1）；辉针城 `pick: 0.28` → 出现率约 10% |
| v52 | 节奏：翻面→效果结算停顿 400ms（仅有效果时），效果→下一张 500ms |
| v51 | 节奏：翻面后先停顿展示再结算「现身」，翻牌节奏统一 500ms |
| v50 | 数值：鬼人正邪 6费 15 → 10 战力；AI 变形辉针城卡仅在落后 ≥10 点时打出，聚变反应炉落子估值 ×0.25 |
| v49 | UI：龙神像改为很浅的灰色主题 |
| v48 | UI：七夕坂改为淡粉纯色主题 |
| v47 | UI：虹龙洞改为淡黄纯色主题 |
| v46 | UI：迷途竹林改为中绿、红魔馆改为淡红纯色主题 |
| v45 | UI（纯色主题修正）：冥界=浅灰底、雾之湖=浅蓝底（含深色边框/表头/格线） |
| v44 | UI：冥界改纯淡灰底、雾之湖改纯淡蓝底 |
| v43 | UI：辉针城背景改为上下各半（上黑下白）的反转意象 |
| v42 | UI：聚变反应炉改为暗红色主题 |
| v41 | 能量重置扩展：本回合 `fly` 移动过的牌移回回合初区域并恢复移动次数 |
| v40 | 重做「射命丸文」（→ 5费/7战力）：卡级 `fly: true` 每回合移动一次（不耗能量；AI 暂不使用） |
| v39 | 重做「魂魄妖梦」（→ 2费/1战力）：效果键 `oc`——翻开当回合对方曾在本区落牌则永久 +4 |
| v38 | 重做「芙兰朵露」（→ 4费/3战力 摧毁）：揭示 `dw` 摧毁同区对方已翻开最低战力卡 |
| v37 | 数值对齐：风见幽香 10→12、射命丸文 7→8 |
| v36 | UI：未开放区域整列加灰色遮罩 + 🔒；卡牌详情弹窗新增「衍生卡牌」区 |
| v35 | 重做「八云紫」（→ 5费/6战力）：揭示 `give` 加入 1 张「废弃列车」（🚂 3费/3战力，`dwh` 摧毁本区敌方最强已翻开卡） |
| v34 | 新增人物「红美铃」（🥋 2费/3战力，scarlet）与效果键 `mv`：本区对方最低战力已翻开卡随机移到另两区 |
| v33 | 开发者调试：顶部新增「⚡ 修改能量」（本回合能量设为 7） |
| v32 | 新增人物「鬼人正邪」（🪞 6费/15战力）与效果键 `xform`：本区变形为「辉针城」（低者胜） |
| v31 | 开发者调试：顶部新增「🎯 指定卡牌」（点选人物卡加入手牌，满 7 失败） |
| v30 | UI：手牌效果文字超两行省略；新增右键手牌弹出完整卡牌详情 |
| v29 | 重做「比那名居天子」（→ 3费/2战力）：揭示 `spawn` 给本区双方各 1 张「石块」；卡级在场光环 `og` / `tk`（己方石块 +2） |
| v28 | 白板人物更名与数值对齐：1费 露米娅→小妖精(2)、2费 红美铃→小妖怪(3)、3费 灵梦→中妖精(4)、4费 蕾米莉亚→中妖怪(6)、5费 早苗→大妖怪(8)、6费 萃香→贤者(12) |
| v27 | 现身增减口径收紧：`bf`/`de`/`ba` 只作用于结算时已翻开的卡 |
| v26 | 「隙间」登记为特殊卡 `SPECIAL.gap`（`un: true`）：0 费 0 威、不可移动/摧毁/被修改；新增 `data/现有机制.md` |
| v25 | 摧毁机制收紧：`dw` 只能指定同区已翻开的对方卡 |
| v24 | 新增地形「聚变反应炉」（🔥 `reactor`，`purge: true`）：每回合翻牌后摧毁本区最低战力卡（并列全删） |
| v23 | 新增地形「冥界」（🪦 `underworld`，`all: -2`）与全区修正 `all`（计入卡面/总数/摧毁/落后判定） |
| v22 | 新增地形「七夕坂」（🌠 `tanabata`，`minTurn: 5`）：第 5 回合起才可放牌 |
| v21 | 新增地形「辉针城」（🏯 `needle`，`inv: true`）：本区总战力更低者获胜；胜负/领先/翻牌顺序走 `zoneEff`（`dbl` 后取负） |
| v20 | 新增地形「雾之湖」（🌅 `lake`）与费用加成 `cb`：本区 1 费卡牌威力 +2（含 token） |
| v19 | 新增地形「龙神像」（🐉 `dragon`）与放满加成 `fill`：某方在本区放满 4 张即总战力 +5 |
| v18 | 结算/认输弹窗改为左「确认」右「再来一局」；「确认」只关弹窗，保留终局盘面 |
| v17 | 「红魔馆」地形重做：改为阵营加成（该区 scarlet 角色 +2）；新增卡牌 `g` 字段、区域 `aff` 字段与 `GROUPS` 阵营表 |
| v16 | 新增地形「虹龙洞」（🕳️）：出现时双方各生成 1 张「石块」；引入特殊卡（`SPECIAL` token）体系（不入牌库/图鉴、落地即翻开、可设卡面色 `cg`）；新增 `data/现有*.md` |
| v15 | UI：区域中间带效果文字放大；地形「博丽神社」更名「无名之丘」（图标 ⛩️→⛰️） |
| v14 | 区域抽选改为不重复：每局三块地形互不相同（池不足 3 时退回兜底） |
| v13 | 所有区域（无论 `max` 为几）敌我两侧均绘制浅灰 2×2 网格线 |
| v12 | 场地格位细化：`max=4` 区画 2×2 网格线；`max<4` 区不可用格位用灰色「隙间」占位卡填充 |
| v11 | 数据分层：卡牌拆到 `data/cards.js`、场地拆到 `data/locations.js`；支持 `img` 图片素材（缺图回退 emoji） |
| v10 | UI 主题：整体改为「幻想乡自然风景」清新淡绿浅色系 |
| v9 | 区域总点数横幅动态着色：领先方亮黄，另一方与平点蓝色 |
| v8 | 主题换皮为「东方逆转」：全部 24 张卡与 3 种区域改用东方角色/地点命名；翻牌先后改为首回合随机、之后领先方先翻、持平随机 |
| v7 | 修复：同回合多张牌翻牌顺序始终等于放置顺序；暗牌不计入总点数 |
| v6 | 胜负规则改为「有平局区域按三区总点数决胜 / 无平局按区域多数」；区域效果移到中间带；缩略卡改为左上费用 + 右上白/绿/红战力 |
| v5 | 场地展示：卡位固定 2×2；场上已翻牌可点击放大；右上角战力改为含区域倍率的最终口径 |
| v4 | UI：认输移左下、跳过/结束回合移右下；能量框可点击重置本回合暗牌（回手 + 返还能量） |
| v3 | 移除区域「圣地之巅」（权重 ×2 太影响平衡）；新增开局起手保护洗牌；本文档首次建立 |
| v2 | 一回合可出多张牌（能量预算制），新增「结束回合」按钮 |
| v1 | 初版：一回合一张牌模式 |
