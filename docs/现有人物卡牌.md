# 现有人物卡牌

> 与 `data/cards.js` 的 `POOL` 保持同步。人物卡按费用放入牌库抽选池（**1~6 费**会被玩家兜底牌库 /
> 对手曲线的随机抽卡抽到；**0 费**暂不入洗牌、**7 费**只存在于 `POOL[7]`、**8 费**只存在于 `POOL[8]`
> ——因两条随机曲线都不含 7/8 费，这两档**只能通过「卡组设置」自建卡组带入**，见 v185 / v194 说明），
> 会出现在图鉴中。
> **v170**：角色池里的**法术卡**（`spell: true`）也收录于本表——法术**只具有能量花费与「揭示」效果**，
> 表中「威力」栏记 **0**（卡框不显示战力，标「法术 · 无战力」）；法术可入卡组、会被随机曲线与 AI 抽到，
> 暗牌占 1 格、揭示瞬间仍占格，揭示效果结算完后**自行消散**（口径见 `docs/现有机制.md` §1「法术」段）。
> 共 **52** 张（**v198 新增 2 费「姬虫百百世」（2 费 / 2 战力）：随机弃自己 1 张手牌 + 按该卡印刷费用加入 1 张「石块」到手牌**；此前 v197「稗田阿求」重做：0 费 / 6 战力 + 终局离场 → 1 费 / 4 战力 + 手牌回合结束自动弃牌；v196 新增 6 费「大鲶鱼」、v195 新增 3 费法术「妖精大战争」、v194 新增 8 费「纯狐」、v192 新增 6 费「四季映姬」、v191 新增 3 费「小野塚小町」、v190 新增 1 费「莉莉黑」）。新增/修改人物卡后请同步本表（效果类型：白板 / bf 同区友军增益 / de 同区敌方削弱 / ba 双方同增 / bl 落后自增（**v179 起也作时机效果复用**：蕾米莉亚·斯卡蕾特 → `fx.turnEnd` 每回合末判一次） / dw 摧毁敌方最弱 / **dwb 摧毁本区双方最弱随机一张（v172）** / **dwc 按印刷费用全场摧毁（v195，妖精大战争：摧毁双方场上所有 1 费已翻开牌，含 1 费 token、不含暗牌与法术，逐区受免摧毁保护）** / spawn 添加石块 / spawnO 本区敌方侧生成特殊卡 / **spawnS 本区自己一侧生成特殊卡（v172；v179 起另有 `fill: true` 写法：填满该侧、消散后正好铺满）** / **spawnMine 己方每个区域（含本区）各生成特殊卡（v180，耀眼之龙玉）** / **deAll 敌方全场（三区）已翻开卡牌各 −N（v180，蓬莱的玉枝；非随机分配）** / **tkBuff 标记卡增幅（v179：己方/全场同 `tk` 标记卡永久 ±N，可先落场生成）** / clone 另两区自己一侧生成分身 / give 加入手牌衍生物（**v177 起由雾雨魔理沙使用** → 法术「极限火花」；**v180 起支持 `pool` 写法** —— 蓬莱山辉夜从 5 张神宝里随机抽 2 张，见 `docs/现有特殊卡牌.md`） / xform 区域变形 / switch 换边到对方一侧 / gift 本区己方最低已翻开卡换边到对方 / morph 变身成对方手牌随机卡复制体 / mv 移走 / dwh 摧毁敌方最强（v177 起**并列最高时随机挑一张**） / oc 对方同区落牌自增 / shift 己方左端已翻开卡整体右移（填满为止）/ costUp 对方手牌随机一张能量消耗 +1（v169，本场战斗有效）/ energyNext 自己下回合额外获得能量（v169）/ gather 集结（v171：三区自己一侧各生成 1 张阵营成员并结算其揭示，随后己方场上已翻开的同阵营 +N 战力）/ **shuffleIn 洗入卡组（v184：把指定牌 n 张洗入自己或对手的牌库并重洗；现由重做后的琪露诺使用 → 向对手牌库洗入 1 张「冰块」）** / **discard 弃牌（**v190**：把牌从**手牌**移出 → 进该牌**归属方**的**弃牌池**；与「摧毁」分工不同——**摧毁只管场上、弃牌只管手牌**，故不触发任何摧毁类机制；写法 `discard: { n, to, pick, card, cost }`，`to` 缺省弃自己、`'opp'` 弃对方，`pick` = `'random'`（缺省）/`'right'`（最右侧）/`'left'`（最左侧）/**`'maxCost'`（**v191** 新增：取印刷费用 `def.c` 最高的一张，并列最高时随机）**，`card`/`cost` 按卡名与**印刷费用**筛选；现由 v190 新卡**「莉莉黑」（1 费 / 3 战力）**使用 → 丢弃自己最右侧的 1 张手牌；**v191 新卡「小野塚小町」（3 费 / 5 战力）** 使用 → 丢弃自己手牌中印刷费用最高的一张；**v198 起另有**可选子句 **`give`**：弃牌**真的发生之后**按**被弃牌的印刷费用 `def.c`** 为**施放方自己**加入手牌衍生物（`powerFromCost: true`；弃牌落空则不结算；进手牌、手牌上限 7 张照常约束、仍需手动暗出）——现由 **v198 新卡「姬虫百百世」（2 费 / 2 战力）** 使用 → 随机弃自己 1 张手牌，并加入 1 张战力＝该卡费用的「石块」）** / **reviveDiscard 复活弃牌池（**v192**：把**自己一方**的**弃牌池**里**所有「角色卡牌」（＝非法术的牌）**以**随机顺序**逐张复活到**随机区域**——每张牌只在“已开放且该侧放得下”的区域里随机（**不会**因掷到满区而复活失败）、落地即翻开并**逐张重新结算其自身揭示**；**法术不参与复活**、留在池里；现由 v192 新卡**「四季映姬」（6 费 / 6 战力）**使用 → 若可能，将你所有被丢弃的角色卡牌，以随机顺序复活到随机区域中）**；另有卡级机制：**持续 og（v55 按 `tk` 标记匹配 token；**v179 起支持按印刷费用 `cost` 匹配**，克劳恩皮丝 → 己方场上该费用的卡牌）**、**法术 spell（v170）**、防摧毁 surv、**自身不可摧毁 ind（v180，佛体金刚石：判定式免疫——摧毁判定照常把它算进去，落在它身上则摧毁失败、判定结束、不改打其他牌；只保护它自己）**、**游戏开始时 gs（**v185**，哆来咪：只要开局时它在你的卡组/牌库里就触发、整局永久——从普通卡牌池
> 不放回随机洗入 10 张牌 + 本局每回合最大能量 +1）**、凤凰重生 phx、**终局离场 leave（**v197 起无使用者**：原唯一使用者稗田阿求已重做，机制保留为预留）**、大体积占格 occ、区域持续免摧毁 prot、**费用随摧毁递减 costDown（**v194**，纯狐：本场战斗中双方每有一张牌被摧毁，此牌能量消耗 −1，下限 0 费）**、**放置条件 playReq（**v196**，大鲶鱼：`playReq: { tk:'rock', n:4 }` —— **只在“从手牌打出”那一刻检查**，己方场上已翻开的「石块」< 4 张就打不出来；**打出后不再追踪**，石块之后被摧毁/移走也不移除它；复活/变身/落场生成等其它上场路径不受限）**，见效果文本；**时机效果 fx（v55，v172 起含帕秋莉的回合开始抽法术；v180 起含无限生命泉的回合结束 `bf +2`；**v197 起含「手牌回合结束」`fx.handEnd`**——只结算“回合结束时仍在手牌里”的牌，现由重做后的稗田阿求使用）**）。
> ⚠️ **v197 起 0 费组为空**（原稗田阿求已重做为 1 费 / 4 战力并搬到 1 费组）：`POOL[0]` 空桶保留，
> 牌库曲线（1~6 费）本就与之无关；图鉴 /「指定卡牌」/ 卡组页的费用筛选为「0-1 费」合档，不受影响。
> 阵营：卡牌 `g` 字段标注所属（`scarlet` 红魔馆、`light` 光之三妖精，v169）；红魔馆区域会为 scarlet 角色常驻 +2。现 scarlet 成员：红美铃 / 十六夜咲夜 / 帕秋莉 / 芙兰朵露 / **蕾米莉亚·斯卡蕾特（v179 新增）**——⚠️ 沿革说明：早期版本曾把「蕾米莉亚」这个**卡位**改名为「中妖怪」并脱离阵营，那是**另一张卡**（泛用白板）的沿革，与本次的人设无关；v179 起以**全新条目**把「蕾米莉亚·斯卡蕾特」作为 scarlet 成员实装；**小恶魔（红魔馆）未实装**，占位待后续加入。**光之三妖精（`light`）三人已齐（v169）：桑尼米尔克（新增）、露娜·切露德（由「莉格露」改名，数值效果不变）、斯塔萨菲雅（新增）**，暂无地形为其提供 `aff` 加成。

## 0 费

| 图标 | 名称 | 威力 | 效果类型 | 效果文本 | 所属 |
| --- | --- | --- | --- | --- | --- |
| — | *（本档 v197 起为空）* | — | — | 原「稗田阿求」（0 费 / 6 战力 · 终局离场 `leave`）已在 **v197 重做**为 **1 费 / 4 战力 + 手牌回合结束自动弃牌**，搬到下方 **1 费组**；`POOL[0]` 空桶保留（见 `data/cards.js`） | — |

## 1 费

| 图标 | 名称 | 威力 | 效果类型 | 效果文本 | 所属 |
| --- | --- | --- | --- | --- | --- |
| 🌟 | 小妖精 | 2 | 白板 | 无 | — |
| 📜 | 稗田阿求 | 4 | **手牌回合结束（`fx.handEnd`，v197 重做）** | 回合结束：若此牌依然在手牌中，则自动丢弃（移入你的弃牌池） | — |
| ❄️ | 琪露诺 | 2 | `shuffleIn` 洗入卡组（**v184 重做**） | 揭示：向对手的牌库洗入 1 张「冰块」（1 费 / 0 战力白板；对手整副牌库将重新洗牌） | — |
| 🌙 | 露娜·切露德 | 1 | de −1 | 揭示：同区对方所有已翻开卡牌 −1 | 光之三妖精 |
| 🧚 | 大妖精 | 1 | ba +1 | 揭示：同区双方所有已翻开卡牌 +1 | — |
| ☀️ | 桑尼米尔克 | 2 | costUp +1 | 揭示：让对方当前手牌中随机一张卡的能量消耗 +1（最多涨到 6 费，对方手上全是 6 费则落空；只影响被选中的那一份卡、仅本场战斗有效，费用数字变红） | 光之三妖精 |
| ⭐ | 斯塔萨菲雅 | 2 | energyNext +1 | 揭示：使自己在下个回合额外获得 1 点能量（一次性，只在该回合有效；同回合打出多张可叠加，如两张=+2；第 6 回合翻开的这份用不上） | 光之三妖精 |
| 🌸 | 莉莉霍瓦特 | 1 | bf +1（**v183 新增**） | 揭示：同区你的其他已翻开卡牌 +1（v183 时与琪露诺同款；**v184 琪露诺已重做，本卡保留这一旧款**） | — |
| 🖤 | 莉莉黑 | 3 | `discard` 弃牌（**v190 新增**） | 揭示：丢弃你**最右侧**的 1 张手牌（移入**你的**弃牌池；弃牌只作用于手牌、不是“摧毁”；手牌为空则无事发生） | — |

> 🖤 **v190：新增人物「莉莉黑」（1 费 / 3 战力，无阵营）**——她是 **v189 新增的「弃牌」机制
> （效果键 `discard`）的第一个实际使用者**。按规则 4 落在 **1 费组**（本组 7 → **8** 张；
> 全池合计 45 → **46** 张）：**可入卡组**、会被玩家兜底牌库与 AI 费用曲线抽到，
> 也是「妖精神社」随机 1 费池的候选之一。口径：
> ① **写法** `k: 'discard'` + `discard: { to: 'own', pick: 'right', n: 1 }` —— 揭示：**丢弃自己
>   最右侧的 1 张手牌**（`to:'own'`＝弃**自己**手牌，不是对手的；`n` 缺省 1 故只弃 1 张）；
> ② **`pick`（v190 随本卡新增的「取牌位置」口径）**：`'right'`＝取手牌里**最右侧**那张
>   （手牌左 → 右即数组顺序，最右＝队尾那张）；缺省 `'random'`＝候选里随机；另有 `'left'`（最左侧）；
>   `n: 'all'` 时位置无意义。`pick` 与既有的 `card`/`cost` 筛选**可叠加**（先筛候选、再按位置取）；
> ③ **代价型、不是“摧毁”**：被弃的牌从手牌移出、进**自己**的**弃牌池**（`discardPile`，
>   侧栏「己方特殊牌池」→「弃牌池」页签可查）；不触发 `surv`/`phx`/`prot`/`ind`、不改战力、
>   不动区域字段与格位——**弃牌只作用于手牌、摧毁只作用于场上**（用户口径，完整口径见
>   `docs/现有机制.md` §1「弃牌（`discard`）」段）；
> ④ **时序**：揭示发生在阶段 ④（暗出之后），此刻这张莉莉黑**已经不在手牌里**，故“最右侧”按
>   **揭示那一刻的剩余手牌**判定；手牌为空（含“她是手上最后一张”的情况）→ 无牌可弃、只记一条日志；
> ⑤ **公开**：弃牌**双方可见**——日志点名 + 场地中央弹出**被弃那张牌的完整卡面**并播
>   **从右上到左下斜切两半**的演出（约 1.6s：弹入后停留约 0.4s 才切、切完 0.62s 内快速消散，v190 调过快慢）；
> ⑥ **无阵营 `g`**（与莉莉霍瓦特同款口径）：报春妖精不属于「光之三妖精」（`light`），
>   因此**不会被法术「三妖精集结」的 `gather` 抽到**，也不吃任何 `aff` 阵营加成。
> **配图**：自 `assets/source-art/240px-莉莉黑（Q版立绘）.png` 复制为 **`assets/cards/lilyblack.png`**
> （**240×240**，规则 1）；卡面 emoji 🖤 仅作缺图兜底。素材状态见 `docs/card-image-map.md`
> 与 `README.md` 的「已知待办（素材侧）」。

> 🌸 **v183：新增人物「莉莉霍瓦特」（1 费 / 1 战力，无阵营）**——当时**效果与「琪露诺」一模一样**
> （揭示 `bf +1`：同区你的其他已翻开卡牌 +1；`p:1 / c:1 / k:'bf' / a:1` 与当时的琪露诺逐字段相同，
> 是当时唯一的“同效果双卡”，口径按用户要求原样复制）。
> ⚠️ **v184 起该说法失效**：**琪露诺已重做为 1 费 / 2 战力的 `shuffleIn`**（向对手牌库洗入 1 张「冰块」），
> 因此现在**只有莉莉霍瓦特**保留「1 费 / 1 战力 / `bf +1`」这一款式（她自身**未被改动**，数值与效果照旧）。
> 按规则 4 落在 **1 费组**
> （本组现 **7** 张；全池合计 43 → **44** 张）：**可入卡组**、会被玩家兜底牌库与 AI 费用曲线抽到，
> 也是「妖精神社」随机 1 费池的候选之一。
> 配图自 `assets/source-art/240px-莉莉霍瓦特（Q版立绘）.png` 复制为 **`assets/cards/lilywhite.png`**
> （240×240，规则 1）；卡面 emoji 🌸 作缺图兜底。⚠️ **刻意无阵营 `g`**——莉莉霍瓦特是报春妖精，
> **不属于「光之三妖精」（`light`）**，因此**不会被法术「三妖精集结」的 `gather` 抽到**，
> 也不吃红魔馆等 `aff` 阵营加成。

> ❄️ **v184：琪露诺重做**（**1 费 / 2 战力**；原为 1 费 / 1 战力、揭示 `bf +1`：同区你的其他已翻开卡牌 +1）——
> 揭示改为 **`shuffleIn`（洗入卡组）**：**向对手的牌库洗入 1 张「冰块」**（token，1 费 / 0 战力白板，
> 见 `docs/现有特殊卡牌.md`），并把对手**整副牌库重新随机洗一次**。费用未变（仍 1 费）故**留在本组**；
> 配图仍 `cirno.png`、emoji 仍 ❄️、仍无阵营（无 `g`，不吃 `aff`，也不进 `gather` 池）。
> 口径：
> ① **写法** `k:'shuffleIn'` + `shuffleIn: { card: 'ice', n: 1, to: 'opp' }` —— `to:'opp'` 指**对方**牌库，
> 归属按牌的**所属方**结算（你打出塞给 AI；AI 抽到琪露诺打出则塞给**你**，对双方一视同仁）；
> ② **不结算被洗入卡的效果**：冰块进的是**隐藏区牌库**（不翻开、不占格位、不进 `fieldQueue`），
> 只有等对手把它**抽到手、再暗出翻开**时才按普通 1 费 / 0 战力白板卡走常规流程；
> ③ **作用**＝**稀释对手牌库 + 占用其手牌位**：对手抽到一张 0 战力白板，手牌满 7 张后还会
> 因「手牌满不抽牌」卡住其后续抽牌；
> ④ **张数无上限**、牌库没有 7 张上限（「手牌满则失败」不适用）；重洗范围＝对手**整副牌库**；
> ⑤ **公开**：日志点名 + 轻量演出（对手侧栏「牌库 N 张」青蓝闪光 + 「🃏 洗入卡组 ×1」气泡）；
> ⑥ **引擎侧零新增**：本键是 v184 同版新增的 `shuffleIn` 机制的首个（也是当前唯一）使用者，
> 完整口径见 `docs/现有机制.md` §1「洗入卡组（`shuffleIn`）」段；冰块清单见 `docs/现有特殊卡牌.md`。
> 本组张数不变（仍 **7** 张；全池合计仍 **44** 张），**但卡组存档按卡名序列化**——
> 旧卡组里这张牌的名字没变（仍叫「琪露诺」），**能正常读回**，只是数值与效果换成新的。

> 📜 **v197：稗田阿求重做 —— 0 费 / 6 战力（`leave` 终局离场）→ 1 费 / 4 战力 + 手牌回合结束自动弃牌**
> 卡面效果＝**回合结束：若此牌依然在手牌中，则自动丢弃**（`k: ''`：**没有揭示效果**，效果全部
> 体现在**手牌时机**上）。按规则 4 落在 **1 费组**（本组 8 → **9** 张；**0 费档 1 → 0 张**、
> 全池合计仍 **51** 张）：**可入卡组**、会被玩家兜底牌库（`DECK_CURVE`）与 AI 费用曲线
> （`AI_DECK_CURVE`）抽到（两条曲线都含 1 费），也因此进入「妖精神社」的随机 1 费池与
> 哆来咪 `gs.shuffleN` 的洗入池（**v197 起它不再被“0 费组不进洗牌”排除**）。口径（**用户确认**）：
> ① **写法**＝新的**时机键 `fx.handEnd`**（手牌回合结束效果）：
>   `fx: { handEnd: { k: 'discard', discard: { self: true }, t: '…' } }` —— 走**既有弃牌收口**
>   `discardFromHand`（v197 给它加了 `onlyCard` 参数：只弃**触发的那一张实例**，
>   故手里有两张同名卡时**不会互相误伤**）；
> ② **两个子句都要成立**：“回合结束”**且**“此牌**仍在手牌中**”——打到场上之后（或已被其它效果
>   弃掉、换边、洗走等任何“不在手牌”的情况）**不再触发**，只记一条 `sys` 日志；
> ③ **双方一视同仁**：玩家手牌与对手手牌都逐张检查（**对手手里的阿求同样会在回合末被丢弃**）；
> ④ **每个回合末都结算一次，含第 6 回合末**（阶段 ⑥ 在 ⑦ 终局结算之前；**手牌本来不计入终局
>   总点数**，故它只影响该方「弃牌池」的记录与演出）；同一方手里有 2 张就**各弃 1 张**（各记一条日志）；
> ⑤ **弃的是「弃牌池」**（不是放逐池、不是移出游戏），并播**完整的弃牌演出**（中央弹出该牌完整卡面
>   + 从右上到左下斜切两半，约 1.6s，**双方可见**——与莉莉黑 / 小野塚小町的弃牌同款）；
> ⑥ **不是“摧毁”**：不触发 `surv`/`phx`/`prot`/`ind`、不改战力、不动区域字段与格位
>   （`sideUsed`/`sideRoom`/`fill`）、不进 `powerLog`、不进 `fieldQueue`（手牌本就不在场上）；
> ⑦ **旧的 `leave`（终局离场）随本次重做一并去掉**——`leave` 机制本身**保留为预留**、
>   `runGameEndEffects` 照常处理，但**当前无任何卡使用**（见 `docs/现有机制.md` §1 两条）。
> **引擎落地**：`js/game.js` 把 v54 起预留的空挂点 **`runHandEndEffects()`（阶段 ⑥）实装**，
> 并给 `discardFromHand` 增加 `onlyCard` 入参；`kindTags` 增加「手牌回合结束 · 仅当此牌仍在手牌中时
> 触发（打到场上后不再生效）」标签（避免把这张无揭示的牌误标成白板）。**配图不变**：
> 仍为 `assets/cards/akyuu.png`（240×240），emoji 📜 仅作缺图兜底
> —— 完整口径见 `docs/现有机制.md` §1「手牌回合结束（`fx.handEnd`）」段与「弃牌（`discard`）」段。
> ⚠️ **联动提示**：① `POOL[0]` 自此为空桶（引擎、图鉴/卡组页的「0-1 费」合档与费用档遍历都不受影响）；
> ② `tests/smoke-test.js` 原把「稗田阿求」当作 **0 费边界卡**随机塞进测试牌库（`ZERO_COST_CHANCE`），
> 重做后它是一张普通 1 费卡，该脚本的口径需另行评估（见下条，**由用户决定是否改测试**）；
> ③ `js/ai.js` 里针对 `leave` 的估值分支（终局不计其战力）成为**死代码**（不影响运行，
> 本次未改动 AI 代码）。

## 2 费

> 🔧 **v172：帕秋莉·诺蕾姬已从本组搬出**——重做为 **3 费 / 2 战力** 的「法术调度」角色（效果由 bf +2 改为回合开始抽法术），
> 按「卡片放在它费用的那一档」的规则移入 **3 费组**（本组现 **8** 张）。

| 图标 | 名称 | 威力 | 效果类型 | 效果文本 | 所属 |
| --- | --- | --- | --- | --- | --- |
| 🥋 | 红美铃 | 3 | mv | 揭示：将此区域对方战力最低的已翻开卡牌移到另外两个区域随机一处（满员区排除；都满则失败） | 红魔馆 |
| 👺 | 小妖怪 | 3 | 白板 | 无 | — |
| ⏱️ | 十六夜咲夜 | 2 | bl +2 | 揭示：若你在此区落后，此卡 +2 | 红魔馆 |
| 🎵 | 米斯蒂娅 | 1 | de −2 | 揭示：同区对方所有已翻开卡牌 −2 | — |
| 🗡️ | 魂魄妖梦 | 1 | oc +4 | 揭示：若翻开当回合，对方曾在本区域放置至少一张牌，则此牌战力 +4 | — |
| 🐇 | 因幡帝 | 3 | gift | 揭示：将此区域己方战力最低的已翻开卡牌换边到对方一侧（不含自己；多张并列最低则随机选一张） | — |
| 🌨️ | 蕾蒂 | 3 | 持续免摧毁（prot） | 持续：此区域双方在场的卡牌均无法被摧毁 | — |
| 🔥 | 藤原妹红 | 3 | 凤凰重生（phx） | 每次此卡被摧毁时：返回手牌，并且战力 +2（可多次触发；手牌满则重生失败） | — |
| ⛰️ | 镇守大地之石 | 0（**法术 · 无战力**） | spawnS + `fill`（**法术** · **v179 新增**） | 揭示：用「石块」（1 费 / 0 战力）填满此牌所在的己方区域 | — |
| 🐛 | 姬虫百百世 | 2 | `discard` 弃牌 + **`give` 子句（v198 新增）** | 揭示：随机丢弃你手牌中的 1 张卡，并将 1 张战力等于该卡能量消耗（印刷费用）的「石块」加入你的手牌 | — |

> ✨ **v179：角色池法术「镇守大地之石」加入本组**（**2 费 / 法术 · 无战力**）——**可入卡组**、
> 会被玩家兜底牌库与 AI 费用曲线抽到（口径同 v171「三妖精集结」，只是落在 2 费档）。口径：
> ① 揭示走既有键 `spawnS` 的**新写法 `fill: true`**：**张数不写死**，按结算那刻**自己一侧的空余格数**铺满；
> ② **消散后正好铺满**——施放的那张法术**先消散**（把自身占的 1 格也让给石块），再按空位铺满，
> 因此最终该侧**正好铺满**（无名之丘 = 4 张石块；迷途竹林 = 2 张；该侧已满/超限 → 0 张，无事发生）；
> 这是对 v170「法术揭示瞬间仍占 1 格」的**明确例外**（见 `docs/现有机制.md` §1「法术」段「占位」条）；
> ③ 石块照吃「雾之湖」1 费 +2、照吃天子「己方石块 +2」，也能被增益/摧毁/移动正常影响；铺满会触发地形
> `fill`（即「龙神像」放满 +5）并堵住对方 `switch`/`gift`/`mv` 的落位；
> ④ **无专属配图**：按规则 1 第 5 条走卡面 emoji **⛰️** 兜底，**不拿别的立绘顶替**。
> 本组现 **9** 张；全池合计 **39 → 40** 张。

> 🐛 **v198：人物「姬虫百百世」加入本组**（**2 费 / 2 战力**，无阵营）——**弃牌机制（`discard`）的第三位
> 使用者**，也是**首个使用 `discard.give` 可选子句**的卡。写法 `k: 'discard'` +
> `discard: { to: 'own', pick: 'random', n: 1, give: { card: 'stone', powerFromCost: true } }`，
> 卡面效果＝**揭示：随机丢弃你手牌中的 1 张卡，并将 1 张战力等于该卡能量消耗（印刷费用）的「石块」
> 加入你的手牌**。口径（用户确认）：
> ① **两步、顺序固定**：先弃（走既有收口 `discardFromHand` → 进**你的**弃牌池、播完整弃牌演出，
> 约 1.6s 双方可见），**再**按**那一张被弃掉的牌的印刷费用 `def.c`** 加 1 张「石块」到**你的手牌**；
> ② **“能量”＝印刷费用**（**不是** `cardCost`）：与 `discard.cost` / `dwc` / `og.cost` / 图鉴分档同口径
> ——被桑尼米尔克加过费的牌**不改变**造出的石块战力（如弃掉 5 费牌 → 5 战力石块；弃掉被加过费的
> 3 费牌 → 仍是 3 战力）；战力差额走 `applyPermBuff` 记在这一份实例上，故卡面显示 **0 + 绿色 +N**；
> ③ **弃牌落空则不生成石块**：手牌为空 / 无候选 → 只记一条“想弃但没牌”的日志，本效果整条落空；
> ④ **加的是既有「石块」本体**（同名、1 费 / 0 战力、带 `tk: 'rock'`）⇒ 照常被**大鲶鱼**的
> `playReq: { tk:'rock', n:4 }` 计数、照吃**天子** `og`（己方石块 +2）与**「地精的起床」** `tkBuff`
> （永久 +1）的强化、也会被**「妖精大战争」**（按 1 费印刷费用）扫掉；
> ⑤ **进的是手牌、不是场上**：仍需**手动暗出**（暗出占 1 格、揭示无效果；落地后才是场上的石块）；
> 手牌上限 7 张照常约束——本卡是“先弃 1 张再加 1 张”，故位置一定够；
> ⑥ **时序**：揭示发生在阶段 ④（暗出之后），此刻这张百百世**已经不在手牌里**，故“随机弃 1 张”按
> **揭示那一刻的剩余手牌**判定（不会弃到自己）；
> ⑦ **弃牌不是“摧毁”**：不触发 `surv`/`phx`/`prot`/`ind`、不改战力、不动区域字段与格位
> （完整口径见 `docs/现有机制.md` §1「弃牌（`discard`）」段）；
> ⑧ **配图 `assets/cards/momoyo.png`**（自 `assets/source-art/240px-姬虫百百世（Q版立绘）.png` 复制，
> **240×240**，规则 1），卡面 emoji 🐛 仅作缺图兜底。
> 本组 **9 → 10** 张；全池合计 **51 → 52** 张；**可入卡组**、会被玩家兜底牌库与 AI 费用曲线抽到
> （2 费档），也在「妖精神社」随机 1 费池之外按常规曲线出现。⚠️ **AI 侧本次未改动**（由用户决定）：
> `discard` 键 AI 自 v190/v191 起已认识，`give` 子句只影响“额外收益”，不影响其出牌估值方向。

## 3 费

> 🪄 **v171 新增首张法术卡「三妖精集结」**（3 费 / **法术 · 无战力**）：`spell: true` + 效果键 `gather` ——
> 只带能量花费与揭示效果，揭示效果结算完后**自行消散**（不占格、不属于被摧毁）；口径见 `docs/现有机制.md` §1「法术」段与「集结」段。
> 🖼️ **v182：配图已接入** —— 自 `assets/source-art/三妖精集结.png` 复制为 **`assets/cards/fairies.png`**
> （**448×488**；按规则 1 取图，`data/cards.js` 该条已写 `img: 'fairies.png'`）。卡面 emoji 🌈
> 自此仅作**缺图兜底**（v171~v181 期间该卡一直无图、走 emoji 兜底，本次划掉；
> 素材状态见 `docs/card-image-map.md` 与 `README.md` 的「已知待办」）。
>
> 🔧 **v172：帕秋莉·诺蕾姬搬入本组**（原 2 费 / 2 战力 bf +2 → **3 费 / 2 战力**，`k` 变为白板 + **时机效果 `fx.turnStart`**）：
> 改为「法术调度」——每回合开始时从**帕秋莉法术池**随机抽 1 张法术加入手牌（池内 5 张 1 费法术见 `docs/现有特殊卡牌.md`；
> 口径见 `docs/现有机制.md` §1「帕秋莉法术池」段）。
>
> 🔧 **v177：雾雨魔理沙重做后搬出本组**——原 **3 费 / 3 战力** `k='dw'` → **4 费 / 6 战力** `k='give'`
> （揭示：将一张法术「极限火花」加入手牌），按规则 4 随费用档位移入 **4 费组**。本组现 **8** 张，全池合计仍为 **38** 张。

| 图标 | 名称 | 威力 | 效果类型 | 效果文本 | 所属 |
| --- | --- | --- | --- | --- | --- |
| 💫 | 中妖精 | 4 | 白板 | 无 | — |
| 🎎 | 键山雏 | 1 | spawnO | 揭示：在本区域对方一侧添加 1 张「厄运」（占对方格位、-3 战力计入对方总点数） | — |
| ☠️ | 梅蒂欣·梅兰可莉 | 3 | de −2 | 揭示：同区对方所有已翻开卡牌 −2 | — |
| 🗿 | 比那名居天子 | 2 | spawn + 持续效果 | 揭示：本区域双方各获得 1 张「石块」；此牌在场时，己方所有「石块」战力 +2 | — |
| ☀️ | 灵乌路空 | 4 | xform + 防摧毁 | 揭示：将此区域变成「聚变反应炉」。此牌被摧毁时：不会被摧毁，取而代之永久降低 2 点战力 | — |
| 🦝 | 二岩猯藏 | 0 | morph | 揭示：此牌变成对方手牌中随机一张卡的复制体（变身后重新触发该卡的效果文本，如揭示、持续等；对方手牌为空则不变化） | — |
| 📖 | 帕秋莉·诺蕾姬 | 2 | 时机效果 · 回合开始抽法术（`drawSpell`，**重做 v172**） | 回合开始时：从帕秋莉法术池（火神之光 / 祖母绿巨石 / 绿色风暴 / 水银之毒 / 金属疲劳，均为 1 费法术，见 `docs/现有特殊卡牌.md`）中随机将 1 张法术加入手牌；手牌满 7 张则本次加入失败 | 红魔馆 |
| 🌈 | 三妖精集结 | 0（**法术 · 无战力**） | gather（**法术**） | 揭示：若可能，把三妖精随机生成到三个区域（每个区域自己一侧各一张、三张互不相同，并当场结算它们各自的揭示）；随后己方场上已翻开的「三妖精」各 +1 战力 | — |
| 🪨 | 地精的起床 | 0（**法术 · 无战力**） | tkBuff + spawnS（**法术** · **v179 新增**） | 揭示：若可能，在本区域自己一侧添加 1 张「石块」；随后己方场上所有「石块」永久 +1 战力 | — |
| ⚰️ | 小野塚小町 | 5 | `discard` 弃牌 · **`pick:'maxCost'`（v191 新增）** | 揭示：丢弃你手牌中能量消耗最高的一张牌（并列最高时随机弃一张；移入**你的**弃牌池；手牌为空则无事发生） | — |
| ⚔️ | 妖精大战争 | 0（**法术 · 无战力**） | `dwc` 按费用全场摧毁（**法术** · **v195 新增**） | 揭示：摧毁双方场上所有 1 费卡牌（按印刷费用；暗牌不算，含石块等 1 费 token） | — |

> ⚔️ **v195：新增法术「妖精大战争」（3 费 / 法术 · 无战力，无阵营）——全新效果键 `dwc`（按印刷费用全场摧毁）**。
> 卡面效果＝**摧毁双方场上所有一费牌**，写法 `k: 'dwc'` + `dwc: { cost: 1 }`（`cost` = 印刷费用，缺省 1）。
> 按规则 4 落在 **3 费组**（本组 **10 → 11** 张；全池合计 **49 → 50** 张）：**可入卡组**、会被玩家兜底牌库与
> AI 费用曲线抽到（AI 也能抽到它，见末尾提示）。口径（**用户确认**，完整说明见 `docs/现有机制.md`
> §1「妖精大战争（`dwc`）」段与 `data/cards.js` 的 `dwc=` 字段说明）：
> ① **范围＝双方场上三个区域**（左 → 中 → 右逐区结算，每区先己方后对方）——与 `deAll`（只打敌方三区）
>   同款“全场”范围，但**双方都打**；
> ② **只摧毁已翻开的牌**（**暗牌不算**，用户口径）——与 `dw`/`dwh`/`dwb`/地形 `purge` 的候选口径完全一致；
> ③ **含 1 费落场 token**（用户口径）：石块（虹龙洞 / 比那名居天子）、**v184 冰块**（琪露诺洗入对手牌库后
>   被打出）、祖母绿巨石占位（v172 法术产物）、水银（水银之毒）——它们的印刷费用都是 1，照常被扫掉；
> ④ 筛选按**印刷费用 `def.c`**（**不是** `cardCost`）：被桑尼米尔克加过费的 1 费牌**照常命中**（与
>   `discard.cost` / `og.cost` / 区域 `cb` / 图鉴分档同口径）；`un` 占位卡「隙间」（0 费）与**法术**
>   （含它自己，3 费）都不是目标；
> ⑤ **逐区受免摧毁保护**：某区是「睡鼠神祠」（地形 `prot`）或有「蕾蒂」（卡级 `prot`）→ **该区整区跳过**
>   （不选目标、也不触发 `surv`/`phx`）；每张目标再依次走 `ind`（自身不可摧毁，逐张跳过而非整条结束）/
>   `phx`（凤凰重生回手 +N）/ `surv`（防摧毁改永久降 N）；
> ⑥ **命中即全部摧毁**（**没有并列随机**、不是只挑一张；这是它与 `dwb`/`dwh` 的关键差别）；
>   ⚠️ **可能摧毁己方自己的 1 费牌**（卡面写的就是“双方”）；
> ⑦ 真正离场的牌**照常进其归属方的「摧毁池」**（v187，侧栏可查），因此也会给 v194「纯狐」的
>   `costDown` **推进减费计数**（本卡一口气摧毁几张就减几点）。
> **无专属配图**：按规则 1 第 5 条走卡面 emoji **⚔️** 兜底，**不拿别的立绘顶替**。
> 主题呼应：1 费牌基本都是妖精（小妖精 / 大妖精 / 琪露诺 / 露娜·切露德 / 桑尼米尔克 / 斯塔萨菲雅 /
> 莉莉霍瓦特 / 莉莉黑）——“妖精大战争”把它们一并扫下场。
> ⚠️ 提示（AI 侧）：`js/ai.js` **未收录 `dwc`**——AI 的 3 费档会抽到这张牌，抽到后按“认不出的法术”估值
> （同 v192 `reviveDiscard` 的情形）；**打出后效果照常结算**（AI 打它同样会连自己场上的 1 费牌一起清掉）。
> 是否让 AI 认这个键由用户决定。

> ✨ **v179：角色池法术「地精的起床」加入本组**（**3 费 / 法术 · 无战力**）——**可入卡组**、
> 会被玩家兜底牌库与 AI 费用曲线抽到（口径同「三妖精集结」）。揭示分**两步、顺序固定**：
> ① **若可能**，在本区域**自己一侧**添加 1 张「石块」（复用 `spawnS` 口径：该侧已放满则不加、只记日志；
> 对方一侧完全不受影响）；② **随后**，**己方场上所有「石块」永久 +1 战力**——**只算己方**
> （不含对方场上的石块）、**一次性永久**（不是持续光环：法术消散后加成留在石块身上，与天子
> `og`「己方石块常驻 +2、离场即失效」是两种机制）；因 ① 在前，**刚生成的那张石块也吃到本次 +1**。
> 实现为**新效果键 `tkBuff`**（标记卡增幅，`{ tk:'rock', a:1, own:true }`）+ 前置子句 `spawnS`
> （口径见 `docs/现有机制.md` §1 效果键表 `tkBuff` 行）。**无专属配图**：按规则 1 第 5 条走卡面
> emoji **🪨** 兜底（**v179 用户指定**，与 token「石块」同一 emoji——呼应“起床造石块、强化石块”的意象），**不拿别的立绘顶替**。本组现 **9** 张；全池合计 **40 → 41** 张。

> ⚰️ **v191：新增人物「小野塚小町」（3 费 / 5 战力，无阵营）**——她是 **v189 新增的「弃牌」机制
> （效果键 `discard`）的第二位使用者**（第一位是 v190 的「莉莉黑」）。按规则 4 落在 **3 费组**
> （本组 9 → **10** 张；全池合计 46 → **47** 张）：**可入卡组**、会被玩家兜底牌库与 AI 费用曲线抽到。
> 口径（**用户确认**）：
> ① **卡面效果**＝揭示：**丢弃你手牌中能量消耗最高的一张牌**；写法 `k: 'discard'` +
>   `discard: { to: 'own', pick: 'maxCost', n: 1 }`；
> ② **弃的是自己的手牌**（`to:'own'`）——**代价型**，与莉莉黑同取向：3 费 / 5 战力是本档最高身材
>   （3 费白板「中妖精」4 力），故配一个“丢自己最贵那张”的代价；
> ③ **`pick: 'maxCost'`（v191 随本卡新增的取牌方式）**＝取**印刷费用 `def.c` 最高**的那些，
>   **并列最高的多张之间随机弃一张**（口径同 dw/dwh/dwb 的“并列里随机挑一张”）；只弃 1 张。
>   ⚠️ 比较的是**印刷费用**、**不是 `cardCost`**——与既有 `discard.cost` 筛选、区域 `cb` 费用加成、
>   `og.cost`、图鉴/卡组页分档同口径：**被桑尼米尔克加过费只改实际能量消耗、不改变“贵贱”排序**
>   （用户确认口径）；
> ④ **不是“摧毁”**：被弃的牌从手牌移出、进**你的**弃牌池（`discardPile`，侧栏「己方特殊牌池」→
>   「弃牌池」页签可查）；不触发 `surv`/`phx`/`prot`/`ind`、不改战力、不动区域字段与格位
>   （完整口径见 `docs/现有机制.md` §1「弃牌（`discard`）」段）；
> ⑤ **时序**：揭示发生在阶段 ④（暗出之后），此刻这张小町**已经不在手牌里**，故“最贵”按
>   **揭示那一刻的剩余手牌**判定；手牌为空（含“她是手上最后一张”）→ 无牌可弃、只记一条日志；
> ⑥ **公开**：弃牌**双方可见**——日志点名 + 场地中央弹出**被弃那张牌的完整卡面**并播
>   **从右上到左下斜切两半**的演出（约 1.6s）；
> ⑦ **无阵营 `g`**（三途河的摆渡人不属于红魔馆 / 光之三妖精）：不吃任何 `aff` 阵营加成，
>   也不会被法术「三妖精集结」的 `gather` 抽到。
> **配图**：自 `assets/source-art/240px-小野塚小町（Q版立绘）.png` 复制为 **`assets/cards/komachi.png`**
> （**240×240**，规则 1）；卡面 emoji ⚰️ 仅作缺图兜底。素材状态见 `docs/card-image-map.md`
> 与 `README.md` 的「已知待办（素材侧）」。

## 4 费

| 图标 | 名称 | 威力 | 效果类型 | 效果文本 | 所属 |
| --- | --- | --- | --- | --- | --- |
| 👹 | 中妖怪 | 6 | 白板 | 无 | — |
| 🦋 | 西行寺幽幽子 | 4 | de −2 | 揭示：同区对方所有已翻开卡牌 −2 | — |
| 🌕 | 芙兰朵露·斯卡蕾特 | 3 | dw | 揭示：摧毁同区对方已翻开的威力最低卡牌 | 红魔馆 |
| 🎆 | 雾雨魔理沙 | 6 | give（加入手牌衍生物 · **v177 重做**） | 揭示：将一张「极限火花」（4 费 · **法术 · 无战力**，揭示：摧毁此区域对方已翻开、战力最高的一张卡牌，并列最高随机挑一张）加入手牌；手牌满 7 张则加入失败 | — |
| 🎪 | 克劳恩皮丝 | 4 | 持续（og · 按费用强化 · **v179 新增**） | 持续：此牌在场时，己方场上所有 1 费卡牌（含特殊卡）战力 +1 | — |
| 🦇 | 蕾米莉亚·斯卡蕾特 | 5 | 时机效果 · 回合结束落后自增（`fx.turnEnd` → `bl +3` · **v179 新增**） | 回合结束时：若此牌所在区域落后，则此牌永久 +3 战力（每回合末各判一次，可累积） | 红魔馆 |

> 🔧 **v188：西行寺幽幽子下调 —— 揭示 `de` 由 −3 改为 −2**（**4 费 / 4 战力**、费用与战力均未变，
> 故**留在本组**、卡名不变、卡组存档照旧能读回）。口径：仍是既有键 `de`——**同区对方所有已翻开
> 卡牌各永久 −2**（只作用于结算那刻已翻开的对方卡，排除 `un` 占位卡与法术；含落场 token；
> 走 `applyPermBuff` 收口 → ±N 演出 + 战力影响历史按来源记本卡），**未新增任何机制**，
> 等同于 v172「梅蒂欣·梅兰可莉」的 −2 档。落地改动只有 `data/cards.js` 的 `POOL[4]` 条目
> （`a: 3 → 2`、效果文案 `-3 → -2`）；`docs/现有机制.md` §6 AI 策略条里作为例子的
> 「`de −3` 当 4 力一张牌」同步改为 `de −2`。本组仍 **6** 张，全池仍 **45** 张。
> ⚠️ `docs/AI强化设计.md`（设计稿 · 未实装）里为「瘴气塞牌」等预设卡组引用的
> 「幽幽子 −3」按设计稿当时的写法**保留原样**，未追溯改写。

> ✨ **v179：蕾米莉亚·斯卡蕾特加入本组**（**4 费 / 5 战力**，阵营 **`scarlet` 红魔馆**——照吃「红魔馆」地形常驻 +2；
> 配图 `assets/cards/remilia.png` 240×240，v178 起就已就位，**本卡实装后该图从「保留备用」转正**）——
> **时机效果「回合结束」**：若此牌所在区域**落后**，则自身**永久 +3 战力**。口径：
> ① **实现＝既有键 `bl` 挂在 `fx.turnEnd` 上（引擎零改动）**；落后判定用**本区按胜负口径比较**
> （`zoneEff`，含 `dbl`；**辉针城等反转区按负值**——"低者胜"语义下真实点数更高才算落后）；
> ② **每回合结束各判一次、可累积**：连续落后就每回合末再 +3，走 `applyPermBuff`（±N 演出 +
> 战力影响历史，来源＝自身 → 面板显示「卡牌效果」）；
> ③ 结算点在**地形类回合末效果（⑤-0）之后**，故读到的是本区被地形改动过的实时威力；第 6 回合末
> 同样结算并计入终局；**「回合结束」不受 v160「刚登场跳过当回合」限制**——翻开的当回合末即判一次；
> ④ 被摧毁 / 凤凰重生回手后出队，不再触发（既有 `fx` 口径）。
> 本组现 **6** 张；全池合计 **41 → 42** 张。她也是**首个**使用 `fx.turnEnd` 的卡（此前该时机无注册者）。

> ✨ **v179：克劳恩皮丝加入本组**（**4 费 / 4 战力**，无阵营；配图自 `assets/source-art/240px-克劳恩皮丝（Q版立绘）.png`
> → `assets/cards/clownpiece.png`，规则 1）——**持续**效果：此牌在场（**已翻开且仍在己方某区**）期间，
> **己方场上所有 1 费卡牌战力 +1**。口径：
> ① **按印刷费用 `def.c === 1` 匹配**（同「雾之湖」的 `cb` 口径——被桑尼米尔克加过费的卡仍算原 1 费档位）；
> ② **含 1 费特殊卡 token**（石块 1费/0力、祖母绿巨石占位 1费/3力；水银 1费/−1 落在对方一侧故通常吃不到）；
> ③ **法术无战力**、不吃任何战力加成，故不受影响；
> ④ **实时派生**（同天子 og、雾之湖 cb）：皮丝被摧毁 / 凤凰重生回手 / 换边离场立即失效，已加成不残留，
> 不计入永久 buff 台账，但会显示在卡牌「战力影响历史」的**持续效果**行里。
> 实现为卡级机制 `og` 的**新匹配口径** `og: { cost: 1, add: 1 }`（口径见 `docs/现有机制.md` §1「持续效果（og）」段）。
> 本组现 **5** 张；全池合计 **38 → 39** 张（3 费组 8 张、其余组不变）。

> 🔧 **v177：雾雨魔理沙重做并搬入本组**——原 **3 费 / 3 战力** `k='dw'`（揭示：摧毁同区对方已翻开的威力最低卡牌）
> → **4 费 / 6 战力** `k='give'`（揭示：将一张法术「极限火花」加入手牌）。按规则 4「卡片放在它费用的那一档」
> 由 3 费组移入本组；配图为原 `marisa.png`、阵营不变（无）。加入的衍生物是**法术**
> （见 `docs/现有特殊卡牌.md` 的「极限火花」），入手后仍需**手动暗出**，翻开结算后按法术口径自行消散。
> 本组现 **4** 张；3 费组随之变为 **8** 张；全池合计仍为 **38** 张。

## 5 费

| 图标 | 名称 | 威力 | 效果类型 | 效果文本 | 所属 |
| --- | --- | --- | --- | --- | --- |
| 📉 | 依神紫苑 | −7 | switch | 揭示：此牌换边——翻开后从自己一侧转移到对方该区域一侧（对方该区已放满则换边失败，留在原地） | — |
| 🍶 | 伊吹萃香 | 10 | 大体积占格（occ 4） | 此牌占据本区域全部 4 个位置（只能放入上限为 4 且己方该区没有任何卡的区域；被摧毁/离开后腾出 4 个空位） | — |
| 🐲 | 大妖怪 | 8 | 白板 | 无 | — |
| 🧪 | 八意永琳 | 5 | dw | 揭示：摧毁同区对方已翻开的威力最低卡牌 | — |
| 🍃 | 射命丸文 | 7 | 每回合移动一次 | 出牌阶段可把此牌移动到另一区域一次（当回合已翻开的己方文） | — |
| 🌗 | 蓬莱山辉夜 | 8 | give · **`pool` 写法（v180 新增）** | 揭示：将随机两张「神宝」加入手牌——从 5 张神宝（无限生命泉 / 火蜥蜴之盾 / 佛体金刚石 / 耀眼之龙玉 / 蓬莱的玉枝）里**不放回随机抽 2 张（互不相同）**；加入的是**新卡实例**、仍需**手动暗出**；**手牌满 7 张则加入失败**（只加入放得下的那几张，日志写明） | — |

> **v180 · 蓬莱山辉夜**：本组 **5 → 6** 张；全池合计 **42 → 43** 张。
> - 她是既有键 **`give`** 的新写法 **`pool`** 的首个使用者：`give: { pool: [SPECIAL 键名…], n }` ——
>   与单卡写法（`{ card, n }`）的差别只在**来源是池、且不放回随机抽**（互不相同）；其余口径完全一致
>   （新卡实例、`justHandAdded` 滑入演出、手牌满 7 张则失败/部分加入、归属按所属方）。
> - 抽到的 6 张神宝/龙玉都是**衍生物池卡**（`SPECIAL`）：**不入牌库、不进图鉴人物卡档**
>   （只在图鉴**「衍生卡牌池」**与她的「衍生卡牌」面板可见）；其中**耀眼之龙玉 / 蓬莱的玉枝是法术**
>   （走「暗出 → 翻牌揭示 → 自行消散」），另 4 张（无限生命泉 / 火蜥蜴之盾 / 佛体金刚石 / 龙玉）
>   是**落场型衍生物**（打出后留在场上、占格位）。清单见 `docs/现有特殊卡牌.md`。
> - **配图 `assets/cards/kaguya.png`**（自 `assets/source-art/240px-蓬莱山辉夜（Q版立绘）.png` 复制，规则 1），
>   emoji 🌗 作缺图兜底。

## 6 费

| 图标 | 名称 | 威力 | 效果类型 | 效果文本 | 所属 |
| --- | --- | --- | --- | --- | --- |
| 🌻 | 风见幽香 | 12 | 白板 | 无 | — |
| 🧙 | 贤者 | 12 | 白板 | 无 | — |
| 🐸 | 洩矢诹访子 | 8 | bl +6 | 揭示：若你在此区落后，此卡 +6 | — |
| 🪞 | 鬼人正邪 | 6 | 区域变形 | 揭示：将此区域变为「辉针城」（战力低者胜；原限张区域的隙间消失、上限变 4） | — |
| 💠 | 赫卡提亚 | 5 | clone（分身） | 揭示：若可能，向另外两个区域各添加 1 张「赫卡提亚的分身」（战力=此牌揭示时）。分身无其他效果 | — |
| ✨ | 八云紫 | 10 | shift | 揭示：将你最左侧区域所有已翻开的卡牌依次移动到最右侧区域，直至最右侧区域填满为止 | — |
| ⚖️ | 四季映姬 | 6 | `reviveDiscard` 复活弃牌池（**v192 新增**） | 揭示：若可能，将你所有被丢弃的**角色卡牌**以随机顺序复活到随机区域中（落地即翻开、并逐张重新结算其揭示；法术不参与复活） | — |
| 🐟 | 大鲶鱼 | 16 | **放置条件 `playReq`（v196 新增）** | **仅在你的场上已翻开的牌中有至少 4 张「石块」时，可以被放置到场上**（只在从手牌打出那一刻检查；打出后不再追踪） | — |

> ⚖️ **v192：新增人物「四季映姬」（6 费 / 6 战力，无阵营）**——她是 **v192 新增效果键
> `reviveDiscard`（复活弃牌池）的首个使用者**：**若可能，将你所有被丢弃的角色卡牌，以随机顺序
> 复活到随机区域中**。按规则 4 落在 **6 费组**（本组 6 → **7** 张；全池合计 47 → **48** 张）：
> **可入卡组**、会被玩家兜底牌库与 AI 费用曲线抽到。口径（**用户确认**）：
> ① **复活对象**＝**你自己弃牌池**（`discardPile`，入池来源只有「弃牌」`discard`）里的
>   **所有「角色卡牌」**＝**非法术的牌**；**法术不参与复活**、留在池里（用户口径：卡面文字因此写作
>   “角色卡牌”）——⚠️ 落场型衍生卡（辉夜给的无限生命泉 / 火蜥蜴之盾 / 佛体金刚石等神宝）属
>   **非法术卡，照常会被复活**；
> ② **随机顺序**：候选洗牌后逐张依次复活；
> ③ **随机区域**：每张牌**只在“已开放（避开七夕坂等 minTurn 锁定）且自己该侧放得下（含大体积卡
>   占格）”的区域里等概率随机**——**不会**出现“掷到一个已放满的区域就复活失败”的情况；三区都
>   放不下或未开放 → 该牌**留在弃牌池**（“若可能”），日志点名；
> ④ **落地即翻开**：占格位、进场上放置顺序队列（故其「回合开始」时机效果按 v160 当回合不结算）、
>   播「凝聚显形」；**逐张重新结算其自身「揭示」**（用户口径，同步结算、口径同 `spawn.reveal`／
>   「集结」——复活辉夜会再抽 2 张神宝、复活魔理沙会再给 1 张极限火花）；
> ⑤ 牌本体带着**永久增益台账**与**本场战斗的费用修正**回到场上（离池时清掉入池元数据）；
> ⑥ **不是“暗出”、不是“打出”**：不消耗能量、不写落牌记录 ⇒ 不会被翻牌流程二次结算，
>   也**不会被「重置暗牌」收回**；
> ⑦ 与 `surv`/`phx`/`prot`/`ind`、摧毁池、区域「翻开时」博彩 `gamble` 与「揭示后吹飞」`gust`
>   均无交互（复活卡落地即已翻开，不经过 revealRound）。
> 完整口径见 `docs/现有机制.md` §1「复活弃牌池（`reviveDiscard`）」段。
> 🎬 **v193：复活改为「逐张」分步演出（用户口径：避免一次性复活太多牌）** —— 正常翻牌流程里
> 逐张推进：**捞出一张 → 播「凝聚显形」→ 结算这一张的揭示 → 停 500ms → 下一张**；其它路径
> （`morph` / `fx` 时机效果 / 落场生成）仍走一次性同步结算，两条路径口径完全一致。
> 节奏常量 `REVIVE_STEP_MS = 500`（纯 JS 计时、无 CSS 关键帧）——详见
> `docs/现有机制.md` §1「复活弃牌池」段末条与 §7 的 v193 条。
> **配图**：自 `assets/source-art/240px-四季映姬（Q版立绘）.png` 复制为 **`assets/cards/eiki.png`**
> （**240×240**，规则 1）；卡面 emoji ⚖️ 仅作缺图兜底。素材状态见 `docs/card-image-map.md`
> 与 `README.md` 的「已知待办（素材侧）」。
> ⚠️ 提示（AI 侧）：`js/ai.js` 目前**未收录 `reviveDiscard`**，AI 抽到这张牌只按白板估值；
> 打出后效果照常结算（复活的是 **AI 自己的**弃牌池）。是否让 AI 认这个键由用户决定。

> 🐟 **v196：新增人物「大鲶鱼」（6 费 / 16 战力，无阵营）**——他是 **v196 新增卡级机制
> `playReq`（放置条件）的首个使用者**：卡面效果＝**仅在你的场上已翻开的牌中有至少 4 张「石块」时，
> 可以被放置到场上**（`k: ''`：**没有揭示效果、也不是持续效果**，效果全部体现在这条放置条件上）。
> 按规则 4 落在 **6 费组**（本组 7 → **8** 张；全池合计 50 → **51** 张）：**可入卡组**、会被玩家
> 兜底牌库（`DECK_CURVE`）与 AI 费用曲线（`AI_DECK_CURVE`，两曲线都到 6 费）抽到。口径（**用户确认**）：
> ① **判定时点＝“从手牌打出（暗出）”那一刻**：己方**三个区域**里**已翻开**的「石块」不足 4 张 →
>   **打不出来**——玩家点区域不落牌、区域不高亮、手牌置灰并给出悬停提示（`setStatus` + `sys` 日志会写
>   “现 N 张”），**不扣能量、不写落牌记录**；AI 侧同一个校验收口，条件不成立时**根本不会把它列入出牌候选**；
> ② **打出后不再追踪**：石块之后被摧毁（如妖精大战争、聚变反应炉）、被移走、被换边到对方一侧使数量
>   掉到 4 张以下，这张牌**照常留在场上**，不移除、不变动战力；
> ③ **计数口径**：只算**自己一侧**、只算**已翻开**、只认**「石块」**（按 `def.tk === 'rock'` 标记匹配）——
>   **暗牌不计**、**对方场上的石块不计**、其它同为 1 费 token 的落场卡（祖母绿巨石占位 / 水银 / 冰块）
>   也**不计**；
> ④ **其它“上场”路径一律不受限**（用户口径）：**复活弃牌池**（四季映姬的 `reviveDiscard`）、
>   **变身**（二岩猯藏的 `morph` 复制体）、**落场生成**（`placeToken` / `clone` / `spawn*`）都不检查；
>   `give` 塞进手牌后**再由手牌打出时**照常检查（检查的正是“手牌里这张牌”）；
> ⑤ **不是“摧毁”/非增减/非放置类**：不触发 `surv`/`phx`/`prot`/`ind`、不动区域字段与格位
>   （`sideUsed`/`sideRoom`/`fill`）、不进 `powerLog`、不进 `fieldQueue`；与区域 `cb`（如雾之湖 1 费 +2）
>   等战力类口径无关（本条件只看**张数**，不看石块战力）。
> **引擎落地**：`js/game.js` 新增 `playReqCheck(side, card)`（唯一校验收口，返回 `{ ok, need, have, label }`）
> 与共用的 `tokenNameLabel(tk)`（由 tk 标记反推可读 token 名，`tkBuff` 段同步改用）；
> 玩家路径接在 `tryPlayAt`（点区落牌）与 `canPlaceP`（区域高亮）+ 手牌渲染（置灰 + 悬停提示）；
> AI 路径（`js/ai.js`，**v196 仅此一处最小改动**）接在 `aiPlanMoves`（候选过滤）、简单档
> `aiThinkLegacy` 的候选过滤、`aiThinkScoreEngine` 的出牌前复核，并给 `aiReasonText` 补一条
> “放置条件”说明，避免把它当成“纯战力（无效果）”。图鉴/放大视图的效果分类标签由 `kindTags` 输出
> 「放置条件 · 仅当你场上已有 ≥ 4 张已翻开的「石块」时可从手牌打出」（不再显示“白板”）。
> **配图**：自 `assets/source-art/大鲶鱼（非想天则立绘）.png` 复制为 **`assets/cards/namazu.png`**
> （**534×325**、161 KB，**非正方形**——口径同 v182 的 `fairies.png` 448×488；场上缩略卡为 cover 裁切）；
> 卡面 emoji 🐟 仅作缺图兜底。素材状态见 `docs/card-image-map.md` 与 `README.md`「已知待办（素材侧）」。
> **联动提示**：现有产石块的手段＝地形「虹龙洞」（出现时双方各 1 张）、人物「比那名居天子」
> （揭示本区双方各 1 张）、法术「镇守大地之石」（`spawnS.fill` 铺满自己一侧）、法术「地精的起床」
> （先 1 张再己方石块永久 +1）——要凑够 4 张通常需要**多张产石块手段叠加**（无名之丘一区即可铺满 4 张）。

## 7 费（**v185 新增档位**）

| 图标 | 名称 | 威力 | 效果类型 | 效果文本 | 所属 |
| --- | --- | --- | --- | --- | --- |
| 💤 | 哆来咪 | 8 | **游戏开始时（`gs`）** | 游戏开始时（此牌在你的卡组中即触发）：从卡牌池随机洗入 10 张牌到你的卡组（第一回合起就可能被抽到）；本局你每回合最大能量 +1 | — |

> **v185 · 哆来咪（**7 费 / 8 战力**，无阵营）**：本组 **1** 张；全池合计 **44 → 45** 张。
> ⚠️ **7 费是全新费用档**：`data/cards.js` 的 `POOL` 新增 `7` 这一桶，因此引擎与三个页面的
> “遍历费用档”同步放开了上限（`game.js` 的 `POOL_COST_KEYS`、`card-browser.js` / `deck-page.js`
> 的 `allPoolDefs()`），图鉴 /「指定卡牌」/ 卡组设置页的费用档里**原「6 费」按钮改为「6 费+」**
> （＝6 费及以上；档内顺序仍是「费用↑ → 战力↑ → 卡名字典序」，故 6 费卡在前、7 费卡在后）。
> ⚠️ **卡面费用 7 > 每回合能量上限 6**：普通局单靠基数打不出它——它自己的
> `gs.energyAdd: 1` 让本局每回合上限 +1（第 1 回合 2 点 … **第 6 回合 7 点**），
> 因此**只有在第 6 回合才打得出来**（设计意图）。
> ⚠️ **随机牌库口径不变**：玩家兜底曲线 `DECK_CURVE`、对手曲线 `AI_DECK_CURVE` 都只到 6 费，
> 所以**哆来咪只能通过「卡组设置」自建 12 张卡组带入**（AI 永远抽不到它）。
> ⚠️ 效果口径（触发条件 / 10 张从哪来 / 是否碰衍生卡池 / 与 `energyNext` 的叠加）见
> `docs/现有机制.md` §1「游戏开始时（`gs`）」段；配图 `assets/cards/doremy.png`
> （自 `assets/source-art/240px-哆来咪（Q版立绘）.png` 复制，规则 1），emoji 💤 作缺图兜底。
> 🎬 **开局演出**：卡组里带了它时，开局会先播一段约 1.8s 的「登场」大卡面动画（遮幕 + 弹入 +
> 光环 + 梦之星光），**演完才开始抽初始手牌** —— 见 `docs/现有机制.md` §1 末条与 §6 演出条。
> 🎨 **卡面底色**：7 费档的底色是 `game.js` `GRADS[7]`（深紫罗兰 → 淡紫）；新增费用档都要在那里补一条，
> 否则卡面 `--cgrad` 为空、整张卡会透明（v185 已修，并给 `gradOf` 加了兜底）。

## 8 费（**v194 新增档位**）

| 图标 | 名称 | 威力 | 效果类型 | 效果文本 | 所属 |
| --- | --- | --- | --- | --- | --- |
| 🌑 | 纯狐 | 16 | **费用随摧毁递减（`costDown`）** | 持续：本场战斗中双方每有一张牌被摧毁，此牌能量消耗 −1（最低 0 费） | — |

> **v194 · 纯狐（**8 费 / 16 战力**，无阵营）**：本组 **1** 张；全池合计 **48 → 49** 张。
> 她是**全新卡级持续机制 `costDown`（费用随摧毁递减）**的首个（也是唯一）使用者——**没有揭示效果**
> （`k: ''`），效果**持续生效、不需要打出**（还在手牌/牌库里就一直算），写法 `costDown: 1`
> （含义＝每有一张牌被摧毁就 −1 费；完整口径见 `data/cards.js` 的 `costDown=` 字段说明与
> `docs/现有机制.md` §5「费用修正」段）。
> ⚠️ **8 费是全新费用档**：`data/cards.js` 的 `POOL` 新增 `8` 这一桶。分档联动**只剩一处**需要改、
> 且已改：**卡面底色 `GRADS[8]`**（game.js，月夜深靛 → 冷银白）——漏了会让整张卡透明（v185 的 7 费档
> 踩过这个坑）；费用档遍历（`POOL_COST_KEYS` / 两个页面 `allPoolDefs()`）与图鉴 /「指定卡牌」/
> 卡组设置的**「6 费+」**筛选档（＝6 费及以上）都已按 v185 口径做成动态，8 费组**自动跟随**。
> ⚠️ **卡面费用 8 > 每回合能量上限 6**：普通局单靠能量基数**根本打不出来**——这张牌**就是靠自己的
> 减费机制上桌**（双方累计被摧毁 ≥2 张 → 6 费即可打出；≥8 张 → 0 费），这是设计意图；
> 配合哆来咪（本局每回合上限 +1，最高 7）或斯塔萨菲雅（下回合 +1）能更早打出。
> ⚠️ **计数口径（用户确认）**：只算**真正进「摧毁池」**的摧毁——`dw` 最弱 / `dwh` 最强 /
> `dwb` 双方最弱随机一张 / 地形 `purge` 回合末最低，**各计 1 张**（`purge` 并列摧毁多张就计多张），
> 即**双方摧毁池张数之和**（实时读取）；**摧毁失败一律不计**（凤凰重生 `phx`、防摧毁 `surv`、
> 自身不可摧毁 `ind`、区域免摧毁 `prot`／蕾蒂），**其它离场方式也不计**（弃牌 `discard`、法术消散、
> 终局离场 `leave`、撤回手牌、移动、换边）。**下限 0 费**（用户口径），不会出现负数费用。
> ⚠️ **随机牌库口径**：玩家兜底曲线 `DECK_CURVE`、对手曲线 `AI_DECK_CURVE` 都只到 6 费，所以
> **纯狐只能通过「卡组设置」自建 12 张卡组带入**（AI 永远抽不到它）；**但 v194 起哆来咪的
> `gs.shuffleN` 洗入池由「1~6 费」放宽为「1 费及以上」，所以纯狐可能被哆来咪从卡池洗进牌库**
> （7 费哆来咪自己仍因带 `gs` 被排除 ⇒ **不会洗出第二张哆来咪**，用户口径）。
> 🎨 **卡面底色**：8 费档是 `game.js` `GRADS[8]`（`#1b2140 → #d7e0ff`，月夜深靛 → 冷银白，
> 与 1 费档的蓝区分开）。
> **配图**：自 `assets/source-art/240px-纯狐（Q版立绘）.png` 复制为 **`assets/cards/junko.png`**
> （**240×240**，规则 1）；卡面 emoji 🌑 仅作缺图兜底。素材状态见 `docs/card-image-map.md`
> 与 `README.md` 的「已知待办（素材侧）」。
> ⚠️ 提示（AI 侧）：`js/ai.js` **未收录本机制**（AI 的卡组曲线也不含 8 费，正常拿不到纯狐）；
> 若日后要让 AI 也能用这张牌，需要 AI 侧按 `cardCost` 的实际费用估值——由用户决定。
