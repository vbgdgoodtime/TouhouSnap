# 东方逆转 · 联机改造进度与交接

> **本文件是"当前进度 + 交接"文档**，给新对话开场读。与 `docs/开发规则.md`、`docs/联机对战.md` 一起读即可开工。
> **接下来要做的界面/布局打磨**抄在 `PvP联机UI待办.md`：对手动作的即时提示、对局页布局、主页面视觉与新手引导。
> **本轮已实现、待用户两台设备实测**：对手信息区（昵称 / 头像 / 他这一手交了没有）、顶栏状态条重做、房间弹窗重做、
> 联机前显式选卡组、结算弹窗与「🔁 再来一局」；配套的 `worker/` 改动（新一局开始时清空去重表）**需要重新 deploy 才生效**。
> 定位说明：`PvP联机需求.md` 是**最初的规划**（其中的做法已被实际实现取代）；
> **当前实现口径以 `docs/联机对战.md` 为准，当前进度以本文件为准。**

## 0. 现在到哪一步了

| 项 | 状态 |
| --- | --- |
| 阶段 0 地基（随机源分离 / 状态指纹 / 数据哈希 / 若干 bug 修复） | ✅ 已完成并被用户验证 |
| 阶段 1（挑战码：录制 → 复盘 / 同一局面挑战） | ✅ 已完成并被用户验证 |
| 赌注口径改造（双方各一次加倍 / 下回合生效 / 撤退按加倍前的值 / 码升级 `TH2P2`） | ✅ 已改完，联机对打中已生效；**细则（撤退保值 / 末回合加倍）待逐项确认** |
| 阶段 2 客户端（`js/net.js`、房间界面、引擎联机挂钩与座位序） | ✅ 已实测：两台设备打通一整局 |
| 阶段 2 房间服务（`worker/`：Workers + Durable Objects） | ✅ 已部署（`touhou2-pvp.touhou2-pvp.workers.dev`）并实测可用 |
| 断线重连 / 掉线托管 | ⬜ 不做（用户已明确：等不到提交包就直接判那一方认输） |
| 阶段 3（服务端权威 / 陌生人匹配 / 防作弊） | ⬜ 不做（按需再说） |

**一句话成果**：任何一局都能烘焙成一行文本码（`TH2P2:…`），别的机器凭这行码能把整局**一字不差地重算出来**；
实时房间也建立在同一套"两端各算一遍"的地基上——房间只转发消息，每回合交换一次动作，**每回合对账**当场暴露分叉，已实测打通一整局。

**还剩三件可选的事**（都不是阻塞项）：
1. **（推荐）绑一个自定义域名**给房间服务：现在必须两人都挂梯子才能连（`workers.dev` 在国内被 DNS 投毒），换成自己的域名后谁都不用挂 —— 步骤见 `worker/README.md` 与本文件 §6。
2. **把 §3 的回归基线重新采一次**（旧值 `d4f9ff26` / `c82cad9e` 已随指纹口径与结算值变化而失效）。
3. **逐项确认赌注细则**：撤退保值（加倍生效前认输只输 2）、末回合加倍直接结算、双方各一次。

**下一步的界面工作**：还剩 3 项待办——对手动作的即时提示（加倍反馈 / 音效）、对局页整体布局、主页面与各入口的视觉，
逐项要求与红线见 **`PvP联机UI待办.md`**。

## 1. 必须遵守的规矩（权威是 `docs/开发规则.md`，这里只列最容易被违反的）

1. **规则 1（最重要）**：只写当前内容、一切改动先精简；不写版本号、不写历史、"以前是怎样"。
2. **规则 6（第二重要）**：**验证由用户负责**——AI 不跑测试、不做烟雾测试、不开浏览器、不起本地服务器；
   并且**不读 `js/ai.js` 与 `tests/` 下的任何脚本**（除非用户明确要求）。
   允许：读代码、grep / glob、不产生运行的语法自查（如 `node --check`）。
   交付方式＝列出改动文件清单 + 建议用户验证的步骤与现象。
3. **规则 7**：有不确定的先问用户，并把候选方案与影响一并列出。
4. 技术约束：**纯前端、无构建工具、无 ES Module、`index.html` 里的脚本加载次序不能动**
   （data → game → ai → card-browser → home → deck-storage → deck-page → dev-page → **net**，`net.js` 加在最后）。
5. 交付前**用 grep 复核**自己声称改过的地方（本次开发中曾两次"只在回复里说改了、实际没改"，用户为此白跑验证）。

## 2. 架构事实（读代码时先知道这些）

- **一局的完整输入 = 种子 + 双方卡组 + 建牌路径 + 数据版本 + 双方每回合的动作。** 五样缺一，两端就算不出同一局。
- **两条互相独立的随机流**（`js/game.js` 顶部）：
  - `rng()`＝主逻辑流（洗牌 / 抽地形 / 翻牌先后 / 掷骰 / 并列取谁 / 随机取牌）；
  - `aiRng()`＝只给 AI 决策用（阶段③ 临时把全局 `Math.random` 换成它，**只在已播种时替换**）；
  - 表现随机（粒子 / 飘字 / 装饰）继续用原生 `Math.random`，不参与复现；
  - `NATIVE_RANDOM` 是脚本加载时捕获的原生引用——**回退必须指向它**（否则 `aiRng` 内部再调 `Math.random()` 就是自己调自己，
    栈溢出 + 整局卡死，已踩过）。
  - **必须分开的原因**：AI 的随机**不属于"一局的输入"**——同一个种子 + 同一串动作必须算出同一局
    （挑战码重放、联机两端对账都靠这条）；AI 若吃走主逻辑流，随机流的位置会随它思考几次而漂移。
- **每局自动播种**：`restart(opts)` 用 `opts.seed`，不传就 `freshSeed()`。`Game.seed(n)` **已被移除**；要指定种子用 `Game._newGame(n)`。
- **对局主循环** `playRound(gen)`：① 回合开始 → ② `await waitPlayer()` → ③ 对手 → ④ 翻牌揭示 → ⑤⑥ 回合末 → 终局判定。
  - 阶段② 在**复盘**模式下跳过玩家等待（动作取自记录）；
  - 阶段③ 依次判断：**重放**（取记录）→ **联机**（等对手提交包；等不到就判他认输）→
    **开发调试**（AI 不出牌）→ 否则跑 `aiThink()`。
  - 阶段④ 之前有一个**联机对账点**（`Net.reconcileRound`：双方动作都灌完、翻牌未开始，两端指纹必须一致）。
- **记录的两个提交点**：`recordTurnInputs('p')`（`waitPlayer()` 返回之后）、`recordTurnInputs('a')`（阶段③ 之后）。
  移动（`fly`）不在 `playerMoves`/`aiMoves` 里，是从 `flyMovedFrom` + 该方场上所在区域**反推**的。
- **重放状态由 `restart` 每局显式设定或清空**（`opts.replay` / `opts.netPvp`），不靠"记得手动清"。
- **房间座位序 `state.seatOrder`**：联机两端各自把本地玩家当 `p`、状态互为镜像，字面座位在两端指向不同的人；
  凡是"按座位遍历、消耗随机数或决定先后"的地方都必须走 `seatOrder`（房主在先），否则同一颗随机数会派给不同的人。
  单机恒为 `['p','a']` ⇒ 单机行为与从前一字不差。口径与覆盖清单见 `docs/联机对战.md` §5。
- **联机下每次开局都是"双方各带 12 张自建卡组"**（`opts.netPvp`），建牌顺序一律**房主在先**（卡牌 id 与卡组顺序两端才会一致）。

## 3. 已实装的对外接口与探针（都在 `window.Game`，联机另在 `window.Net`）

| 接口 | 用途 |
| --- | --- |
| `dataHash()` | 数据内容哈希（卡池 / 特殊卡 / 阵营分组 / 地形；排除 `img`/`i`）。**当前值 `2fa1d6d2`** |
| `_fingerprint()` | 状态指纹：回合 / 阶段 / 赌注 / 加倍状态 / 三块地形 / 放置队列 / 双方（**按座位序取、键名 `s0`/`s1`**）能量·手牌·牌库·三区·三个特殊牌池 |
| `_inputs()` | 这一局由哪些输入决定（双方卡组顺序 / 地形 / 起手 / 能量） |
| `_newGame(seed, opts)` | 设种子重开并等到「停在等你操作」，返回该时刻指纹；`opts.freshDeck` 强制默认曲线卡组、`opts.autoPass` 自动打完返回终局指纹 |
| `_record()` / `_code()` / `_replayInfo(code)` | 本局录制内容 / 一行挑战码 / 把码解回可读结构（与 `_record()` 同构） |
| `_replay(code, { mode })` | `review` 返回终局指纹；`shadow` 等到「轮到你」时交还控制权并返回那一刻指纹 |
| `ui.onChallengeExport / onChallengeOpen / onChallengeCopy / onChallengeReview / onChallengeShadow / closeChallenge` | 挑战码弹窗的六个入口（界面在 `index.html` 的 `#challengeMask`） |
| `net.restart / localPackage / applyPackage / applyPeerSnap / applyPeerRetreat / fingerprint` | 联机引擎侧挂钩（`js/net.js` 调用）：按握手结果开局 / 打提交包 / 灌包 / 应用对手的加倍与认输 |
| `Net.active / awaitRemoteTurn / submitLocalTurn / sendSnap / sendRetreat / sendTimeout / reconcileRound / abort / ui.*` | 联机通道与房间界面（`js/net.js` 的 `window.Net`） |
| `Net.room() / Net.role() / Net.peerOnline() / Net.timeoutSeconds()` | 联机：当前房间码 / 本机角色 / 对手是否在线 / 超时判负的秒数 |

**回归基线**（⚠️ 指纹口径与结算值本轮都变过，**旧值 `d4f9ff26` / `c82cad9e` 已经对不上** —— 请在浏览器里用下面两条命令重新采集后填回这里）：

- `await Game._newGame(3, { freshDeck: true })` → （待填）
- `await Game._newGame(3, { freshDeck: true, autoPass: true })` → （待填）
- `Game.dataHash()` → `2fa1d6d2`（数据文件未动，应仍是这个）
- `JSON.stringify(Game._replayInfo(Game._code())) === JSON.stringify(Game._record())` → `true`（应仍成立）

## 4. 已经踩过并修好的坑（**对下一个对话最有价值的部分**）

1. **建牌路径是输入的一部分**：自建 12 张（`buildDeckFromDefs`，洗 1 次）与费用曲线随机组
   （`buildDeckCards`，每个费用档各洗一次 + 再洗一次曲线）**消耗的随机数个数不同**——走错一条，后面整条流错位。
2. **卡组不能"再洗一遍"**：记录里的 `pDeck` 是**洗完之​后**的顺序，而 `buildDeckFromDefs` 的洗牌输入是**玩家选的顺序**。
   重放时必须用 `buildDeckInOrder()`：**不洗**、但**照样消耗那 11 次随机数**、并**按记录的倒序建牌**以还原卡牌 id。
   症状很好认：**对手牌组一致、我方牌组"牌一样但顺序不同"**。
3. **静态托管 ≠ 同一版本**：浏览器缓存会让朋友跑旧页面（已两次踩到）。报错文案里已加"请先 Ctrl+F5"；
   联机握手另外比一次**引擎指纹**（本页所有 `<script src>` 源码的哈希），它专门抓"有一方还在跑缓存里的旧页面"。
4. **AI 的随机**：不能让 AI 消耗主逻辑流（同一颗种子必须算出同一局），也不能让它用未播种的 `Math.random`
   （同种子跑两遍不可复现）。现在的做法是"独立种子流 + 阶段③ 临时替换全局 `Math.random`"。
5. **弹窗层级**：`.modal-mask` 统一 `z-index: 50`，**同层时 DOM 靠后的盖住靠前的**——新弹窗要么抬高，要么放对位置
   （`#challengeMask` 58、`#netMask` 与其它主页面弹窗一样靠 `body.in-home …:not(.hidden)` 抬高到 212）。
6. **`Math.random` 被临时替换时**，任何"回退到 `Math.random`"的代码都会变成无限递归。
7. **核对卡组的时机**：必须在两副牌都还是**完整 12 张**时比；晚一步就被抽掉 3 张，永远比不相等。
8. **中止要保留现场**：重放失败时把"本机算出来的"和"码里记的"整份打到控制台——否则无从排查。
9. **镜像座位不是免费的**：两端各自把本地玩家当 `p` 之后，凡按字面座位遍历的地方（取牌 / 掷骰 / 翻牌先后 / 放置队列插序）
   都会把同一颗随机数派给不同的人 ⇒ 必须走 `state.seatOrder`。**漏掉一处就在那一处开始分叉**，
   所以每回合都有一次对账：分叉会在**当回合**被报出来（带回合号与两个指纹），不会闷到终局。
10. **提交包里的 `side` 是"发送方的本地座位"**，不是"包的归属"：`p` ＝ 他自己那一手，
    其它情况由房间按 `(回合, 座位)` 只认第一条 —— 判超时的 `timeout` 消息同理，**两端的结论必须一致**（谁没交包谁输）。
11. **时间线的两处硬约束**：`uiRetreat` 之后主循环要在 `state.phase === 'over'` 时立刻收摊（对手认输可能发生在你正等操作时）；
    加倍要**当场**广播（对手要在本回合内决定撤退），随后的提交包带同一标记、两端幂等。
12. **不做托管就少了两个漂移源**：本机不会再替对手出牌（不跑 AI、不广播别人的包），确定性只依赖"双方各交一手 + 座位序"。
13. **房间只转发、不保存 ⇒ 先到的人发的消息是"发给空气"的**：房主在自己连上时发的那条 `hello`（版本 + 卡组），
    那时房间里还没有第二个人，等加入者进来时它早没了。**必须让先到的人在对手进来时把 `hello` / `ready` 再报一次**
    （`js/net.js` 收到 `peer: online` 时重发）。症状很好认：**房主能开局、加入者卡在"开局信息不全（少种子或卡组）"**。
14. **房间的去重键不能用包里的座位名**：两端各自把本地玩家当 `p`，双方交上来的包 `side` 都是 `p`，
    用 `(回合, side)` 当键会让**两个人的包互相顶掉**。键必须是 `(回合, 发送方角色)`。
    症状：**一方顺利进下一回合，另一方一直卡在"等对手出牌"**。
15. **发卡 id 的顺序也是"按座位遍历"的一处**：`newCard` 用 `state.cardSeq++` 发 id，而 id 进状态指纹。
    地形「出现时」给双方各生成 token（虹龙洞石块 / 黄瓜田河童 / 幽灵洋馆幽灵）时若写死
    `placeToken('p')` 再 `placeToken('a')`，两端就会把**同一批 id 分给不同的人** ⇒ 当回合对账必不一致。
    症状：**揭晓或变身成这三种地形的那一回合报"盘面不一致"**（第 5 回合尤其常见 —— 秘封俱乐部的
    `xformTurn` 定时变形正落在此时，随机变成幽灵洋馆）。生成与遍历一律走 `state.seatOrder`。

## 5. 本轮定下来的口径（用户已拍板）

1. **赌注 / 双倍下注**（单机与联机同一套，AI 永不加倍）：
   结算值 `2 × 2^(本局加倍人数)`（无人 2 / 一人 4 / 两人 8）；双方**各一次**、整局限用；宣布当回合侧栏就显示新值，
   **下一回合开始生效**；从宣布到生效回合结束之前撤退按**加倍前的值**；**末回合宣布**则直接按新值结算。
   挑战码升级为 `TH2P2`（每回合 9 字段：双方各自的"本回合是否宣布加倍"与"是否撤退"），**旧 `TH2P1` 码一律拒绝**。
2. **房间服务放本仓库的 `worker/` 子目录**（跟随版本管理；GitHub Pages 会把它当静态文件一并发布，无害）。
3. **「手动转发提交包」这条退路先不做**：先只做实时房间，`*.workers.dev` 连通性不行再说（阶段 1 的码通道可临时顶替）。
4. **不做超时 AI 托管**：等对手的提交包超过 90 秒就**直接判他认输**（走认输那条路，按撤退价结算），
   本机不替他出牌、不接管他的回合。
5. **暂时不做断线重连**：刷新页面＝离开房间，对面超时后判你认输；`worker/` 因此也不再保存消息日志。

## 6. 阶段 2 的实际落点（代码已就位，验收待用户实测）

- **房间服务** `worker/`：一个房间＝一个 Durable Object（`idFromName(6 位房间码)`），只**转发 + 按 `(回合, 座位)` 去重**，
  不做裁判；`wrangler.toml` 用 SQLite 后端的 DO（免费计划只支持这个）。部署与消息表见 `worker/README.md`。
- **客户端** `js/net.js`（`window.Net`，脚本加载在最后）：连接 / 房间码 / 握手 / 每回合提交包 / 对账 / 超时判负 / 房间界面。
- **界面**：主页面「🌐 联机对战」（`#homeBtnNet`）→ **先选一套出战卡组**（复用 `#battleDeckMask`）→ 房间弹窗 `#netMask`（创建 / 加入 / 我已准备 / 复制房间码 / 退出）；
  对局中顶栏状态条 `#netBar`（房间码 · 角色 · 对手在线 · 本回合阶段 · 倒计时 · 最近一次对账）与侧栏对手信息区（昵称 · 他这一手交了没有）；
  一局打完由结算弹窗的「🔁 再来一局」开下一局（两端各点一次）。
- **验收口径**：两台设备（含手机）用 6 位房间码完成一整局；每回合都要出现「第 N 回合对账一致」；
  一方久不交牌（>90 秒）时，两端都应显示"判他/你超时认输"并按撤退价结算。
- **风险与兜底**：座位序若还有漏改的点，表现就是某一回合对账对不上——**报告里会带回合号与两个指纹**，
  据此回去查那一回合用到的那张牌 / 那块地形对应的 `rng()` 取用点即可。

## 7. 已知的、被有意推迟的事

- **AI 对法术的估值质量问题**（用户观察："AI 好像没正常识别法术、退化成随机挑位"）：**不影响同步**，
  用户已明确"先不查"。要查必须先取得读 `js/ai.js` 的**明确许可**（规则 6）。
- **联机对局不生成挑战码**：码里还要带"谁执哪一边"的座位信息，现有格式装不下；单机对局照常生成（详见 `docs/联机对战.md` §5）。
- **联机没有防作弊**（阶段 3）：两端各算一遍，改内存就能作弊；对账只能发现"算不到一起"，不能证明是谁改的。
- **断线重连 / 掉线托管**：都不做（用户明确）——掉线或太慢一律按超时判负，房间不保存历史消息。
- **「同一局面挑战」不是"各带卡组的对战"**：影子依赖确定性重放，双方卡组与种子必须与原局一致；
  各带卡组的真人对打走实时房间。
