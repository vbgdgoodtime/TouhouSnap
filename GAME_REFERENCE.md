# 东方逆转 · 游戏机制参考手册（GAME REFERENCE）

> 本文档与 **`data/cards.js`（卡牌数据）、`data/locations.js`（区域数据）以及 `game.js`（逻辑/口径）** 保持同步，是当前版本**所有地形效果与卡牌效果**的唯一清单。
> 卡牌美术素材放在 `cards-image/`（规格见该目录 README，缺图自动回退 emoji）。
> 若改卡/改地形，请同步维护本文件。更新记录见文末。

---

## 1. 一局游戏的结构

- **主页面（首页，v120）**：打开游戏先显示**主页面**（`#homeScreen`，`<body class="in-home">` 时全屏显示），而不是直接进入对局。主页面三个入口按钮：
  1. **开始对战（v137）** —— 先弹出**出战卡组选择**弹窗（`#battleDeckMask`）：按卡组栏同序（创建时间倒序）列出**已满 12 张**的卡组，点选一套后点「开始战斗」才隐藏主页面并调用 `Game.restart({ playerDeckDefs })` 开新对局（玩家用该组卡牌洗牌入局，对手仍随机；无满编卡组时提示并引导「卡组设置」）；与顶部「重新开始」相比，后者复用上一局所选卡组；
  2. **卡组设置（v121）** —— 进入**卡组设置页面**（`#deckScreen` + `deck-page.js`，`window.DeckBuilder`，见下条）；
  3. **开发调试** —— 后续页面，当前为占位提示（点击给出「开发中」提示条）。

  引擎侧 `game.js` 底部的启动 `restart()` 保持原样：**游戏在后台完成首局初始化**并停在玩家出牌阶段等待输入（不会自行推进），玩家点「开始对战」时再重开一局。主页面交互在独立文件 `home.js`（暴露 `window.Home`：`show()`/`hide()`/`startBattle()`/`openPage(key)`），页面注册表见该文件的 `PAGES`；任何页面都能用 `Home.show()` 返回主页。
- **卡组设置页面（v121~v131）**：版面为**上方 1/3 卡组栏 + 下方 2/3 卡牌区**（整页纵向 flex，`flex:1` : `flex:2`）。卡组栏有两种形态：
  1. **浏览态（默认）**——**动态列表**：只渲染「＋ 新建卡组」位**与已创建的卡组位**（**没有固定空位**），**每新建一个卡组就动态扩展出一格**（新卡组位播入场动画，横向滚动）；显示顺序为**创建时间倒序（v123）**——「＋ 新建卡组」固定在最左，**越晚创建的卡组越靠左**（新建后紧挨「＋」出现）、**越早创建的越靠右**（`DECKS` 内部仍按创建先后追加存储，仅渲染反序）。容量上限 **20 套**（`DeckBuilder.MAX_DECKS`），每套张数按牌库口径 **12 张**（`DECK_SIZE`）。卡组位显示**卡背叠放缩略 + 名称 + `N/12`**；点「＋ 新建卡组」即追加一套空卡组（默认名「新卡组 N」，并自动选中）；**点卡组位任意位置（含图片）直接进入编辑态（v129，不再需要单独的 ✏️ 按钮）**；满 20 套后新建位置灰并提示。每个卡组位的**缩略图右上角悬浮一个 🗑️ 删除卡组按钮（v124 加入；v129 去掉 ✏️ 修改按钮）**——桌面端悬停 / 键盘聚焦时淡入（触屏无 hover 故常驻显示），隐藏时连 `pointer-events` 一并关闭以免留隐形热区；
  2. **编辑态（v125，点卡组位进入）**——卡组栏换成**该卡组的 12 个卡槽**：已放的显示**卡面**（点它即移出卡组），未放的显示**虚线「幽灵卡」空位 + 序号**；顶部出现「✏️ 改名」「🧹 清空卡组」「💾 保存」三个按钮（改名 v129；原「完成修改」v135 改为保存），标题行只显示**卡组名 + `N/12`**（v130 去掉「正在修改：」前缀，改用金色区分于浏览态的「我的卡组」；满 12 张数字转绿）。**卡组改名（v129）**：点「✏️ 改名」弹出专用小弹窗（`#deckRenameMask`，输入框预填原名并全选，回车 = 确定、Esc / 点遮罩 = 取消），校验**不能为空、最多 12 字、不得与其他卡组重名**（不通过时在弹窗内红字提示且不关闭）；确认后卡组位名称、标题行与悬停提示即时刷新并写入本地。卡槽为**自适应网格（v126）**：宽屏 1 行 12 格；**≤1000px 自动改为 2 行 × 6 格**，手机端（≤700px）**卡槽适当缩小**（不显示效果文字、费用/战力角标与卡名同步缩小）+ 顶部按钮只留图标，保证 **12 个卡槽一屏看全、无需左右滑动**。**空卡槽与已放卡槽尺寸完全一致（v127）**：空位内部做成一模一样的「角标行 / 正方形立绘区 / 卡名行」三段（`.dcs-top`/`.dcs-art`/`.dcs-hint`），且边框宽度相同，因此卡组空或满时卡槽/卡组栏高度都不变（填卡过程中不会跳高）；编辑态**上栏改为按内容自适应高度**（`flex: 0 0 auto`，12 张填满后不再多出空白，省下的高度让给下方卡池；仅极端矮屏以 `max-height: 62%` 兜底、由卡槽区纵向滚动）。此时下方卡牌区：**左键 = 加入卡组**——**已在卡组里的卡牌加灰色遮罩、不可点击（v126，不再显示绿色 ✓ 角标）**，移出一律改走「点上方卡槽」；**右键 = 放大查看**；同名卡**限 1 张**，满 12 张时加入会提示先移出一张。编辑中改动先留在内存；**每次加入/移出一张卡后立即按费用升序→同费用战力升序重排卡槽（v136）**；点「**💾 保存**」（或 Esc / 返回主页，v135）时：经 **`deck-storage.js`** 写入 `localStorage`（键 `touhou2.decks.v1`，卡以卡名序列化）并退出编辑态。启动时自动从本地读回；新建/删除/改名也会即时持久化。「对局使用自建卡组」后续加入。
  3. **卡牌区（`#deckFilter` + `#deckGrid`，v131）**——费用筛选条与游戏图鉴同款（全部 / 0-1 费 / 2~6 费 / 衍生卡牌）；「全部」与费用档展示 `POOL` 人物卡（0~6 费），「衍生卡牌」展示 `SPECIAL` token（不含隙间），**仅可查看、不可加入卡组**（虚线描边）；浏览态左键 / 任何状态右键 = 复用既有 `showZoom()` 放大查看；编辑态对人物卡左键加入卡组。筛选档在打开/关闭页面间保留。
  开局预置两套**占位卡组**（占位卡组 A/B，`cards` 为空 → 显示 `0/12`）用于展示卡组位形态（仅本地无存档时）。**删除卡组**：点 🗑️ → 确认弹窗 → 从 `DECKS` 移除并**收回一格**、刷新计数与「当前卡组」（若删的正是选中/编辑中的卡组则改选最近创建的一套 / 退出编辑态）并写入本地。**确认弹窗**（`#deckConfirmMask`，v125 由「删除专用」通用化为 `openConfirm({emblem,title,lines,okText,danger,onOk})`，删除与清空共用）复用游戏弹窗样式，z-index 50 盖在卡组页之上；文案用 `textContent` 组装避免卡组名注入；「取消」/ 点遮罩空白 / Esc 均不执行。「对局使用自建卡组」后续加入。页面层级 `z-index 45`，低于游戏弹窗（`modal-mask` 50 / `zoom-mask` 60），因此放大查看与确认弹窗都正常盖在页面上；进入本页与主页面一样由 `body.in-deck` / `body.in-home` 隐藏游戏区，页面内「← 返回主页」→ `Home.show()`（离开时若仍在编辑态会先保存）。
- 共 **6 回合**。第 1 回合能量为 1，之后每回合 +1，上限 6（即第 6 回合能量 6）。
- 能量是**当回合预算**：一回合内可以暗出**任意多张牌**，只要这些牌的费用总和 ≤ 当回合能量；用不完的能量不会累积到下一回合。
- 出牌流程：玩家不断「选手牌 → 点区域」暗出（牌背显示 ?），点「**结束回合**」把回合交给对手；AI 会贪婪地把能量花完。
- **翻牌先后（先手）规则**：
  - **第 1 回合随机**决定谁先翻开；
  - 第 2 回合起按“**结算胜利标准**”判当前领先方（只统计已翻开的牌，判据见 §6）：当前领先（赢下区域更多，或存在平局区域时三区总点数更大）的一方**先翻开**；
  - 若按该标准双方打平（如三区皆平且总点数相同），则**继续随机**决定先后。
  - **同一方内部的多张牌严格按出牌（放置）先后翻**，与谁先无关。
- 每张暗牌**翻面后才把战力计入区域总点数**（含全部修正），总点数随翻面实时上升；终局时所有牌均已翻开，口径一致。
- **地形揭晓（v74）**：开局三块区域一律先以「未揭示」地形展示（❓，无特殊效果、max 4、中间带文案「未揭示地形」；外观用专属“随机未知”主题——淡紫灰底 + 随机散布的地形小图标，v75）；三块**真实地形**已在开局按 `pick` 权重抽定（互不相同）并预存，**第 1 / 2 / 3 回合开始时依次揭晓到左 / 中 / 右列**——揭晓瞬间换上真实地形（上限 / 加成 / 反转 / 回合末摧毁等全部字段与列头配色即刻生效），并结算该地形的「出现时」一次性效果（如虹龙洞在揭晓时给双方各生成 1 张石块）。未揭示地形**可正常放牌**（每侧上限按 4 计），列下方提示「🃏 第 N 回合揭晓」；地形效果自揭晓起作用——此前已放下的牌在揭晓后按现盘面即时受其影响（不追溯撤销、不加不删）。揭晓时播放 **500ms 换场演出**（v98→v99：未揭示外观淡出、真实地形显形）。
- **限张地形揭晓超限口径（v74）**：若揭晓成上限 <4 的地形（现仅迷途竹林 max 2）而某侧此前在未揭示期已放了**超过上限**的卡（只有第三列可能：前两回合每侧至多放 3 张）——**不摧毁、不移走任何卡**，已放卡保留原格位；隙间只铺在“空置的不可用格位”，数量 = 4 − max(已放张数, 上限)（放 3 张 → 1 个隙间；放满 4 张或整侧被大体积卡占满 → 0 个），此后该侧视为已满、不可再放。
- 每块区域双方最多放 4 张（特殊区域可能更少，超限口径见上条），第 6 回合翻面结束后立即终局结算。
- **重置暗牌（撤回）**：出牌阶段内（本回合暗牌尚未翻面时）点击顶部「能量」框，确认后将本回合已暗出的牌**按放置前的手牌顺序放回**并全额返还能量；只影响玩家自己的暗牌，不改动对手已暗出的牌。确认弹窗可按「取消」放弃，点遮罩空白或 Esc 同样关闭。
- **场地展示口径**：每个玩家区域固定为 2×2 四个格位，按「左上 → 右上 → 左下 → 右下」依次落牌，第一张牌永远占最左上格，不会因后续放牌而移动；卡牌放大铺满各自格位。**所有区域（无论允许放几张）敌我两侧都会用浅灰网格线画出 2×2 格子**（max=4 时空格位显示浅灰虚线框）；**允许数量 <4 的区域**，不允许放卡的格子（右下往后的格位）会铺灰色「隙间」占位卡——隙间由渲染层铺在**空置的不可用格位**（开局三列先以 max 4 的「未揭示」展示故无隙间，揭晓成限张地形后自动出现；已放卡超上限时数量自动减少，见 §1 超限口径），且不可点击/不可操作。桌面卡缩略图上：**左上角为白色费用**，**右上角为当前战力**——与基础威力相同显示白色，更高显示绿色，更低显示红色。每个区域按「对手区 → 对手总点数 → 区域效果 → 己方总点数 → 己方区」纵向排布，两个总点数横幅显示该区**当前受全部效果修正后的最终战力**（与终局结算同口径，含区域倍率），并随谁领先**动态着色：领先方亮黄色、另一方/平点为蓝色**。
- **场上卡牌检视**：已经翻开的敌我卡牌可以像图鉴一样**点击放大查看**：右上角威力为该牌受修正后的**当前场上威力**，弹窗内只保留费用/基础威力/效果说明（不再堆修正明细）；同遮罩内、**详情弹窗外部**并排弹出独立的 **「战力影响历史」** 面板（v56），逐条列出该卡当前全部战力影响来源 —— 基础战力 → 永久 buff 台账（按施加先后，来源显示为施加方卡牌，自身效果显示"卡牌效果"）→ 持续效果（分来源、实时，源卡离场即消失）→ 区域加成（阵营/费用/全区，实时）；**提高 +N 绿色、降低 −N 红色**，合计战力与场上显示一致；暗牌不可点击。

---

## 2. 牌库与抽牌顺序

**玩家**：出战使用自建满编卡组（12 张具体卡已定）；开局把这 12 张**纯随机洗牌**后入牌库（v139：已取消起手友好拒绝采样）。无自建卡组时的兜底随机曲线为 `1×3, 2×3, 3×2, 4×1, 5×1, 6×2`（各费从卡池随机抽、同费用不重复，抽牌顺序同样纯随机）。

**对手（AI，v138）**：玩家进入对局瞬间，按费用结构 `1×2, 2×3, 3×3, 4×1, 5×1, 6×2` 从人物卡池**每种费用随机抽**（同费用不重复）组成 12 张，再**纯随机洗牌**。

每回合（**含第 1 回合**，v94 起）开始时双方各从牌库抽 1 张；开局另有 3 张在开场逐张发放（附入场动画）：即玩家首回合手牌共 **4 张**，随后每回合再各抓 1 张。

---

## 3. 区域（地形）效果

每局**开局即抽定**三块真实地形：按区域 `pick` 权重（默认 1）不放回抽 3 块，**三块互不相同**（池中种类 ≥3 时一定不重复；种类不足 3 时退回允许重复的旧逻辑兜底），抽定后按揭晓顺序预存，随回合逐列揭晓（见 §1「地形揭晓」）。辉针城 `pick: 0.28` → 每局出现率约 10%（约每 10 局 1 次）。
「限制类」效果在出牌时即时生效；「限回合」类在出牌阶段按回合数即时判定；「出现时」类效果在**该地形被揭晓时**结算一次（不再在开局统一结算）；「终局类」效果在第 6 回合翻面结束后、判定胜负前结算；「阵营加成」「费用加成」「放满加成」类为**常驻实时**效果（见下表附注）。

> 「未揭示」（❓，id: `unreveal`）不是随机池里的可玩地形——数据放在 `data/locations.js` 的 `EXTRA` 表（非 POOL，**绝不进入随机抽选**）；它是**每局开局三块地形的初始未揭示态**（v74）：上限/规则与「无名之丘」同款（max 4、无特殊效果），仅名称、emoji 与中间带文案不同（「未揭示地形」），外观用专属“随机未知”主题视觉（v75，随机散布地形小图标）区别于普通地形。未揭示列可正常放牌，随后由地形揭晓系统逐回合揭晓为真实地形。

| 图标 | 名称 | 类型 | 效果 | 结算时机 |
| --- | --- | --- | --- | --- |
| ⛰️ | 无名之丘 | 普通 | 无特殊效果 | — |
| ❓ | 未揭示 | 普通（不入随机池） | 未揭示地形（无特殊效果；样式同无名之丘、上限 4） | — |
| 🏰 | 红魔馆 | 阵营加成 | 位于此区域的「红魔馆」角色（红美铃/十六夜咲夜/帕秋莉/芙兰朵露；小恶魔待实装）威力 **+2** | 常驻（落区即生效、实时） |
| 🎋 | 迷途竹林 | 限制 | 双方在此区域**最多各放 2 张**牌 | 出牌时即时 |
| 🕳️ | 虹龙洞 | 生成 | 区域出现时：**双方各获得 1 张「石块」**（见 §5 特殊卡牌） | 地形揭晓时（该地形出现时，v74） |
| 🐉 | 龙神像 | 放满加成 | **某一方在本区放满 4 张时，该方额外 +5 战力**（敌我各自独立判定） | 常驻（放满判定、实时） |
| 🌅 | 雾之湖 | 费用加成 | 此区域**所有 1 费卡牌**威力 **+2**（双方、特殊卡同享） | 常驻（落区即生效、实时） |
| 🏯 | 辉针城 | 反转 | 本区域**总战力更低的一方获胜**（敌我通用） | 常驻（比较口径反转） |
| 🌠 | 七夕坂 | 限回合 | 双方第 **5 回合起**才可在此区域放牌（此前锁定） | 出牌阶段即时 |
| 🪦 | 冥界 | 全区修正 | 此区域**所有卡牌威力 −2**（双方、特殊卡同享） | 常驻（落区即生效、实时） |
| 🔥 | 聚变反应炉 | 回合结束 | **每回合翻牌结算后**，摧毁本区域全场战力最低的卡牌（敌我混比，并列最低一并摧毁） | 每回合结束（含第 6 回合，结算前） |

阵营加成规则口径（红魔馆示例，引擎字段 `aff`，可拓展给更多区域复用）：

- 卡牌数据里用 `g` 字段标注所属阵营；区域用 `aff: { group, add }` 声明给哪个阵营加多少威力。
- 加成**双方通用**：任何一方把该阵营角色放进此区域都生效（红魔馆角色不分敌我）。
- 属于**常驻实时**威力：会显示在该卡的右上角战力/修正角标与放大视图中，计入区域总点数、`dw` 摧毁的“最低威力”判定、`bl` 落后判定等所有实时读取；暗牌阶段不显示也不计入（与普通威力口径一致，翻面后才可见可计）。
- 未来新增同机制区域：只需在卡上加 `g`、在区域上加 `aff`；新增“红魔馆”以外的阵营时在 `cards.js` 的 `GROUPS` 补一行中文名。

放满加成规则口径（龙神像示例，引擎字段 `fill`，可拓展给更多区域复用）：

- 双方**各自独立**判定：一方在本区**实际放满 4 张**（含暗牌、含地形生成的牌）即该方 +5；双方同时放满则各 +5。
- 属于**常驻实时**：计入总点数横幅、领先判定、`bl` 落后判定等；**一旦因摧毁（或本回合撤回暗牌）导致该方不满 4 张，加成立即失效**，4→3 不再加成。
- AI 落子预判会把“这一手正好放满”的 +5 计入估值。

费用加成规则口径（雾之湖示例，引擎字段 `cb`，可拓展给更多区域复用）：

- 判定条件：卡牌费用**恰为** `cb.c`（例 1 费）。双方卡牌都算，费用为 1 的特殊卡（如「石块」）同样享受。
- 属于**常驻实时**威力：与阵营加成一致——显示在卡面右上角/修正角标与放大视图，计入区域总点数、`dw` 摧毁“最低威力”与 `bl` 落后判定；暗牌阶段不显示不计入（翻面口径一致）。

反转区域规则口径（辉针城示例，引擎字段 `inv: true`）：

- 该区域按“**总战力低者胜**”判定：区域横幅仍显示双方**真实点数**，但领先亮黄、区域胜负、翻牌先后领先判定均按低者胜的口径；平点仍记为平局区域。
- “三区总点数”决胜时，反转区域以**负值**计入总点数（弹窗会注明）。
- `bl` 落后自增在此按“是否落后于该区的获胜标准”触发（辉针城内“落后”= 真实点数反而更高的一方）；`dw` 等卡牌效果不变。
- AI 估值已按反转口径处理（会把低战力视为领先）。

限回合开放规则口径（七夕坂示例，引擎字段 `minTurn`）：

- 锁定期间（第 1~4 回合）**双方都不能**在此区域放牌：玩家点击会提示"还没开放"，AI 也不会往该区落子，悬停可放高亮不出现；区域仍显示"已放 0/4 · 🔒 第 5 回合开放"。
- 第 5 回合起恢复正常放牌；该字段只影响"何时能放"，不影响该区其它机制（可叠加 max/加成等）。
- 区域没有牌时点数横幅照常为 0、不影响领先判定（双 0 平点）。

全区修正规则口径（冥界示例，引擎字段 `all`，可拓展给更多区域复用）：

- 判定条件：**无**——本区域所有卡牌（双方、特殊卡）一律加减 N；N 可为负（如 -2），也可为正。
- 属于**常驻实时**威力：与阵营/费用加成同口径——显示在卡面右上角/修正角标与放大视图（标注"区域效果：本区域所有卡牌 威力 -2"），计入区域总点数、`dw` 摧毁“最低威力”与 `bl` 落后判定；暗牌阶段不显示不计入（翻面口径一致）。
- 与其它区域加成可叠加（如某卡同时吃 -2 与费用/阵营加成、再吃 bf/de 等永久增益）。

回合结束摧毁规则口径（聚变反应炉示例，引擎字段 `purge`）：

- 时机：**每回合翻牌结算结束后**立即执行一次（跳过/无牌回合也会执行），第 6 回合翻面后同样先执行再终局结算。
- 判定：把本区域**敌我双方所有已翻开的卡**放在一起比较战力（含区域加成/增益等实时威力），“全场最低”的那张被摧毁；**并列最低的多张一并摧毁**。
- 摧毁后此前的增益不回收；区域总点数/横幅随即下降；若该侧放满的龙神像式加成因此失效按既有规则处理（4→3 不再 +5）。
- AI 不会专门针对该区调整出牌策略（当前 AI 仅按分值落子），后续可再优化。

> ⚠️ **版本说明**：早期版本曾有过「圣地之巅」（赢得该区域按 2 个区域计，权重 ×2），因过于影响平衡已移除；当前不存在任何“计区加权”地形。

---

## 4. 卡牌效果体系

所有卡牌效果均为「**揭示**」（On-Reveal）：**翻面时结算一次**，结算完毕后永久生效，之后不再触发。

| 效果键 | 名称 | 规则 |
| --- | --- | --- |
| — | 白板 | 无效果，纯靠身材 |
| `bf` | 友军增益 | 同区域**你的其他已翻开**卡牌（不含自己）威力 +N |
| `de` | 敌方削弱 | 同区域**对方所有已翻开**卡牌威力 −N |
| `ba` | 双方同增 | 同区域**双方所有已翻开**卡牌威力 +N |
| `bl` | 落后自增 | 结算时若**你在该区域总威力落后**（严格小于对方，已计入自己这张牌），此卡威力 +N |
| `dw` | 摧毁 | 摧毁同区域**对方已翻开且威力最低的一张**卡牌（**暗牌不可选为目标**；平局取第一张最低者）；该区域没有已翻开的对方卡牌则无事发生 |
| `spawn` | 添加石块 | 揭示：向本区域**双方**各添加 1 张特殊卡（当前为「石块」）；该侧放满则不放，落地翻开、占用格位 |
| `spawnO` | 敌方侧生成 | 揭示：向本区域**对方一侧**添加 1 张特殊卡（当前为「厄运」）；对方该侧放满则不放，落地翻开、占用对方格位 |
| `clone` | 另两区生成分身 | 揭示：向**另外两个区域**（不含本区）**自己一侧**各添加 1 张特殊卡（当前为「赫卡提亚的分身」）；该侧放满或未开放则跳过该区，两区都不可放则失败；分身战力按本卡揭示时快照 |
| `switch` | 换边 | 揭示：翻开后从自己一侧转移到**对方该区域一侧**（当前为「依神紫苑」）；对方该侧放满则换边失败、留在原地 |
| `morph` | 变身 | 揭示：把自己变成**对方手牌中随机一张卡的复制体**（当前为「二岩猯藏」；原卡留在对方手牌，变身后按新卡的 k 重新结算一次其揭示文本） |
| `gift` | 送己方最低卡 | 揭示：把本区域**己方战力最低的已翻开卡**（不含自己；并列最低随机选一）换边到对方该区域一侧（当前为「因幡帝」；对方该区放满则失败） |
| `xform` | 区域变形 | 揭示：把本区域整体替换为目标地形（`xf` 指向地形 id，如「辉针城」） |
| `mv` | 移走 | 揭示：把本区**对方已翻开且实时战力最低**的一张卡移到另外两个区域随机一处（对方该区放满或未开放的地形排除；两个都不行则失败） |
| `give` | 加入手牌 | 揭示：把 1 张指定特殊卡（手牌衍生物，如「废弃列车」）加入**自己手牌**；手牌已满 7 张则加入失败（现无卡注册，v76 起预留） |
| `dwh` | 摧毁最强 | 揭示：摧毁本区**对方已翻开且战力最高**的一张卡（平局取第一张最高者）；无已翻开的对方卡则无事发生（现仅预留 token「废弃列车」自带，暂无生成来源） |
| `oc` | 对方同区落牌自增 | 揭示：若**翻开当回合**对方曾在本区域放置过至少一张牌（含后来被摧毁的），此牌永久 +N |
| `shift` | 整体右移 | 揭示：把己方**最左侧区域**所有**已翻开**的卡牌按放置顺序依次搬到**最右侧区域**己方一侧，直到右侧己方侧填满为止（占格判定；现仅八云紫） |

补充规则：

- 增益/削弱会**永久叠加**到每张被影响的牌上，并实时反映在区域总战力与牌面战力数字中（正增益绿色、被削弱红色）。**v80→v84**：场上卡牌发生**永久 ±N 战力**变化（正向：`bf/ba/bl/oc` 揭示增减、`phx` 重生成长；负向：`de` 敌方削弱、`surv` 防摧毁降战力等，统一收口 `applyPermBuff`）时，播放约 **1000ms 演出**——正向**绿色「+N」**、负向**红色「-N」**（v84）：卡面短暂提亮/泛红，光环与数字气泡置于 body 独立悬浮层（v83 起不随卡面重建中断），末段约 300ms 渐隐，动画在效果结算后的渲染时统一触发。
- **增减只作用于"结算那一刻已翻开"的卡牌**：先结算的 `bf/de/ba` 不会预留给还没翻开的暗牌——后翻开的牌会错过这次结算（不吃 +N 也不吃 −N）；想吃到增益/避开削弱，就让该牌在效果结算前先被翻开（翻牌顺序 = 当回合放置顺序，见 §1）。
- 摧毁会把目标卡移出区域，后续不再计入总战力；但之前已施加在其他牌上的增益**不回收**。**v88 演出**：真正被摧毁的卡会播放约 1s 的**分崩离析**动画（卡面切成 3×2 六块碎片向四周迸散、旋转并渐隐，节奏同 ±N 气泡）；仅实际离场移除的摧毁（`dw`/`dwh`/反应炉 purge）触发——防摧毁 `surv`、凤凰重生 `phx`、蕾蒂免摧毁等卡未真正被移除时不触发；移动（`mv`/`fly`/换边）不触发。
- `dw` 摧毁**只可指定已翻开的对方卡牌**：暗牌不会被提前摧毁，也就不存在"被毁的暗牌不翻牌"的情况（v25 起）。
- 区域效果按类型区分口径：**倍率/终局类**只乘进**结算用的总战力**，不改卡牌自身数字；**阵营加成型**则算作卡牌的实时威力（见 §3 附注），会反映在卡面数字上。
- **持续效果（非揭示；原「在场光环」）**：卡牌翻开后**持续存在**的效果（非一次性触发，与「揭示」互补）——带 `og` 的卡（现仅「比那名居天子」）只要**已翻开且仍在己方场上**，己方所有带对应 `tk` 标记的卡（石块）威力常驻 +N，按己方全场计算、不受目标所在区域/回合限制；实时动态判定——该卡一旦被摧毁/离场，效果立即消失，已加成数值不残留。
- **防摧毁（`surv`，卡级防护，现仅灵乌路空 `surv:2`）**：带 `surv: N` 的卡被任何“摧毁”指向时（`dw`/`dwh`/区域回合末 `purge` 等）**不会离场**，取而代之**永久降低 N 点战力**（每次触发再降 N、可多次）；替代时卡不离场、仍在放置顺序队列中，其战力降低会记入战力影响历史；若在聚变反应炉中反复成为全场最低会被反复降低。与 `un`（绝对不可动）不同——只是“摧毁被替换”，其余规则照常。
- **凤凰重生（`phx`，卡级机制，现仅藤原妹红 `phx:2`）**：带 `phx: N` 的卡被任何“摧毁”指向时（`dw`/`dwh`/区域回合末 `purge` 等）**不会消失**——从场上移除后**返回自己手牌**并**永久 +N 战力**（每次触发 +N、可重复“摧毁→重生→再打出”并持续成长）；重生时移出放置顺序队列，再次打出时重新暗出/翻面入队；**手牌已满 7 张则重生失败、按原样被摧毁**。与防摧毁 `surv` 的区别：`surv` 留场降战力、`phx` 离场回手并成长。
- **区域免摧毁（`prot`，卡级持续机制，现仅蕾蒂 `prot: true`）**：带 `prot` 的卡翻开后、只要仍在某区域场上，该区域（**双方**）所有在场卡牌均**无法被摧毁**——任何指向本区域的“摧毁”（`dw`/`dwh`/区域回合末 `purge` 等）一律失效：不选择目标、不结算防摧毁 `surv`、不触发凤凰重生 `phx`；保护含双方已翻开的人物卡与落场特殊卡（token，如石块/厄运/分身），也保护该 `prot` 源卡自身；被保护卡只是“不会被摧毁”，其余效果（增减/移动/换边/移走等）照常。**防护按源卡所在区域实时判定**：蕾蒂被移去其它区域 → 原区域立即失效、新区域立即生效；同区换边（switch/gift 类把她在本区换到对方一侧）仍在同一区域，因保护的是敌我双方故效果不变；源卡真正离场才全场失效。
- **终局离场（`leave`，现仅稗田阿求）**：带 `leave: true` 的卡在**游戏结束时**（第 6 回合全部阶段后、结算胜负前）**从场上消失**——它不是“摧毁”，**不触发任何摧毁相关机制**（防摧毁 `surv`/凤凰重生 `phx`/摧毁类文本均不会触发），永久 buff 随卡一并离开、**不计入终局结算**；`leave` 卡若在场，游戏结束离场后即从各区域与放置队列移除（该区放满等实时判定随之刷新）。
- **大体积占格（`occ`，现仅伊吹萃香 `occ:4`）**：带 `occ:N` 的卡单卡占据 N 个格位（普通卡为 1）。`occ:4` 占满区域全部 4 格：**只能放进最大可放数为 4 且己方该区没有任何卡**的区域；放置/在场期间该侧视为已满（其它落牌、spawn/移入、放满加成 fill 按占格口径判定）；卡面放大覆盖整侧 2×2；被摧毁、移走、换边、撤回或离场后自动腾出格位（不属于额外 3 个实体，摧毁即整卡移除）。**换边/移动交互**：换边牌（`switch`/`gift`）落到对方侧要求对方侧空余 ≥ 移入卡占格数——对方侧被萃香占满（空余 0）时无法换边；`mv` 等把卡移往另一侧同理要求目标侧空余 ≥ 目标卡占格数，因此**目标侧已有任意卡时萃香不会被移过去**（她只能移进该侧完全空出且上限足够的区域）。
- **时机效果（非揭示，v55）**：卡牌 def 可带 `fx: { turnStart?, turnEnd?, gameEnd? }`，在对应全局时机结算一次（每回合的回合开始/回合结束、以及第 6 回合的游戏结束）；条目与揭示规格同构（`k/a/spawn/xf/give/t`），**复用揭示结算逻辑**。结算顺序 = **场上放置顺序队列**：双方卡牌放入场上的先后（谁先放谁先结算，跨敌我混排）。各时机位置：回合开始效果**先于能量结算与抽牌**；回合结束效果在**翻牌结算后、区域回合末（purge）前**；游戏结束效果在**第 6 回合全部阶段后、结算胜负前**（对盘面的修改会进入终局结算）。手牌打出/落场特殊卡入队时记录顺序，被摧毁或重置撤回手牌的卡移出队列（不再触发后续时机）；移动（`mv`/`fly`）不变序。当前无卡注册 `fx`。
- **区域变形（`xform`，现仅 鬼人正邪 → 辉针城、灵乌路空 → 聚变反应炉）**：揭示把该区域的 `def` 整体换成目标地形——列名/图标/效果文字/配色、上限 `max`、反转等口径立即生效；若原为限张地形（如迷途竹林），隙间格位随上限变 4 自动消失、两侧都可继续放牌；若任一方现卡数超过目标上限则变形失败；目标地形的一次性“出现时”效果**不会**重触发（如变形为虹龙洞不会再次生成石块），`purge`/`minTurn` 等常驻类按新地形从变形起生效。
- **移走（`mv`，现仅红美铃）**：揭示把本区**对方已翻开**的实时战力最低卡移走——只在“另外两个区域”里选；对方某侧放满/该区未开放（如七夕坂锁定中）的地形不会被选中，两处都不可达则移动失败、卡留在原位；移动后涉及区域的总战力/加成/放满等全部按现盘面重算（移动后自动刷新，无残留）。
- **敌方侧生成（`spawnO`，现仅键山雏 → 厄运）**：揭示向本区域**对方一侧**放 1 张落场型特殊卡「厄运」（3 费 / −3 战力，落地翻开、占对方格位，−3 计入对方该区总点数）；对方该区已放满则放置失败、不做替代处理。
- **分身生成（`clone`，现仅赫卡提亚 → 赫卡提亚的分身）**：揭示向**另外两个区域**（不含本区）**自己一侧**各放 1 张分身（该侧放满或未开放则跳过该区，两区都不可放则失败）；分身落地即翻开、占格位、进入放置顺序队列，**战力按本体揭示时 `cardPower`（基础 + 永久 buff）快照固定**（差值记在分身 buff 上，本体之后的增减不影响分身），分身自身随后可被增益/削弱独立修改；分身无其他效果。
- **换边（`switch`，现仅依神紫苑）**：揭示后**立即把自己从一侧转移到对方该区域一侧**（任何卡都在自己一侧翻开，故统一表现为“翻开后换到对方该区”）；转移后卡牌归属对方（`card.side` 更新），作为对方已翻开卡参与结算——依神紫苑自身 −7 战力随之计入对方该区并占对方一个格位，场上放置顺序不变；对方该区已放满则**换边失败、留在自己一侧**（−7 继续计入己方）。
- **变身（`morph`，现仅二岩猯藏）**：揭示时从**对方手牌随机取一张**，把自身整体替换为该卡的**复制体**（原卡留在对方手牌；名称/费用/威力/效果文本/配图全部变为目标卡），并**重新触发新卡的效果文本**：新卡若带 `k`（揭示）立即按其再结算一次（若新卡同为变身卡则不二次变形，防死循环）；持续 `og`/每回合移动 `fly`/时机效果 `fx`/防摧毁 `surv` 等由新 def 实时驱动自动生效。对方手牌为空则变身失败、保持原样。**大体积目标限制**：随机目标若是占多格的大体积卡（如萃香 occ:4），仅当**本区域 max 恰为其占格数**且**己方该区（明牌+暗牌合计）有且仅有变身者这一张卡**时才可变身；否则变身失败、保持原样。
- **送己方最低卡（`gift`，现仅因幡帝）**：揭示时把本区域**己方战力最低的已翻开卡**（不含自己；多张并列最低随机选一）**换边**到对方该区域一侧——目标占对方格位、归属变对方，其持续效果（如天子 og）按**新归属方**生效；对方该区已放满则换边失败，本区没有其他已翻开的己方卡则无事发生。
- **整体右移（`shift`，现仅八云紫）**：揭示时把己方**最左侧区域**（第 1 列）**已翻开**的卡牌**按放置顺序依次**搬到**最右侧区域**（第 3 列）己方一侧，直到右侧己方侧**填满**为止（按 `occ` 占格判定：右侧空余 ≥ 该卡占格数才搬，右侧放满即停止；只搬已翻开的卡，暗牌不动）。搬移不改变归属、揭示状态与场上放置顺序队列，移动后各区域总点数/放满加成实时重算；若八云紫自身就在最左侧区域也会被一起搬过去。**揭示演出（v78→v79）**：正常翻牌结算时**逐张搬运**——每张卡以**滑行 + 缩放**的飞行演出落到右侧格位（约 0.26s，带轻微弧线与落地回弹；`flyCardTo` 用 WAAPI 克隆卡做位移动画），卡片间隔保持约 **0.3 秒**；morph/fx 等其它结算路径为一次性同步搬运（规则口径不变）。

---

## 5. 全部卡牌（35 张，东方 Project 主题换皮）

> 数值与效果文本完全沿用演示原型，仅更换名称与图标。
> 阵营标注：红美铃 / 十六夜咲夜 / 帕秋莉 / 芙兰朵露 已标 `g:'scarlet'`（红魔馆），会被「红魔馆」区域 +2；旧版"小妖怪"占位并非红魔馆成员、蕾米莉亚→中妖怪 已更名并**脱离该阵营**；**小恶魔（红魔馆）未实装**，先占位，日后加入时打上同一阵营标记即自动生效。

### 0 费

| 卡牌 | 威力 | 效果类型 | 效果文本 |
| --- | --- | --- | --- |
| 📜 稗田阿求 | 6 | 终局离场（leave） | 游戏结束时：此牌从场上消失（不属于被摧毁，增益随之一同消失） |

> ⚠️ 0 费组**暂不入牌库洗牌**（牌库固定 1~6 费曲线），仅图鉴/开发者指定可查看与测试。

### 1 费

| 卡牌 | 威力 | 效果类型 | 效果文本 |
| --- | --- | --- | --- |
| 🌟 小妖精 | 2 | 白板 | 无 |
| ❄️ 琪露诺 | 1 | 友军增益 +1 | 揭示：同区你的其他已翻开卡牌 +1 |
| 🐛 莉格露 | 1 | 敌方削弱 −1 | 揭示：同区对方所有已翻开卡牌 −1 |
| 🧚 大妖精 | 1 | 双方同增 +1 | 揭示：同区双方所有已翻开卡牌 +1 |
| 🔥 藤原妹红 | 1 | 凤凰重生（phx） | 每次此卡被摧毁时：返回手牌，并且战力 +2（可多次触发；手牌满则重生失败、被摧毁） |

### 2 费

| 卡牌 | 威力 | 效果类型 | 效果文本 |
| --- | --- | --- | --- |
| 🥋 红美铃 | 3 | 移走（mv） | 揭示：将此区域对方战力最低的已翻开卡牌移到另外两个区域随机一处（满员区排除；都满则失败） |
| 👺 小妖怪 | 3 | 白板 | 无 |
| ⏱️ 十六夜咲夜 | 2 | 落后自增 +2 | 揭示：若你在此区落后，此卡 +2 |
| 🎵 米斯蒂娅 | 1 | 敌方削弱 −2 | 揭示：同区对方所有已翻开卡牌 −2 |
| 📖 帕秋莉·诺蕾姬 | 2 | 友军增益 +2 | 揭示：同区你的其他已翻开卡牌 +2 |
| 🗡️ 魂魄妖梦 | 1 | 对方落牌自增 +4 | 揭示：若翻开当回合，对方曾在本区域放置至少一张牌，则此牌战力 +4 |
| 🐇 因幡帝 | 3 | 送己方最低卡（gift） | 揭示：将此区域己方战力最低的已翻开卡牌换边到对方一侧（不含自己；多张并列最低则随机选一张） |
| 🌨️ 蕾蒂 | 3 | 持续免摧毁（prot） | 持续：此区域双方在场的卡牌均无法被摧毁 |

### 3 费

| 卡牌 | 威力 | 效果类型 | 效果文本 |
| --- | --- | --- | --- |
| 💫 中妖精 | 4 | 白板 | 无 |
| 🎆 雾雨魔理沙 | 3 | 摧毁 | 揭示：摧毁同区对方已翻开的威力最低卡牌 |
| 🎎 键山雏 | 1 | 敌方侧生成（spawnO） | 揭示：在本区域对方一侧添加 1 张「厄运」（占对方格位、−3 计入对方总点数） |
| ☠️ 梅蒂欣·梅兰可莉 | 3 | 敌方削弱 −2 | 揭示：同区对方所有已翻开卡牌 −2 |
| 🗿 比那名居天子 | 2 | spawn + 持续效果 | 揭示：本区域双方各获得 1 张「石块」；此牌在场时，己方所有「石块」战力 +2 |
| ☀️ 灵乌路空 | 6 | 区域变形（xform）+ 防摧毁 | 揭示：将此区域变成「聚变反应炉」。此牌被摧毁时：不会被摧毁，取而代之永久降低 2 点战力 |
| 🦝 二岩猯藏 | 0 | 变身（morph） | 揭示：此牌变成对方手牌中随机一张卡的复制体（变身后重新触发该卡的效果文本，如揭示、持续等；对方手牌为空则不变化） |

### 4 费

| 卡牌 | 威力 | 效果类型 | 效果文本 |
| --- | --- | --- | --- |
| 👹 中妖怪 | 6 | 白板 | 无 |
| 🦋 西行寺幽幽子 | 4 | 敌方削弱 −3 | 揭示：同区对方所有已翻开卡牌 −3 |
| 🌕 芙兰朵露·斯卡蕾特 | 3 | 摧毁 | 揭示：摧毁同区对方已翻开的威力最低卡牌 |

### 5 费

| 卡牌 | 威力 | 效果类型 | 效果文本 |
| --- | --- | --- | --- |
| 📉 依神紫苑 | −7 | 换边（switch） | 揭示：此牌换边——翻开后从自己一侧转移到对方该区域一侧（对方该区已放满则换边失败，留在原地） |
| 🍶 伊吹萃香 | 12 | 大体积占格（occ 4） | 此牌占据本区域全部 4 个位置（只能放入上限为 4 且己方该区没有任何卡的区域；被摧毁/离开后腾出 4 个空位） |
| 🐲 大妖怪 | 8 | 白板 | 无 |
| 🧪 八意永琳 | 5 | 摧毁 | 揭示：摧毁同区对方已翻开的威力最低卡牌 |
| 🍃 射命丸文 | 7 | 每回合移动一次 | 出牌阶段可把此牌移动到另一区域一次（已翻开后才可移动） |

### 6 费

| 卡牌 | 威力 | 效果类型 | 效果文本 |
| --- | --- | --- | --- |
| 🌻 风见幽香 | 12 | 白板 | 无 |
| 🧙 贤者 | 12 | 白板 | 无 |
| 🐸 洩矢诹访子 | 8 | 落后自增 +6 | 揭示：若你在此区落后，此卡 +6 |
| 🪞 鬼人正邪 | 10 | 区域变形 | 揭示：将此区域变为「辉针城」（战力低者胜；原限张区域的隙间消失、上限变 4） |
| 💠 赫卡提亚 | 5 | 分身生成（clone） | 揭示：若可能，向另外两个区域各添加 1 张「赫卡提亚的分身」（战力=此牌揭示时）。分身无其他效果 |
| ✨ 八云紫 | 10 | 整体右移（shift） | 揭示：将你最左侧区域所有已翻开的卡牌依次移动到最右侧区域，直至最右侧区域填满为止 |

> 牌面数值平衡基准：白板大致遵循「费用 × 2」线（1/2、2/3、3/4、4/6、5/8、6/10/12）；带功能（尤其 `dw` 摧毁）的卡在身材上让渡 1~3 点威力作为代价。

### 特殊卡牌（token，不入牌库/图鉴）

特殊卡牌不属于上方人物池：**不洗入牌库、不出现在手牌与图鉴**，不参与回合翻牌流程、不可被玩家选中操作。分两类：**生成型**（由地形/效果按键名生成，见 `data/cards.js` 的 `SPECIAL`）与**场地占位型**（随场地固定生成、纯静态）。

| 卡牌 | 费用 | 威力 | 类型 | 效果 | 生成来源 |
| --- | --- | --- | --- | --- | --- |
| 🪨 石块 | 1 | 0 | 生成型 | 白板占位（土黄色卡面） | 地形「虹龙洞」出现时双方各 1 张；人物「比那名居天子」翻开时双方各 1 张 |
| 🌀 厄运 | 3 | −3 | 生成型（对方侧） | 白板占位（深紫卡面） | 人物「键山雏」翻开时放到本区域**对方一侧** 1 张 |
| 💠 赫卡提亚的分身 | 6 | 5 | 生成型（另两区·自己一侧） | 白板占位（与本体同立绘；快照本体战力） | 人物「赫卡提亚」揭示时放到**另外两个区域**自己一侧各 1 张 |
| 🚂 废弃列车 | 3 | 3 | 手牌衍生物 | 揭示：摧毁此区域敌方一张**已翻开的战力最高**的卡牌 | 预留 token：原「八云紫」（v76 前）翻开时加入手牌；八云紫重做后无生成来源 |
| ≋ 隙间 | 0 | 0 | 场地占位型 | 绝对占位（灰色隙间格） | 限张地形（`max<4`，如迷途竹林）的空置不可用格位：渲染层铺入，地形揭晓后自动出现、该侧超限时数量减少 |

「石块」的规则细节：

- **落地即翻开**、占用该区一个格位（会挤占后续出牌空间），之后按普通已翻开卡牌参与结算。
- 威力 0 不直接贡献点数，但**会正常被增益/削弱影响**（如「帕秋莉」+2 会让它涨战力），也可能成为「摧毁」（`dw`）的最低威力目标（被削成负值后更容易被摧毁）。
- 若己方场上有「比那名居天子」（翻开且未被摧毁），己方所有石块会被其**持续效果**常驻 +2（实时、随天子离场消失）。
- 不属于任何玩家的本回合出牌，**不参与回合翻牌流程**；不可重置、不可被选中操作。

「厄运」的规则细节（`spawnO` 落场型，对方侧）：

- 由「键山雏」揭示时放到**本区域对方一侧**（对方该区已放满则放置失败、不做替代处理）；落地即翻开、**占对方一个格位**，不占己方格位。
- 自身 **−3 战力直接计入对方该区总点数**（拖低对方该区），会显示在对方卡面（红色）与对方总点数横幅中；会被区域加成/增益/削弱影响，也可能成为摧毁/移走/回合末摧毁等效果的目标（威力为负时尤其容易成为最低威力目标）。
- 不属于本回合出牌，不参与翻牌流程；不可重置、不可被选中操作。

「赫卡提亚的分身」的规则细节（`clone` 落场型，另两区·自己一侧）：

- 由「赫卡提亚」揭示时生成到**另外两个区域**（不含本区）**自己一侧**各 1 张；该侧放满或区域未开放（如七夕坂锁定中）则跳过该区，两区都不可放则生成失败。
- 落地即翻开、占己方格位、进入场上放置顺序队列；与本体同立绘。
- 战力按本体**揭示时 `cardPower`（基础 + 永久 buff）快照固定**：快照与分身基础威力（5）的差值记在分身自身 buff 上；本体随后的增益/削弱不影响分身；分身自身随后仍可被增益/削弱独立修改，并正常受所在区域加成影响。
- 无其他效果；不属于本回合出牌，不参与翻牌流程；不可重置、不可被玩家选中操作。

「废弃列车」的规则细节（手牌衍生物）：

- 预留 token（当前无生成来源）：原由「八云紫」在 v76 重做前于揭示时加入**施放方手牌**（手牌满 7 张则加入失败）；若再有机制生成它，之后像普通手牌一样暗出、翻面时结算自身效果：摧毁本区对方一张**已翻开且实时战力最高**的卡（平局取第一张最高者；暗牌与 `un` 不可选）。
- 打出用完后不会回牌库/牌池；留在手里会占手牌位。

「隙间」的规则细节（`un: true`，绝对不可操作卡）：

- **无能量（0 费）、无战力（0 威力）**，灰色占位展示、不可点击/选中；由渲染层按需铺在**空置的不可用格位**——仅当区域为限张（max<4，如迷途竹林）时出现（开局三列先以 max 4 的「未揭示」展示故无隙间，揭晓成限张地形后自动出现；该侧已放卡超过上限时隙间数量自动减少，见 §1 超限口径）。
- **不可移动、不可被摧毁**（`dw` 不以其为目标、回合结束摧毁不会波及），**不可被任何效果修改**：区域加成（aff/cb/all）对它无效，`bf/de/ba` 不会改变它的 buff。
- 界面层它就是不可用格位的静态占位（不进 zone 数组）；`un` 是数据级的引擎防护，保证任何代码路径都动不了它。

---

## 6. 胜负与赌注

- 终局逐区域比较**最终战力**：战力高者赢得该区域；**「辉针城」等反转区域相反（战力低者胜，决胜总点数按负值计入）**；相等则记为**平局区域**。
- **胜负判定分两种情况**：
  1. 只要**存在平局区域**（一个或多个区域平手）→ 按**三个区域的总点数**（各区最终战力之和）高低决胜，总点数高者获胜；总点数也相同才是平局。
  2. **完全没有平局区域** → 直接比较赢下的区域数，3 块中赢下更多（≥2）的一方获胜（无平局时双方区域胜数不会相同）。
- 同一套判据也用于回合初判定“**当前领先方**”，决定翻牌先后（见 §1）；双方打平则随机。
- 初始赌注 1 立方；「双倍下注」把当前赌注 ×2（1→2→4→8，封顶 8），AI 可能跟进。终局赢家拿走当前赌注（胜利 +N，失败 −N）；平局无立方变动。
- 「认输」立即判负，输掉当前赌注。
- **终局演出（v85→v87）**：第 6 回合全部结算后，先等场上遗留的 ±N 加减动画播完，再**依左→右顺序**依次把每个区域的**胜方总点数横幅**做 **700ms 放大高亮**（金色光晕，`settle-win`；区域间隔 **150ms**，v87 由 300ms 调短；平局区域跳过），**三个区域演出全部结束后才弹出结算弹窗**。

---

## 7. 更新记录

| 版本 | 变更 |
| --- | --- |
| v139 | 抽牌：**去掉双方起手友好拒绝采样与兜底序列**——玩家自建卡组 / 玩家兜底曲线 / AI 曲线组牌后均为**纯随机洗牌** |
| v138 | AI 开局组牌：进入对局时按费用结构 **`1×2, 2×3, 3×3, 4×1, 5×1, 6×2`** 从人物卡池各费随机抽（同费用不重复），再走起手友好洗牌；与玩家自建卡组分离 |
| v137 | 开始对战：点「开始对战」先弹出**出战卡组选择**（`#battleDeckMask`）——按卡组栏同序（创建时间倒序）列出**满 12 张**的卡组，选中后才能开战；玩家牌库改用该组卡牌（仍洗牌并尽量起手友好），对手仍随机；无满编卡组时引导去卡组设置；「重新开始 / 再来一局」沿用上一局所选卡组 |
| v136 | 卡组设置：卡序改为**编辑中即时整理**——每次加入/移出一张卡后立即按费用升序→同费用战力升序重排卡槽；「💾 保存」只负责写入本地，不再在保存时排序 |
| v135 | 卡组设置：**保存**实装——编辑态原「完成修改」改为「💾 保存」；保存时按**费用升序 → 同费用战力升序**整理卡序，并经独立模块 **`deck-storage.js`**（`localStorage` 键 `touhou2.decks.v1`，卡名序列化）持久化；启动时自动读回；新建/删除/改名即时写入；返回主页时若仍在编辑亦视为保存 |
| v134 | 卡组设置手机端（≤700px）编辑态顶部布局：把「卡组名 N/12」并入顶部栏，与 ✏️/🧹/✓/← **同一行**显示（隐藏「卡组设置」页标题腾位；`.deck-bar-head` 迁入 `.deck-top`） |
| v133 | 卡组设置卡池：卡面高度统一——无图卡补 `.no-img`（emoji 正方形立绘区，同手牌 v103）；效果文字固定两行（`min-height:2.6em` + 行钳制），卡名单行省略；筛选后各卡同高 |
| v132 | 卡组设置卡池：筛选后卡少时卡片不再被网格纵向拉长——`.deck-grid` 改为 `align-content/align-items: start`，并关掉卡池卡 `.hc-text` 的 `flex:1` 吸收多余高度 |
| v131 | 卡组设置页面：下方卡牌区新增与游戏图鉴**同款费用筛选条**（`#deckFilter`，复用 `.card-filter-bar` / `.chip`：全部 / 0-1 费 / 2~6 费 / 衍生卡牌）；切换筛选即时重建 `#deckGrid`，计数显示当前档；「衍生卡牌」展示 SPECIAL token（不含隙间、虚线描边），**仅可查看不可加入卡组**；筛选档打开/关闭页面间保留 |
| v130 | 卡组设置页面（文案精简）：编辑态标题行与卡池标题行的「正在修改：」前缀**去掉**——`#deckBarTitle` 编辑态只显示**卡组名**、`#deckActiveLabel` 显示「卡组名（N / 12）」，改用金色标题（`.deck-screen.edit-mode .db-title`）与浏览态的「我的卡组」区分，信息不变但更清爽 |
| v129 | 卡组设置页面两处交互调整：①**点卡组位（含图片）直接进入修改模式**——`deckSlotEl` 的主体按钮由 `selectDeck()` 改为 `editDeck()`，**去掉缩略图上的 ✏️ 修改按钮**（`deckActionsEl` 只保留 **🗑️ 删除**，CSS 同步删除 `.ds-act.edit` 与 `.ds-actions` 的“两个按钮”描述），`selectDeck()` 退化为内部「设为当前卡组（高亮/联动显示）」且不再弹提示，浏览态提示文案改为「点卡组位直接进入修改」；②**新增卡组改名**——编辑态顶部新增「✏️ 改名」按钮（`#deckRenameBtn`）与专用弹窗 `#deckRenameMask`（输入框 `#deckRenameInput` 预填原名并全选、`maxlength=12`、iOS 字号 16px 防聚焦缩放），回车 = 确定 / Esc / 点遮罩 = 取消；`confirmRenameDeck()` 校验**非空、≤12 字（`DeckBuilder.NAME_MAX`）、不与其他卡组重名**，不通过时弹窗内红字提示且不关闭；通过后写入 `deck.name` 并整体重渲染（卡组位名称 / 标题行 / 悬停提示），并弹提示条 |
| v128 | 卡组设置页面（手机端卡池密度）：下方「全部卡牌」网格 `.deck-grid` 在 ≤640px 的最小列宽 **84px → 74px**、间距 8px → 6px，**每行由 3 张提升到至少 4 张**（360~430px 宽手机 ≈ 4 张 / 480px ≈ 5 张 / 640px ≈ 7 张；>640px 仍由基础 `minmax(104px, 1fr)` 控制，每行同样 ≥4 张）；并新增 **≤420px 兜底固定 4 列**（320px 老机型也保证 4 张/行）。卡片同步紧凑化：手机端费用/战力角标 20px → **19px**、卡名 11px → **10.5px 并加省略号**（避免「帕秋莉·诺蕾姬」这类长名溢出到相邻卡上） |
| v127 | 卡组设置页面（编辑态两处视觉/布局修复）：①**空卡槽与已放卡槽尺寸完全一致**——空位由「虚线框 + 序号」改为**与卡面同构的“幽灵卡”**（`deckEmptySlotEl` 生成 `.dcs-top` 角标行 / `.dcs-art` 正方形立绘区 / `.dcs-hint` 卡名行 三段，尺寸与边距分别对齐 `.cost-orb`、`.hand-card .hc-art`、`.hc-name`，边框同为 2px），因此**空行不再是矮条、填卡过程中卡槽与卡组栏不会跳高**，卡组全空与 12/12 时上栏高度相同；②**编辑态上栏改为按内容自适应高度**——`.deck-screen.edit-mode .deck-bar { flex: 0 0 auto }`（替掉 v126 的 `flex 1.5 : 2`），**12 张填满后不再多出一片空白**，省下的高度全部让给下方卡池；仅极端矮屏（横屏手机）以 `max-height: 62%` 兜底、由 `.deck-slots` 纵向滚动 |
| v126 | 卡组设置页面（编辑态两处调整）：①**已在卡组里的卡牌在卡池中改为灰色遮罩 + 不可点击**——`.deck-pool-card.in-deck` 用 `::after` 半透明灰层覆盖整张卡、立绘 `grayscale(1)`、取消悬停上浮、`cursor: default`、`aria-disabled`，点击不再有反应（移出一律改走「点上方卡槽」，卡池提示改为「点上方卡槽移出 · 右键放大查看」），**移除 v125 的绿色 ✓ 角标 `.de-in-badge`**，`addToDeck` 对已在卡组的卡改为提示而非移出；②**编辑态卡槽改为自适应网格、不再横向滚动**——`.deck-slots.edit-mode` 由 flex 行改为 `grid`（宽屏 `repeat(12, minmax(0,1fr))` 一行 12 格；**≤1000px 变 `repeat(6, ...)` = 2 行 × 6 格**），≤700px 再**适当缩小**卡槽（padding/圆角/角标/卡名缩小、卡名截断省略号）并把编辑态上栏 `flex` 调为 `1.5 : 2`、顶部按钮只留图标（`.bt-ico`/`.bt-txt`），**保证手机上 12 个卡槽一屏看全、无需左右滑动**（空间仍不足时才纵向滚动） |
| v125 | 卡组设置页面实装**修改卡组**（点卡组位上的 ✏️ 进入**编辑态**）：卡组栏由卡组位列表切换为**该卡组的 12 个卡槽**（已放 = 卡面、点它移出；未放 = 虚线空位 + 序号，`renderEditStrip`/`deckCardSlotEl`/`deckEmptySlotEl`），顶部新增「🧹 清空卡组」「✓ 完成修改」按钮（`#deckClearBtn`/`#deckDoneBtn`，仅编辑态显示），标题行显示「正在修改：卡组名 + `N/12`」（满 12 张转绿 `.db-count.full`）。下方卡池：**编辑态左键 = 加入 / 移出卡组**（`addToDeck`/`removeFromDeck`，同名卡限 1 张、满 12 张提示；已在卡组的牌加 `.in-deck` 绿框 + `.de-in-badge` ✓ 角标，标题显示「已在卡组 N 张」），**右键 = 放大查看**（`contextmenu`，两种形态均可）；「✓ 完成修改」或 Esc 退出（Esc 在游戏 `#zoomMask` 打开时不抢）。**确认弹窗通用化**：v124 的删除专用弹窗改为 `openConfirm({emblem,title,lines,okText,danger,onOk})`（`#deckConfirmMask` 系列 id + `.deck-confirm-modal`），删除卡组与清空卡组共用；删除编辑中的卡组会自动退出编辑态。改动实时写入 `DECKS`（暂存内存，保存后续加入） |
| v124 | 卡组设置页面：每个卡组位的**缩略图上竖排悬浮两个操作按钮**——**✏️ 修改卡组**（后续实装，现给提示条）与 **🗑️ 删除卡组**（本版实装）。结构上把卡组位由 `<button>` 改为容器 `div`、内容移入内部 `.ds-main`（`newSlotEl` 同步改造），使两个悬浮按钮与主体同层（合法嵌套、互不串点击）；`.ds-actions` 默认隐藏（`visibility/pointer-events` 一并关闭以免留隐形热区），悬停 / `:focus-within` 淡入，`@media (hover:none)` 触屏常驻。**删除卡组流程**：新增确认弹窗 `#deckDelMask`（`index.html` body 层级，复用 `.modal undo-modal` 样式 + `.deck-del-modal` 危险色，z-index 50）与 `askDeleteDeck / confirmDeleteDeck / cancelDeleteDeck`；确认后从 `DECKS` 移除该套、卡组栏收回一格、刷新 `N/20` 与「当前卡组」（删除选中卡组时改选最近创建的一套），取消 / 点遮罩 / Esc 均不删除；弹窗文案用 `textContent` 组装避免名称注入 |
| v123 | 卡组设置页面：卡组栏显示顺序改为**创建时间倒序**——「＋ 新建卡组」固定在最左，**越晚创建的卡组越靠左、越早创建的越靠右**（`renderSlots()` 反序遍历 `DECKS`，新建后新卡组位紧挨「＋」出现并播入场动画）；`DECKS` 内部仍按创建先后追加存储（id / 默认名「新卡组 N」/ 张数统计不变），仅渲染反序；卡组栏提示文案同步为「新建的卡组排在最左（越早创建的越靠右）」 |
| v122 | 卡组设置页面：卡组栏由「固定 20 个卡组位（含虚线空位）」改为**动态列表**——去掉全部空位，`renderSlots()` **只渲染「＋ 新建卡组」位 + 已创建的卡组位**，并把「＋ 新建卡组」实装为**真正的新建**（`createDeck()`：追加一套空卡组、默认名「新卡组 N」、自动选中、新卡组位播入场动画 `deckSlotIn`），**每新建一个卡组卡组栏就扩展出一格**（横向滚动，上限 20 套，满位置灰提示）；同步调整页面提示文案与文档口径 |
| v121 | 新增**卡组设置页面**（主页面「卡组设置」入口实装）：`index.html` 新增静态骨架 `#deckScreen`、新文件 **`deck-page.js`**（`window.DeckBuilder`：`open/close/render/selectDeck`，`index.html` 于 home.js 后加载），`home.js` 的 `PAGES.deck.ready` 置 `true`。版面**上方 1/3 卡组栏 + 下方 2/3 卡牌区**：卡组栏容量 **20 套**（`MAX_DECKS`，每套 12 张 `DECK_SIZE`），含「＋ 新建卡组」位 + 20 个卡组位（占位卡组 A/B 显示卡背叠放缩略 / 名称 / `N/12`，其余为虚线空位；点击高亮选中并联动「当前卡组」提示）；卡牌区列出 `POOL` **全部人物卡 35 张（不含衍生卡牌 token）**，复用 `gradOf`/`cardFaceHTML` 卡面与 `showZoom()` 放大查看。层级 `z-index 45`（低于弹窗 50/60），新增 `body.in-deck` 隐藏游戏区（与 `body.in-home` 合并为 `:is()` 规则；卡组页**不**隐藏 `.modal-mask` 以便复用放大弹窗）。**本版仅框架 + 占位卡组**：新建/编辑/保存等交互后续加入（点击给提示条），**游戏主逻辑仍未改动** |
| v120 | 新增**主页面（首页）**：打开游戏先显示主页面而非直接开局——`index.html` 首帧即带 `<body class="in-home">` 与静态 `#homeScreen` 标记（全屏不透明层，`body.in-home` 期间游戏区 `display:none`、`overflow:hidden`），交互逻辑放在新文件 **`home.js`**（`index.html` 于 card-browser.js 后加载；暴露 `window.Home.show/hide/startBattle/openPage`）。主页三个按钮：**开始对战**（隐藏主页面 → 调既有入口 `Game.restart()`）、**开发调试**、**卡组设置**（后两者为后续页面占位，点击给出提示条；实装接入点 = `home.js` 的 `PAGES` 注册表 `ready/page`）。**游戏主逻辑未改动**：`game.js` 底部启动 `restart()` 原样保留（后台完成首局初始化并停在出牌阶段等待输入），仅新增页面层 |
| v119 | 演出微调：开场抽完 3 张到第 1 回合开始的间隔由 **1s → 500ms**（`playOpening` 末段 sleep） |
| v118 | UI：在「认输」与「结束回合/跳过」两个按钮中间新增**当前手牌数量提示**（`N/7`，`.corner-center` 胶囊，桌面与手机都显示）；随每次手牌渲染更新（`updateHandCount`） |
| v117 | 代码拆分：人机思考逻辑（`hypotheticScore`/`aiThink`）从 `game.js` 移到独立文件 **`ai.js`**（`index.html` 在 game.js 后加载，同名全局函数供 playRound 调用），`game.js` 只留调用入口注释 |
| v116 | AI：**不再主动往反转区（辉针城 `inv`）放牌**（`aiThink` 候选区跳过 `locDef(j).inv`）——反转区胜者=点数更低，避免高战力大牌误拍导致“更高反而输”；「鬼人正邪」变形为辉针城的既有“落后≥10 才打”策略不变 |
| v115 | UI 修复（手机端）：手牌与能量点的左右内边距由 84px（避让角落按钮）**收窄到 14px**——v109 已把手牌抬到按钮上方，无需再留大空隙；抽牌后手牌可正常居中/贴左，不再左侧空出一张牌的空间 |
| v113→v114 | UI 微调（手机端 ≤700px）：费用/战力角标**压入立绘的程度减半**——角标上移至 `top:4px`、立绘上边距加回 `6px`，仅轻压图片上缘 |
| v113 | UI（手机端 ≤700px）：手牌高度略微减小——费用/战力角标不再占独立首行，改为绝对定位于卡面上部并**下移约一半、轻微遮挡立绘顶部**（省出一行高度，卡片更矮） |
| v112 | UI（手机端 ≤640px）：卡牌详情弹窗整体收小——卡牌本体 `min(176px,58vw)`、立绘艺术区 `min(140px,46vw)`（含放大查看/场上“战力影响历史”场景），相关文字/角标/影响历史面板同步缩小 |
| v110→v111 | UI（手机端 ≤640px）：图鉴与「指定卡牌」弹窗内卡片简略显示——只隐藏**效果文字说明**，保留 卡名/费用/战力 + 图片(或 emoji)（v111 更正：不隐藏卡名）；卡格按 `minmax(78px,1fr)` 排布节省空间 |
| v109 | UI（手机端）：手牌区整体上移——`.hand-wrap` 底部留白加大（≤900px 为 `42px+safe`、≤700px 为 `48px+safe`），避免左下「认输」/右下「结束回合」按钮遮住手牌 |
| v108 | UI 更正（手机端 ≤640px）：区域效果的“讲解说明”**保留在中间带原位**（撤销 v106 的移到底部方案，不再加列底 34px 预留与绝对定位）；仅保留隐藏列底 `已放 x/4 / 第 N 回合揭晓(开放)` 小字 |
| v107 | UI（手机端）：把“回合/能量/操作提示”状态条从顶栏下移到**地形区之后、手牌之前**（index.html DOM 顺序），文案换行增减不再让地形上下位移 |
| v106 | UI（手机端 ≤640px）：地形“效果讲解说明”从中间带移到**地形列底部**（绝对定位 + 列底预留 34px，仍继承各主题配色，中间带只剩双方点数横幅）；并隐藏列底 `已放 x/4 / 第 N 回合揭晓(开放)` 等小字（`.slot-count`）——⚠️ 移动说明部分已由 v108 撤销 |
| v105 | UI（手机端）：≤900px 断点隐藏顶栏「双倍下注」按钮（`#btnSnap`）；AI 仍可自行加倍，桌面端按钮保留 |
| v104 | UI（手机端）：场地三列改为**等分占满整宽**（`repeat(3, minmax(0,1fr))`）、去掉整板横向滚动（≤640px 断点）；网格单行 `1fr` + `.location` 拉伸，**三列高度始终一致**，内容变化不再让某列忽高忽低 |
| v103 | UI：手牌尺寸统一——无图（emoji 兜底）的手牌卡新增 `.no-img`：emoji 占一块与图片卡同款的正方形艺术区（aspect-ratio 1:1）；效果文字区统一预留两行高度（`min-height:2.6em`）。白板/短文案卡不再比带图/长文案卡矮，入场滑入也不会再互相撑高手牌区域 |
| v102 | UI：卡牌详情弹窗——**没有衍生卡牌**的卡（如贤者/小妖精等白板）弹窗收窄为 `min(520px, 94vw)`（`.zoom-stage.no-deriv`，renderDeriv 自动切换）；带衍生卡的仍保持宽版两列（880px） |
| v100→v101 | 演出（改稿+修复）：暗牌翻开**不做淡入淡出**，卡面以 **CSS `flipIn` 0.5s 从小到大缩放**入场（scale .3 → 1.12 过冲 → 回落 1，带提亮，观感同生成 token「凝聚显形」）；**根因修复**：此前白板/效果未触发的卡同帧被结算后的重建覆盖、动画不可见——现在仅当效果确会变化时才在结算后重建，其余保留入场元素直到动画播完 |
| v99 | 演出微调：地形揭晓换场淡出时长 **700ms → 500ms**（`revealLocFade` duration 与注释同步） |
| v98 | 演出：地形揭晓换场——在把真实地形换上之前，把当前「未揭示」外观的整列克隆到悬浮层（`revealLocFade`）并 **700ms 淡出**，下方真实地形随之显形（未揭示逐渐消失、真实逐渐显示）；克隆 pointer-events:none、不随重建中断 |
| v97 | UI：图鉴与开发者「指定卡牌」筛选条新增**「衍生卡牌」按钮**（紧跟 6 费后，`card-browser.js` 的 token 档）——点击展示所有特殊卡 token（石块/厄运/分身/废弃列车；静态「隙间」不列入，卡面加虚线描边）；图鉴可点击放大查看；「指定卡牌」页衍生卡**仅可查看**（不参与加入手牌，`confirmPick` 拦截） |
| v96 | 演出：换边效果（`switch` 依神紫苑自换边、`gift` 因幡帝送己方最低卡）也使用与移动相同的**滑行+缩放克隆飞行**（`flyCardTo`）：换边时记录源格位 → 数据搬边 → 本次渲染后播放（`pendingSwitchFly`/`flushPendingSwitchFly`，约 0.26s，不阻塞） |
| v95 | 抽牌入场改为回退方案：不再用 body 固定克隆，改回**浏览器原生 CSS transform 滑入动画**（`.hand-drawn`，0.65s）；为消除根因（transform 撑大滚动区），动画期间由 `lockDrawAnimScroll` 给 #hand 加 `.draw-anim`（overflow-x:hidden）抑制横向滚动条，连续抽牌计时器会重置保持全程抑制 |
| v94 | 抽牌节奏/开场演出：开局对手 3 张直接落数据，玩家 3 张由开场演出**逐张**播放“从右滑入”入场动画（每张约 0.72s，`playOpening`），三张齐后**间隔 1s** 进入第 1 回合；**第 1 回合也正常各抓 1 张**（含玩家滑入动画，起手共 4 张），第 2 回合起维持每回合 1 张；`restart` 改为 async |
| v93 | 修复：抽牌“从右滑入”改为 **body 固定克隆演出**（`animateHandDrawEntry`：真身渲染在手牌末位并隐藏，克隆从屏幕右缘滑到其格位 0.65s 后露出），不再用 transform 平移手牌内的卡，桌面端不再闪现横向滚动条；原 `.hand-drawn` CSS 移除 |
| v92 | UI 重构：图鉴与开发者「指定卡牌」页面实现拆到新文件 `card-browser.js`（game.js 仅保留入口转发 `uiOnCodex`/`uiOnPick`/`closeCodex` 等 → `window.CardBrowser`，`index.html` 于 game.js 后加载）；两页新增**费用筛选条：全部 / 0-1 / 2 / 3 / 4 / 5 / 6 费**（0-1 = 0费+1费合并），两页各自记忆筛选档、切换即重建网格并更新计数/提示 |
| v91 | 演出：每回合开始玩家抽牌时，新抽到的卡**从屏幕最右侧滑入**落到手牌最右端（`.hand-drawn`，0.65s、微过冲回弹；标记 `justDrawn` 只在回合开始抽牌处设置，开局 3 张与 AI 不触发） |
| v90 | 演出：给「生成/添加卡牌」加入口动画（1s，节奏同 ±N 气泡）——①**场上落场/生成的卡**（石块/厄运/分身等，经 `placeToken`/clone 标记 `justSpawned`）：**凝聚显形**（scale .45→弹 1.16 提亮→回稳，淡青光环扩散，`.spawned-now`）；②**加入手牌的卡**（give 衍生物、开发者指定卡，标记 `justHandAdded`）：**底部滑入弹起**（`.hand-new`）；渲染处一次性消费标记，常规抽牌不触发 |
| v89 | 演出：射命丸文的 `fly` 移动也使用与八云紫 `shift` 同款的**滑行 + 缩放飞行克隆动画**（`tryMoveFlyTo` 搬移后调用 `flyCardTo`，真身落位、克隆飞过去约 0.26s，不阻塞后续操作） |
| v88 | 演出：被摧毁的卡牌新增**分崩离析**动画（`playShatter`）——摧毁前抓取卡面克隆到 body 悬浮层，切成 3×2 六块碎片向四周迸散 + 旋转 + 末段渐隐（1s，节奏同 ±N 气泡）；仅真实移除触发（`dw`/`dwh`/反应炉 purge）；surv/phx/蕾蒂防护（卡未被移除）与移动类效果不触发 |
| v87 | 演出微调：终局胜区横幅区域间隔 **300ms → 150ms**（`playEndHighlights` sleep 调整） |
| v86 | 演出微调：终局胜区横幅动画时长 **1s → 700ms**、区域间隔 **500ms → 300ms**（`playEndHighlights` 两处 sleep 与 `style.css` `.settle-win` 同步） |
| v85 | 演出：终局流程新增**胜区点数放大演出**——第 6 回合全部结算后先等待场上 ±N 加减动画清空（`waitBuffFxDone`），再依左→右依次把每个区域的**胜方总点数横幅**放大高亮（`playEndHighlights`，每区 1s、区间隔 500ms、平局区跳过），全部结束后才执行 `finishMatch` 弹出结算弹窗；CSS `.settle-win`（缩放 + 金色光晕） |
| v84 | 演出扩展：新增**红色「-N」版本**——卡牌**永久降战力**（`de` 敌方削弱、`surv` 防摧毁降战力等负向路径，与正向统一收口 `applyPermBuff`）时播放同构 1000ms 红色动画（卡面泛红压暗 + 红环 + 红色大号 -N 气泡，末段 300ms 渐隐；`style.css` `.buff-loss`/`.gain-ring.loss`/`.buff-gain-pop.loss`）；原绿色收口函数 `gainBuff` 泛化更名 `applyPermBuff` |
| v83 | 演出修复：+N 气泡改为「迅速冲大 → 停留 → 末段约 300ms 渐隐」（1s 内 70%→100% 淡出），不再有“刚到最大就瞬间消失”；绿环与气泡移到 body 独立悬浮层（`.gain-ring`/`.buff-gain-pop`），不再因 renderZones 重建缩略卡而中途被打断；卡面本体仅保留短暂提亮 |
| v82 | 演出微调：绿色“+N 战力”动画时长 700ms → **1s**，且「+N」气泡**字号加大突出数字**（clamp 26–46px、光环提亮峰值后移、环扩至 20px、JS 清理延迟 1080ms；`style.css` `.buff-gain`） |
| v81 | 演出微调：绿色“+N 战力”动画时长由 300ms **调长至 700ms**（绿环扩至 16px、提亮峰值后移、+N 气泡上浮距离加大；`style.css` `.buff-gain` / JS 清理延迟 320→760ms） |
| v80 | 演出：卡牌获得**永久 +N 战力**时播放约 300ms **绿色小动画**（绿环扩散 + 提亮 + 上浮「+N」气泡；收口于 `gainBuff` 排队、`renderZones` 末尾 `flushBuffFlash` 统一触发；覆盖 `bf`/`ba`/`bl`/`oc` 及 `phx` 重生路径，`de`/`surv` 等负向暂无红色动画） |
| v79 | 演出：八云紫 `shift` 逐张搬运改为**滑行+缩放飞行演出**（`flyCardTo` 克隆卡从原格位飞向目标格位，弧线 + 落地缩放回弹，WAAPI 动画约 0.26s；卡片间隔仍约 0.3s；无动画能力/拿不到坐标时自动退化为纯停顿）；拆出 `peekShiftCard`/`shiftMoveCard`，场上缩略卡加 `data-cardid` 便于定位；中途重新开局会安全中断 |
| v78 | 演出：八云紫 `shift` 整体右移改为**逐张搬运动画**——正常翻牌揭示结算时每移动一张间隔 0.3s（新增 `shiftMoveOne` / `applyShiftReveal`，逐步 renderZones + sleep(300)）；morph/fx 等其它结算路径仍走一次性同步搬运，规则口径不变 |
| v77 | 新增人物「蕾蒂」（🌨️，**2费/3战力**，立绘 `letty.png` 就位）：新增卡级**持续免摧毁**机制 `prot: true`——此牌翻开后、仍在自己某区域场上期间，**该区域（双方）所有在场卡牌均无法被摧毁**（本区 `dw`/`dwh`/回合末 purge 等摧毁一律失效，防摧毁 surv / 凤凰重生 phx 不触发，含 token 与源卡自身；防护按所在区域实时判定——移去其它区域后原区失效、新区生效，同区换边因保护双方效果不变，真正离场才全场失效）；引擎新增 `locNoDestroy` 防护并在 dw/dwh/reactorPurge 三处摧毁点接入 |
| v76 | 重做「八云紫」（5费/6战力 give → **6费/10战力**）：新增引擎效果键 `shift` 整体右移——揭示：把己方**最左侧区域**（第 1 列）**已翻开**的卡牌按放置顺序依次搬到**最右侧区域**（第 3 列）己方一侧，直至右侧己方侧填满为止（占格判定；暗牌不动；不变归属/揭示态/放置队列）；不再生成「废弃列车」衍生物（该 token 转为预留、无生成来源）；5 费池移出、6 费池加入 |
| v75 | 视觉：未揭示地形列重做为“随机未知”主题——淡紫灰底 + 虚线边框 + 池中所有地形的小图标随机散布整列（JS 每次开局随机布局、缓慢漂浮动画，CSS `.unreveal-decor`；揭晓成真实地形后装饰自动移除）；配色按浅色底调优，暗牌/卡面/横幅可读性保持 |
| v74 | **地形揭晓系统（开局流程大改）**：三块真实地形开局即按 `pick` 权重抽定（互不相同）预存于 `state.locPlan`，场上三列一律先以「未揭示」占位地形（`EXTRA.unreveal`，样式同无名之丘 / max 4 / 无效果）展示；**第 1/2/3 回合开始时依次揭晓到左/中/右列**（`locationRevealStage`，位于回合开始阶段最先）：换上真实地形（全部字段与列头配色即刻生效）、结算该地形「出现时」一次性效果（如虹龙洞在揭晓时给双方各 1 张石块）。未揭示地形可正常放牌（上限 4），列下提示「🃏 第 N 回合揭晓」；**限张超限口径**：揭晓成 max<4（迷途竹林）而某侧已放超上限时（仅第三列可能）不摧毁不移走，卡保留原格位，隙间只补空置不可用格（数量 = 4−max(已放,上限) ≤2；放满 4 张或大体积占满则 0） |
| v73 | 新增「未揭示」地形（❓，id: `unreveal`；⚠️ 勿用 id `hidden`，与全局 `.hidden` 隐藏类冲突）：max 4 / 无特殊效果 / 样式同无名之丘，中间带文案「未揭示地形」；**不入随机地形池**——放在 `data/locations.js` 新增的 `EXTRA` 表，开局三区抽选（只遍历 POOL）永不抽到；引擎地形按键名查找（`findLocDef`，供 xform 用）扩展为 POOL+EXTRA；v74 起作为每局三列的初始未揭示态被揭晓系统使用 |
| v72 | `morph` 变身补充**大体积目标限制**：随机目标是占多格的大体积卡（如萃香 occ4）时，仅当本区域 max 恰为该占格数、且己方该区（明牌+暗牌合计）有且仅有二岩这一张卡才可变身成萃香；否则变身失败保持原样 |
| v71 | 占格口径复核与修复：`mv`（红美铃）移走目标卡时，目标侧空余需 ≥ 该卡占格数——目标为大体积萃香（occ4）时只能移进该侧**完全空出**的 max4 区域（目标侧已有任意卡则萃香不会被移走）；换边（`switch`/`gift`）落对方侧按占格计，对方侧被萃香占满（空余 0）时换边失败 |
| v70 | 新增人物「伊吹萃香」（🍶，**5费/12战力**，复用 `suika.png`）：新增卡级机制**大体积占格** `occ:4`——单卡占满区域 4 格，只能放入 max=4 且己方该区空无卡的区域；在场该侧视为已满（放满 fill 按 4 格计），卡面放大覆盖整侧 2×2；被摧毁/移走/换边/离场后腾出格位；出牌/AI/生成/移入等容量判定统一改走占格口径（sideUsed/sideRoom） |
| v69 | 新增人物「稗田阿求」（📜，**0费/6战力**，立绘 `akyuu.png`；0 费组**暂不入牌库洗牌**）：新增卡级机制 `leave`——游戏结束时从场上消失（非摧毁、不触发任何摧毁类机制，buff 随卡一并消失、不计入终局结算）；图鉴与开发者指定卡可查看/测试 |
| v68 | UI 修复：**手牌卡面与手牌详情弹窗显示当前战力**（基础 + 永久增益，如凤凰重生回手的妹红在手中即显示已 +2 并绿色高亮）；新增 `showHandCard`（手牌点击/右键查看） |
| v67 | 重做「藤原妹红」（🔥，6费/9战力 dw → **1费/1战力 凤凰重生**）：新增卡级机制 `phx:2`——被任何“摧毁”指向时不会消失，改为**返回自己手牌并永久 +2 战力**（每次触发、可重复打出持续成长；手牌满 7 张则重生失败、按原样被摧毁），立绘沿用 `mokou.png` |
| v66 | 新增人物「因幡帝」（🐇，2费/3战力，立绘 `tewi.png`，素材文件名为「因幡天为」）：揭示 `gift`——把本区域**己方战力最低的已翻开卡**（不含自己，多张并列最低随机选一）换边到对方该区域一侧（归属变对方、占对方格位，其持续效果按新归属方生效；对方该区放满则失败）；新增效果键 `gift` |
| v65 | UI：侧栏对手信息由「已打 N 张」改为显示**对方当前手牌剩余张数**（手上还有 N 张；数据源 ai.hand.length） |
| v64 | 新增人物「二岩猯藏」（🦝，3费/0战力，立绘 `mamizou.png`）：揭示 `morph` 变身——从**对方手牌随机取一张**，把自身整体替换为该卡**复制体**（原卡保留，卡面/数值/效果全变），并**重新触发新卡效果文本**：新卡若带 k 立即再结算其揭示（同为变身卡则不二次变形，防死循环），持续/移动/时机/防摧毁等由新 def 实时生效；对方手牌为空则不变 |
| v63 | 新增人物「依神紫苑」（📉，5费/−7战力，立绘 `shion.png`）：揭示 `switch` 换边——翻开后立即从自己一侧转移到**对方该区域一侧**（归属同步为对方、占对方格位、−7 计入对方，场上放置顺序不变）；新增效果键 `switch`；对方该区已放满则换边失败、留在原地 |
| v62 | UI 修复：卡牌详情弹窗桌面加宽至 `min(880px, 96vw)`（本体卡 + 衍生卡两列完整展示）；衍生小卡不再沿用放大主卡的 240px 艺术区（原会溢出卡宽导致显示不全），改为自适配尺寸（卡宽 260 / 艺术区 224 / 衍生列 min 300），文字允许任意换行 |
| v61 | 新增人物「赫卡提亚」（💠，6费/5战力，立绘 `hecatia.png`）：揭示 `clone` 向**另外两个区域**（不含本区）**自己一侧**各放 1 张「赫卡提亚的分身」（该侧放满/未开放则跳过）；新增效果键 `clone`（`def.clone` 负载）与特殊卡「赫卡提亚的分身」（💠，6费/5战力，同立绘，无效果）——分身战力按本体**揭示时 cardPower 快照固定**（差值记 buff，可再被独立增减）；分身落地即翻开、占格位、进放置队列 |
| v60 | 新增人物「灵乌路空」（☀️，3费/6战力，立绘 `okuu.png`）：①揭示 `xform` 将本区域变成「聚变反应炉」（原有地形机制复用，变身瞬间起每回合末 purge）；②新增卡级**防摧毁**（`surv:2`）——被任何“摧毁”指向（dw/dwh/反应炉 purge）时不会离场，取而代之永久 −2 战力（可多次触发，记入战力影响历史） |
| v59 | 术语统一：**「在场光环」机制更名为「持续效果」（简称「持续」）**——定义：此牌翻开后**持续存在**的效果（非一次性触发，与「揭示」互补；源卡离场/被摧毁即失效）。现仅「比那名居天子」的 `og`（翻开后在场期间，己方全场石块 +2，实时）满足该定义；改名已同步卡面/图鉴/注释/当前规则文档（历史更新记录保留旧称）；每回合移动 `fly`、区域常驻加成（aff/cb/all/fill）、时机效果 `fx`、揭示施加的永久 buff 等不并入 |
| v58 | 术语统一：**「现身」(On-Reveal) 机制更名为「揭示」**——定义：此牌翻开时立刻执行的一次性效果，后续不再触发。全部现有效果键（`k` 体系：`bf/de/ba/bl/dw/dwh/spawn/spawnO/xform/mv/give/oc`）均符合该定义，改名已同步到卡面文案、图鉴标签、代码注释与当前规则文档（历史更新记录保留旧称）；在场光环 `og`、每回合移动 `fly`、时机效果 `fx` 等非翻开一次性机制不改名 |
| v57 | 新增人物「键山雏」（🎎，3费/1战力，立绘 `hina.png`）：现身（k=`spawnO`）在本区域**对方一侧**添加 1 张「厄运」；新增敌方侧生成效果键 `spawnO`（`def.spawnO` 负载，对方该区放满则失败）与特殊卡「厄运」（🌀，3费/−3战力，深紫卡面，白板占位：落对方侧占格位、−3 计入对方总点数，可被增益/削弱/摧毁等正常影响） |
| v56 | 新增**战力影响历史**：卡牌实例维护 `powerLog` 台账，在现身增减结算处自动记录永久 buff 的来源（模块化可扩展）；点击**场上已翻开卡**时，在卡牌详情弹窗旁（同遮罩、独立并排面板）展示该卡全部影响来源：基础战力 + 永久 buff（来源=施加方卡牌，自身效果显示"卡牌效果"）+ 在场光环（分来源、实时）+ 区域加成 aff/cb/all（实时）；**+N 绿色 / −N 红色**，合计 = 场上当前威力；详情弹窗内移除重复的"场上修正/区域加成"文字行 |
| v55 | 新增“场上放置顺序队列”（`state.fieldQueue`：记录双方卡牌放入场上的先后——手牌打出/落场特殊卡入队、被摧毁/重置撤回手牌出队、移动不变序）与卡牌时机效果字段 `fx: { turnStart?, turnEnd?, gameEnd? }`（条目与现身规格同构 `k/a/spawn/xf/give/t`，复用现身结算逻辑）；回合开始（先于能量/抽牌）、回合结束（翻牌后、区域回合末 purge 前）、游戏结束（结算胜负前）三个全局时机按队列先后结算全场带对应 fx 的卡。现无卡注册 fx |
| v54 | 引擎流程重构（**行为不变**）：主循环拆为显式阶段管线 —— 游戏开始（⓪开局效果挂点）→ 每回合【回合开始效果(挂点) → 能量结算+抽牌 → 玩家放置/移动 → AI 放置 → 翻牌现身结算 → 区域回合结束(purge) → 手牌回合结束(挂点)】→ 第 6 回合后【游戏结束效果(挂点) → 结算胜负】；各阶段以独立函数实现并预留挂点（v55 起回合开始/回合结束/游戏结束三处由放置队列驱动） |
| v53 | 区域抽选改为按 `pick` 权重不放回抽取（默认 1）；辉针城 `pick: 0.28` 出现率降至约 10%（≈ 每 10 局 1 次） |
| v52 | 节奏优化：翻面→效果结算停顿为 **400ms**（有效果时才停顿；空转直接结算）；效果→下一张仍 500ms |
| v51 | 节奏：卡牌翻面后先停顿展示裸数值/位置，再结算「现身」效果；翻牌节奏统一 **500ms**（翻面→效果 500ms、效果→下一张 500ms） |
| v50 | 数值：鬼人正邪 6费 15→**10** 战力；AI 策略优化：1) 区域变形（辉针城）卡仅当 AI 在本区**落后对方 ≥10 点**时才会打出；2) 往**聚变反应炉**落子的估值权重 ×0.25（-75%），尽量避开该区 |
| v49 | UI：龙神像改为**很浅的灰色**主题（主底 `#f2f2f0`，比冥界浅；深灰边框/表头） |
| v48 | UI：七夕坂改为**淡粉**纯色主题（浅粉主底 + 深粉边框/表头） |
| v47 | UI：虹龙洞改为**淡黄**纯色主题（浅黄主底 + 金黄/棕深色边框与表头） |
| v46 | UI：迷途竹林改为**中等明暗绿色**纯色主题（中绿主底 + 深绿边框/表头）；红魔馆改为**淡红**主题（浅红主底 + 深红边框/表头） |
| v45 | UI（成套纯色主题修正）：冥界=浅灰主底 + 深灰边框/表头/格线；雾之湖=浅蓝主底 + 深蓝边框/表头/格线；不再是简单叠淡色 |
| v44 | UI：冥界区域改**纯淡灰**底（`#dcdcd8`）、雾之湖改**纯淡蓝**底（`#d9ebf8`），列内文字/横幅保持可读 |
| v43 | UI：辉针城区域背景改为**上下各半（上黑下白）**的反转意象，表头/中间带文字与格线相应调色保证可读 |
| v42 | UI：聚变反应炉区域整体改为**暗红色**主题底（列底/表头/中间带/两侧格区联动配色，保持可读性；横幅/卡牌样式不变） |
| v41 | 能量重置扩展：点能量框“重置”除放回暗牌/返还能量外，把本回合移动过的牌（fly，如射命丸文）**移回回合初区域并恢复移动次数**；未暗出牌但只移动过时同样可重置 |
| v40 | 重做「射命丸文」（5费/8战力 白板 → 5费/7战力）：新增卡级机制 `fly: true`（每回合移动一次）——出牌阶段点击已翻开的己方文 → 再点另一区域即可移动（目标须未满/已开放，不耗能量，每回合一次，下回合重置；AI 暂不使用）；卡片带 ⇄移动 徽标 |
| v39 | 重做「魂魄妖梦」（3费/2战力 bf+3 → 2费/1战力）：现身（k=`oc`）若翻开当回合对方曾在本区域放置至少一张牌，此牌永久 +4；新增 `oc` 引擎效果键（按当回合对方落牌记录判定） |
| v38 | 重做「芙兰朵露·斯卡蕾特」（4费/5战力 白板 → 4费/3战力 摧毁）：现身摧毁同区对方已翻开的威力最低卡牌（dw）；scarlet 红魔馆成员身份保留 |
| v37 | 数值对齐：风见幽香（6费白板）10→**12**、射命丸文（5费白板）7→**8**，与同费用白板卡（贤者/大妖怪）战力一致 |
| v36 | UI：1) 未开放区域（七夕坂第 5 回合前）整列叠加灰色遮罩 + 🔒，开放后自动消失；2) 卡牌详情弹窗右侧新增「衍生卡牌」区——凡卡与特殊卡有关联（spawn/give/在场光环 og，如天子→石块、八云紫→废弃列车）都会并排展示对应特殊卡卡面，与主弹窗同框，关闭主弹窗时一起关闭 |
| v35 | 重做「八云紫」（4费/4战力 ba → 5费/6战力）：现身（k=`give`）把 1 张「废弃列车」**手牌衍生物**加入手牌；新增特殊卡「废弃列车」（🚂，3费/3战力，k=`dwh`：摧毁本区敌方一张已翻开的战力最高卡）；新增 `give`（入手指派）与 `dwh`（摧毁最强）引擎效果键 |
| v34 | 新增人物「红美铃」（🥋，2费/3战力，scarlet 红魔馆成员，立绘 meiling.png 复用）：现身（k=`mv`）把本区对方**已翻开且实时战力最低**的卡移走——随机移到另外两区之一（对方该侧满员或区域未开放则排除，两处都不可达则失败）；移动后各区域总战力自动重算 |
| v33 | 开发者调试：顶部导航新增「⚡ 修改能量」——出牌阶段点击把本回合能量设为 7（能量条/手牌可负担即时刷新）；下回合起恢复按回合数计，仅本回合生效 |
| v32 | 新增人物「鬼人正邪」（🪞，6费/15战力，立绘 seija.png 已就位）：现身（k=`xform`）把本区域整体变形为「辉针城」（战力低者胜；原限张地形的隙间随上限变 4 消失、可继续放牌；任一侧超上限则失败；不重触发目标地形的一次性出现效果）；引擎新增区域变形机制与列头刷新 |
| v31 | 开发者调试：顶部导航新增「🎯 指定卡牌」——弹窗内点选任意人物卡并确认即加入手牌；手牌达 7 张上限时加入失败并提示（弹窗保持打开）；确认/取消/Esc/点遮罩均可关闭 |
| v30 | UI：手牌效果文字超过两行以省略号截断（避免长文案撑高卡面，如天子）；新增**右键手牌弹出完整卡牌详情**弹窗（含完整效果文案；终局复盘点击手牌同样看详情） |
| v29 | 重做「比那名居天子」（5费/5战力 → 3费/2战力）：现身（k=`spawn`）给本区域双方各添 1 张「石块」；新增**在场光环**卡级机制（`og`/`tk`）——天子已翻开且仍在己方场上时，己方所有石块 +2（实时判定，离场/被摧毁立即失效）；石块打上 `tk:'rock'` 标记、生成来源含天子；卡对象开始记录属方(side) |
| v28 | 白板人物批次更名（按各费战力匹配）：1费 露米娅→小妖精(🌟/2)；2费 红美铃→小妖怪(👺/3)；3费 博丽灵梦→中妖精(💫/4)；4费 蕾米莉亚→中妖怪(👹/6)；5费 东风谷早苗→大妖怪(🐲/8)；6费 伊吹萃香→贤者(🧙/12)。新卡暂无图片（img 清除、emoji 兜底）；红美铃、蕾米莉亚更名后**脱离 scarlet 阵营**（红魔馆成员现为：咲夜/帕秋莉/芙兰朵露 + 待实装小恶魔） |
| v27 | 现身增减目标口径收紧：`bf/de/ba` 只作用于结算时**已翻开**的卡牌——先结算的加减不会预留给尚未翻开的暗牌（后翻开的牌错过本次结算，与 v25 的 dw 同口径）；相关卡面文案（琪露诺/帕秋莉/妖梦/莉格露/米斯蒂娅/梅蒂欣/幽幽子/大妖精/八云紫）与手册/机制表同步 |
| v26 | 「隙间」登记为特殊卡牌（`SPECIAL.gap`，`un: true` 绝对不可操作：0 费 0 威、不可移动/摧毁/被修改）；引擎新增 un 免疫防护（区域加成/bf/de/ba/dw/回合结束摧毁均跳过）；新增 `data/现有机制.md` 引擎机制总表，特殊卡牌文档补隙间条目 |
| v25 | 摧毁机制收紧：`dw` 只能指定同区**已翻开**的对方卡牌为摧毁目标（暗牌不可被提前摧毁，故不再存在"被毁暗牌不翻牌"）；魔理沙/永琳/妹红效果文案同步（反应炉等回合结束效果不受影响） |
| v24 | 新增地形「聚变反应炉」（🔥，id: reactor，`purge: true`）：每回合翻牌结算后摧毁本区全场（敌我混比）战力最低的牌、并列最低一并摧毁（第 6 回合翻面后也执行）；引擎新增 `reactorPurge` 回合结束钩子 |
| v23 | 新增地形「冥界」（🪦，id: underworld，`all: -2` 全区修正）：此区域所有卡牌威力 -2（双方、特殊卡同享）；引擎新增可拓展的 `all` 字段（每卡 ±N 常驻实时，与 aff/cb 同口径计入卡面/总数/摧毁/落后判定） |
| v22 | 新增地形「七夕坂」（🌠，id: tanabata，`minTurn: 5` 限回合）：第 5 回合起双方才可在此区域放牌；引擎新增可拓展的 `minTurn` 字段，玩家/AI/可放高亮统一经 `locOpen` 判定，未开放时计数角标显示「🔒 第 N 回合开放」 |
| v21 | 新增地形「辉针城」（🏯，id: needle，`inv: true` 反转区域）：本区域总战力更低的一方获胜；引擎统一改用 `zoneEff` 有效口径（dbl 后反转取负）驱动区域胜负/领先判定/翻牌先后/总点数决胜（反转区按负值计入）/`bl` 落后判定/AI 估值，横幅与结算行仍显示真实点数并标注「低者胜」 |
| v20 | 新增地形「雾之湖」（🌅，id: lake）：此区域所有 1 费卡牌威力 +2（双方、含 1 费特殊卡）；引擎新增可拓展的 `cb` 费用加成字段（按卡牌费用条件加威力，常驻实时，与阵营加成同口径计入卡面/总数/摧毁/落后判定） |
| v19 | 新增地形「龙神像」（🐉，id: dragon）：某一方在本区实际放满 4 张（含暗牌/生成卡）即该方总战力常驻 +5（敌我各自独立）；因摧毁/撤回掉回不满 4 张立即失效；引擎新增可拓展的 `fill` 放满加成字段，AI 落子会预判放满回合的 +5 |
| v18 | 结算/认输弹窗改为左「确认」右「再来一局」：点「确认」仅关闭弹窗，终局盘面保留（场上卡与手牌不清空），已翻牌可点击放大、手牌复盘时可点击查看卡面 |
| v17 | 「红魔馆」地形重做：原"终局总威力 ×2"改为**阵营加成**——位于该区的「红魔馆」角色（scarlet 阵营，红美铃/帕秋莉/咲夜/蕾米莉亚/芙兰朵露，小恶魔待实装）威力常驻 +2；引擎新增可拓展的区域-阵营加成机制（卡牌 `g` 所属字段 + 区域 `aff` 字段 + `GROUPS` 阵营名表），加成计入卡面实时威力/区域总点数/摧毁与落后判定/放大视图 |
| v16 | 新增地形「虹龙洞」（🕳️，出现时双方各生成 1 张「石块」）；引入特殊卡牌体系（`SPECIAL` token：不入牌库/图鉴，落地即翻开、占格位、正常受增益/削弱/摧毁影响，支持自定义卡面色 `cg`）；地形效果文字继续沿用 v15 放大尺寸；新增 `data/现有地形.md` / `现有人物卡牌.md` / `现有特殊卡牌.md` 三份清单文档（与数据文件同步维护） |
| v15 | UI：区域中间带效果文字放大（桌面 10.5→12.5px、图标 12→15px；移动 9→10.5px）；地形「博丽神社」更名「无名之丘」（无效果白板地形，图标 ⛩️→⛰️） |
| v14 | 区域抽选改为不重复：每局三块地形互不相同（区域池不足 3 种时退回旧逻辑兜底） |
| v13 | 所有区域（无论 max 为几）的敌我两侧均绘制浅灰 2×2 网格线 |
| v12 | 场地格位细化：max=4 区域敌我两侧绘制浅灰 2×2 网格线；max<4 区域的不允许格位用灰色「隙间」占位卡填充（随区域初始化生成、不可操作） |
| v11 | 数据分层：卡牌拆到 `data/cards.js`、场地拆到 `data/locations.js`（game.js 不再内建数据）；卡牌支持 `cards-image/` 图片素材（新增 `img` 字段，缺图自动回退 emoji） |
| v10 | UI 主题：整体改为「幻想乡自然风景」清新淡绿浅色系（晨光/雾霭/草地渐变背景，草木绿面板 + 森林墨字 + 金色点缀）；游戏规则无改动 |
| v9 | 区域总点数横幅动态着色：该区领先的一方亮黄色，另一方及平点区域均显示蓝色 |
| v8 | 主题换皮为「东方逆转」：全部 24 张卡与 3 种区域改用东方 Project 角色/地点命名（效果不变）；翻牌先后改为「首回合随机，之后按当前领先方先翻、持平随机」 |
| v7 | 修复：同一回合多张牌的翻牌顺序始终等于放置顺序（偶数回合不再整体倒序）；暗牌战力不计入区域总点数，翻面后才计入（AI 决策估值另按全盘面计算） |
| v6 | 胜负规则改为「有平局区域按三区总点数决胜 / 无平局按区域多数」；区域效果移到敌我中间带，双方总点数改为醒目横幅；桌面卡缩略图改为左上白色费用 + 右上白/绿/红战力 |
| v5 | 场地展示：卡位固定 2×2（落位不随张数移动）、卡牌放大铺满格位；场上已翻牌可点击放大（实时场上威力+效果说明）；右上角战力改为含区域倍率的最终口径 |
| v4 | UI：认输移至左下、跳过/结束回合移至右下；能量框可点击重置本回合暗牌（返回手牌+返还能量） |
| v3 | 移除区域「圣地之巅」（权重 ×2 太影响平衡）；新增开局起手保护洗牌；本文档首次建立 |
| v2 | 一回合可出多张牌（能量预算制），新增「结束回合」按钮 |
| v1 | 初版：一回合一张牌模式 |
