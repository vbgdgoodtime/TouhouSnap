# 东方逆转（Touhou2）

> Marvel Snap 玩法 + 东方 Project 主题的**纯前端卡牌网页 Demo**：无构建工具、无后端、无网络请求。
> 6 回合（地形「虚假之月」🌕 在场时为 7 回合）/ 3 块随机地形 / 双倍下注 / 卡组构筑与本地存档。

## 快速开始

**方式一（最简单）**：直接用浏览器打开 `index.html`。
本项目没有 `fetch`/`XMLHttpRequest`/ES Module，所有脚本都是普通 `<script>`，`file://` 下可直接运行。

**方式二（推荐，本地存储更干净）**：起一个静态服务器再访问。

```bash
python -m http.server 8000        # 然后打开 http://localhost:8000/
# 或 npx serve .
```

首页入口：**开始对战**（先选满 12 张的出战卡组）、**卡组设置**（构筑/保存卡组）、**📚 图鉴**（v181：不进对局，直接查看全部卡牌与说明 —— 与对局顶栏「📖 图鉴」同一个弹窗，含「普通卡牌池 / 衍生卡牌池」两层筛选）、**开发调试**（空牌库 + 固定地形 + 指定卡牌 / v206 添加石块等调试工具）、**⚙️ 设置**（v174：切换对手 AI 强度 —— v175 起四档 **简单（默认）/ 普通 / 困难 / 月狂**，其中「简单」是最初版的贪心 AI；点选即生效并记在本地）；五个主入口**下方**另有 **📖 新手引导**（v172，只读的机制速览弹窗，不进入对局）。

## 目录结构

```
Touhou2/
├─ index.html            入口页面（静态标记 + 按顺序加载脚本）
├─ style.css             全部样式（单文件）
├─ js/                   运行时脚本
│  ├─ game.js            引擎主文件：对局流程、规则结算、渲染（体量最大）
│  ├─ ai.js              对手 AI（v173 重做 / v175 扩为四档：「简单」＝最初版贪心 AI aiThinkLegacy +
│  │                     其余三档＝影子盘面效果估值 mdEvaluate + 回合级束搜索 aiPlanMoves；
│  │                     简单/普通/困难/月狂，默认简单 —— 主页面「⚙️ 设置」或 AI.setLevel() / ?ai= 切换）
│  ├─ card-browser.js    图鉴与「指定卡牌」弹窗（v181：卡池内按 费用↑ → 战力↑ → 卡名字典序 排序）
│  ├─ deck-page.js       卡组设置页面（window.DeckBuilder；v181：卡池与 12 个卡槽按 费用↑ → 战力↑ → 卡名字典序，复用 CardBrowser.orderDefs）
│  ├─ deck-storage.js    卡组本地持久化（localStorage: touhou2.decks.v1）
│  ├─ home.js            主页面（window.Home）
│  └─ dev-page.js        开发调试模式（window.DevTools）
├─ data/                 游戏数据（改卡/改地形只动这里）
│  ├─ cards.js           人物卡 POOL + 特殊卡 SPECIAL
│  └─ locations.js       地形池 POOL
├─ assets/
│  ├─ cards/             运行时卡图（ASCII 文件名，被 data/cards.js 的 img 字段引用）
│  └─ source-art/        原始立绘素材（v178 起为 240×240 Q 版，取图来源，不进页面）
├─ docs/                 全部文档（见下方索引）
├─ tests/
│  └─ smoke-test.js      jsdom 冒烟测试：随机卡组 + 随机打牌 + 不变量校验
├─ package.json          仅用于测试依赖（jsdom）与 npm test
└─ .gitattributes / .gitignore
```

**加载顺序＝依赖顺序**：`data/*.js → js/game.js → js/ai.js → js/card-browser.js → js/home.js → js/deck-storage.js → js/deck-page.js → js/dev-page.js`。
各脚本之间靠全局对象（`window.Game` / `Home` / `DeckBuilder` / `DeckStorage` / `CardBrowser` / `DevTools`）与全局函数互调，**不要调整 `index.html` 里的脚本次序**。

## 运行测试

```bash
npm install          # 首次：安装 jsdom（唯一的开发依赖）
npm test             # 等价于 node tests/smoke-test.js
```

冒烟测试会：用卡组设置页 UI **随机构造 2 套 12 张卡组** → 校验 localStorage 存档 → 从主页面**随机选一套出战** →
**随机打若干局**（随机双倍/移动/暗出多张），逐回合校验能量账目、手牌与盘面渲染、区域总点数与领先着色、
翻牌顺序＝放置顺序、能量重置回归等不变量，最后跑图鉴/放大/认输流程。

```bash
MATCHES=6 DECKS=4 SEED=3 node tests/smoke-test.js   # 加量；SEED 相同即可复现同一次运行
COVERAGE_BIAS=0 node tests/smoke-test.js            # 关掉「优先打没出过的卡」的覆盖率引导
```

失败时退出码非 0，并打印盘面诊断与去重后的问题清单（页面 `console.error`、`window` 报错、jsdomError 也算失败）。

## 文档索引（`docs/`）

| 文档 | 内容 |
| --- | --- |
| `docs/现有机制.md` | **引擎机制的权威口径（v168 起）**：流程管线与阶段挂点、卡牌效果键与时机效果、卡级机制、区域字段总表与各机制口径、数值口径、其它已实装系统 |
| `docs/现有地形.md` | 地形（区域）清单与字段口径 —— 地形改动只写这里 |
| `docs/现有人物卡牌.md` | 人物卡当前清单（费用/威力/效果/配图） —— 人物卡改动只写这里 |
| `docs/现有特殊卡牌.md` | token（石块/厄运/分身/废弃列车/河童/幽灵/隙间）清单 —— 特殊卡改动只写这里 |
| `docs/GAME_REFERENCE.md` | **跨模块总纲**（一局结构 / 牌库与抽牌 / 胜负与赌注）+ **专项文档索引**；§5 只保留历史版本记录的索引（正文已迁至 `docs/历史版本.md`） |
| `docs/历史版本.md` | **历史版本记录（v1 ~ v167 精简版）**：历史变更记录的**唯一存放处**；v168 起新改动只写各专项文档的行内版本标记 |
| `docs/开发规则.md` | 全局开发规则：新卡取图流程（规则 1）、改动只写专项文档（规则 2）、机制术语口径（规则 3）、**数值记法「A-B = A 费 / B 战力」（规则 4）**、验证由用户负责/AI 不自行测试（规则 5）、不确定先问用户（规则 6） |
| `docs/card-image-map.md` | 卡图文件名对照表、素材状态、新增卡配图流程 |
| `docs/AI强化设计.md` | **设计稿 · 未实装（v176 起草）**：第五档 **EX（Extra）** 与**预设强化卡组**方案 —— 6 套卡组推荐（妖精园 / 瘴气塞牌 / 标准强卡 / 削弱控场 / 反转夺区 / 能量加速）、陷阱卡清单、选组规则、落地改动点与验证清单（**当前代码里没有这一档**） |

**改动约定**（见 `docs/开发规则.md` 规则 2）：加卡 / 改机制 / 改地形 / 改特殊卡**只更新对应的专项文档**，
`docs/GAME_REFERENCE.md` 不再重复收录这些清单与口径，只维护总纲与索引；历史记录只放 `docs/历史版本.md`
（v168 起不再逐版本追加，留痕走专项文档里的行内版本标记）。
另：用户口述的卡牌数值 **「A-B」= A 费 / B 战力**（规则 4）。

## 已知待办（素材侧）

- `assets/cards/` 中 `rumia.png` / `reimu.png` / `sanae.png` 三张对应角色尚未进卡池（保留备用；**v179：`remilia.png` 已由新卡「蕾米莉亚·斯卡蕾特」引用、转出备用**）；
- 6 张「泛用妖怪/妖精」卡、**v179 新增的 2 费角色池法术「镇守大地之石」**（揭示：用石块填满此牌所在的己方区域；⛰️ 兜底）、**v179 新增的 3 费角色池法术「地精的起床」**（揭示：本区自己一侧加 1 张石块，随后己方所有石块永久 +1；🪨 兜底，与石块同 emoji），以及 **v172 新增的 5 张法术**（火神之光 / 祖母绿巨石 / 绿色风暴 / 水银之毒 / 金属疲劳，均为 **1 费 · 法术 · 无战力**，属「帕秋莉法术池」，由帕秋莉回合开始抽入手牌）、**v195 新增的 3 费法术「妖精大战争」**（揭示：摧毁双方场上所有 1 费卡牌；⚔️ 兜底）、**v204 新增的 3 费法术「天文密葬法」**（揭示：将此区域替换为「虚假之月」——该地形在场时本局共有第 7 回合；🌑 兜底）、**v212 新增的 4 费法术「星之仪式」**（揭示：将此区域替换为「守矢神社」——在此区域翻开的揭示牌，其揭示效果再重复触发一次；🌟 兜底，与 1 费「小妖精」同 emoji，重复用法有先例）**均无专属配图**（emoji ⛰️ / 🪨 / 🔥 / 💚 / 🌿 / 💧 / ⚙️ / ⚔️ / 🌑 / 🌟 兜底）；**v182：法术「三妖精集结」已接入专属配图**（**`assets/cards/fairies.png`**，自 `assets/source-art/三妖精集结.png` 复制，**448×488**）——该卡从本清单划掉（卡面 emoji 🌈 仅作缺图兜底）；
- **v180 新增的 6 条「神宝」**（`SPECIAL`，均由人物「蓬莱山辉夜」的揭示经 `give.pool` 随机抽 2 张入手牌；⚠️「龙玉」不属于神宝池、只由「耀眼之龙玉」生成）**也没有专属配图**：无限生命泉（3 费/0 战力，回合结束：本区其他己方已翻开卡 +2；⛲ 兜底）/ 火蜥蜴之盾（3 费/4 战力，揭示：本区敌方已翻开卡 −1；🛡️ 兜底）/ 佛体金刚石（3 费/6 战力，持续·自身不可摧毁；💎 兜底）/ 耀眼之龙玉（3 费·法术，揭示：己方每区各生成 1 张龙玉；🐉 兜底）/ 龙玉（3 费/3 战力白板 token；🔮 兜底）/ 蓬莱的玉枝（3 费·法术，揭示：敌方全场已翻开卡各 −1；🎍 兜底）——**均按规则 1 第 5 条走 emoji 兜底，不拿别的立绘顶替**；另有 8 张 token（石块 / 厄运 / 废弃列车 / 河童 / 幽灵 / **v172 的祖母绿巨石 / 水银** / **v184 的冰块（🧊，由重做后的琪露诺洗入对手牌库）**；以及渲染层占位「隙间」）同样走 emoji 兜底；
- **v177 新增的法术「极限火花」（4 费 · 法术 · 无战力，由重做后的雾雨魔理沙（4 费 / 6 战力，揭示 `give`）加入手牌；揭示 `dwh`：摧毁本区域对方已翻开战力最高的一张）有图**——没有独立立绘，**配图共用魔理沙本体的 `marisa.png`**（口径同「赫卡提亚的分身」共用 `hecatia.png`），emoji 仍为 💥 作缺图兜底；
- **v185 新增的人物「哆来咪」（7 费 / 8 战力，全新 7 费档，效果＝游戏开始时：从卡牌池洗入 10 张随机牌 + 本局每回合最大能量 +1）有图**：`assets/cards/doremy.png`（自 `assets/source-art/240px-哆来咪（Q版立绘）.png` 复制，240×240，规则 1），emoji 💤 仅作缺图兜底；
- **v190 新增的人物「莉莉黑」（1 费 / 3 战力，无阵营，揭示＝丢弃你最右侧的 1 张手牌 —— 弃牌机制 `discard` 的首个使用者）有图**：`assets/cards/lilyblack.png`（自 `assets/source-art/240px-莉莉黑（Q版立绘）.png` 复制，240×240，规则 1），emoji 🖤 仅作缺图兜底；
- **v194 新增的人物「纯狐」（8 费 / 16 战力，全新 8 费档，无阵营，无揭示；持续＝本场战斗中双方每有一张牌被摧毁，此牌能量消耗 −1，最低 0 费 —— 新卡级机制 `costDown` 的首个使用者）有图**：`assets/cards/junko.png`（自 `assets/source-art/240px-纯狐（Q版立绘）.png` 复制，240×240，规则 1），emoji 🌑 仅作缺图兜底；
- **v198 新增的人物「姬虫百百世」（2 费 / 2 战力，无阵营，揭示＝随机丢弃你手牌中的 1 张卡，并将 1 张战力等于该卡能量消耗（印刷费用）的「石块」加入你的手牌 —— 弃牌机制 `discard` 的第三位使用者，也是新增可选子句 `discard.give` 的首个使用者）有图**：`assets/cards/momoyo.png`（自 `assets/source-art/240px-姬虫百百世（Q版立绘）.png` 复制，240×240，规则 1），emoji 🐛 仅作缺图兜底；
- **v196 新增的人物「大鲶鱼」（6 费 / 16 战力，无阵营，无揭示；卡级**放置条件** `playReq`（v196 首个使用者）＝仅在你的场上已翻开的牌中有至少 4 张「石块」时，可以被放置到场上）有图**：`assets/cards/namazu.png`（自 `assets/source-art/大鲶鱼（非想天则立绘）.png` 复制，**534×325 非正方形**、161 KB——该素材不是 `240px-…（Q版立绘）` 的 240×240 规格，按规则 1 原样复制、**不裁切**，口径同 v182 的 `fairies.png` 448×488），emoji 🐟 仅作缺图兜底；
- `assets/source-art/`（**v178 起为 240×240 清晰版；⚠️ v185 校正：实际为 53 张 240×240 立绘 + 1 张非立绘的 `三妖精集结.png`＝54 个 PNG——此前文档记的“52 张”漏登了 `240px-哆来咪（Q版立绘）.png`**；**⚠️ v190 再校正：现为 54 张 240×240 立绘 + 1 张非立绘＝55 个 PNG**）中还有 **14 张立绘**对应角色尚无卡（铃仙 / **晒黑的琪露诺（备选立绘，`cirno.png` 已用标准版）** / 八云蓝 / 丁礼田舞 / 丰聪耳神子 / 九十九八桥 / 九十九弁弁 / 上白泽慧音 / 上白泽慧音2 / 依神女苑 / 云居一轮&云山 / 驹草山如 / 星熊勇仪 / 茨木华扇），日后设计卡牌时取用（**v179 已取用「克劳恩皮丝」→ `assets/cards/clownpiece.png`**；**v180 已取用「蓬莱山辉夜」→ `assets/cards/kaguya.png`**；**v183 已取用「莉莉霍瓦特」→ `assets/cards/lilywhite.png`**；**v185 已取用「哆来咪」→ `assets/cards/doremy.png`**；**v190 已取用「莉莉黑」→ `assets/cards/lilyblack.png`**；**v191 已取用「小野塚小町」→ `assets/cards/komachi.png`**；**v192 已取用「四季映姬」→ `assets/cards/eiki.png`**——这四张立绘此前都没登记进上面这份清单，本次直接转正，故清单张数不变；**v194 已取用「纯狐」→ `assets/cards/junko.png`**——她**本来就在上面这份清单里**，故本次 **15 → 14 张**；**v198 已取用「姬虫百百世」→ `assets/cards/momoyo.png`**——她的立绘**此前也没登记进上面这份清单**（漏登，与哆来咪 / 莉莉黑 / 小野塚小町 / 四季映姬同款情形），本次直接转正，故清单张数仍为 **14 张**；**v213 已取用「梅莉」→ `assets/cards/merry.png`**（240×240，56 KB，规则 1）——她的立绘**此前也未登记进上面这份清单**（漏登，与哆来咪 / 莉莉黑 / 小野塚小町 / 四季映姬 / 姬虫百百世同款情形），本次直接转正，故清单张数仍为 **14 张**）；
- 阵营 `light`「光之三妖精」三人已在 1 费池凑齐（桑尼米尔克 / 露娜·切露德 / 斯塔萨菲雅，v169）；但**还没有对应的地形加成**——若日后要加，照红魔馆的 `aff: { group: 'light', add: N }` 写一条地形即可（v171 起的法术「三妖精集结」已用 `gather: { group: 'light', add: 1 }` 引用这个阵营键）；
- **卡图清晰度（v178；v179 起为 35 张，v180 起为 36 张，**v183 起为 37 张**，**v185 起为 38 张**，**v190 起为 39 张**，**v191 起为 40 张**，**v192 起为 41 张**，**v194 起为 42 张**，**v198 起为 43 张**，**v207 起为 44 张**，**v213 起为 45 张**）**：`assets/cards/` 中 **45 张已是 240×240 清晰版**——**卡池内 45 张有图人物卡全部就位**（**v179 新增「克劳恩皮丝」`clownpiece.png`、并让既有图 `remilia.png` 随「蕾米莉亚·斯卡蕾特」转正**；**v180 新增「蓬莱山辉夜」`kaguya.png`**；**v183 新增「莉莉霍瓦特」`lilywhite.png`**；**v185 新增「哆来咪」`doremy.png`**；**v190 新增「莉莉黑」`lilyblack.png`**；**v191 新增「小野塚小町」`komachi.png`**；**v192 新增「四季映姬」`eiki.png`**；**v194 新增「纯狐」`junko.png`**；**v198 新增「姬虫百百世」`momoyo.png`**；**v207 新增「东风谷早苗」`sanae.png`——她用的是**早已就位的既有备用图**（**由备用转正**，无需新下载、无新文件）**；**v213 新增「梅莉」`merry.png`**（自 `assets/source-art/240px-梅莉（Q版立绘）.png` 复制，240×240，56 KB，规则 1）**；含稗田阿求（**v197 起为 1 费**：重做为 1 费 / 4 战力 + 手牌回合结束自动弃牌，配图不变）；共用图的「赫卡提亚的分身」「法术极限火花」同步变清晰），另有 **1 张备用图（博丽灵梦 `reimu.png`）**——**原备用图里的「东风谷早苗」已于 v207 随新卡转正**；
  **只剩 2 张仍是 100×100**：露米娅 `rumia.png` / 莉格露 `wriggle.png`（**角色都不在卡池**，属保留备用，优先级最低，日后可下载 240px 以上立绘放进 `assets/source-art/`）。清单与逐张对应见 `docs/card-image-map.md`。
  ⚠️ **v196 追加**：新增 `namazu.png`（大鲶鱼）为 **534×325 非正方形**（非 240×240 立绘规格，清晰度反而更高），故「240×240 清晰版」这一计数**不含**它。
  ⚠️ **v198 追加**：新增 `momoyo.png`（姬虫百百世，240×240，规则 1；77 KB）——`assets/cards/` 现共 **47 张 PNG**、其中 **43 张被引用**（240×240 清晰版 **43 张**，另含 1 张非 240×240 的 `namazu.png` 与 1 张 448×488 的 `fairies.png`，口径见 `docs/card-image-map.md`）。
  ⚠️ **v207 追加**：新增人物「**东风谷早苗**」（5 费 / 6 战力，无阵营，揭示＝**再触发本区己方卡牌的揭示**——**v207 新增效果键 `retrigger`** 的首个使用者）**使用既有备用图** `sanae.png`（240×240，自 `assets/source-art/240px-东风谷早苗（Q版立绘）.png`，**早已复制就位 ⇒ 本次未新增/复制任何图片文件、无需新下载**，规则 1）——`assets/cards/` 仍为 **47 张 PNG**、其中 **44 张被引用**（240×240 清晰版 **44 张**），**保留备用图 2 张 → 1 张（只剩博丽灵梦 `reimu.png`；露米娅 / 莉格露那两张 100×100 仍属“角色不在卡池”的保留项，口径见 `docs/card-image-map.md`）**。⚠️ 人物卡 **v204「天文密葬法」（🌑 emoji 兜底、无专属配图）未登入上面的计数链条**，故 `docs/现有人物卡牌.md` 的“全池 54 张 / 有图 44 张 / emoji 兜底 10 张”比本条链条多 1 张，两者不矛盾（口径见 `docs/card-image-map.md` 的 v207 注）。
  ⚠️ **v213 追加**：新增人物「**梅莉**」（1 费 / 2 战力，无阵营，揭示＝**将此区域变为随机另一个地形**——**v213 新增效果键 `xformR`** 的首个使用者，口径完整沿用 v199 地形「秘封俱乐部」的 `xformTurn`）**自带配图** `merry.png`（240×240，**56 KB**，自 `assets/source-art/240px-梅莉（Q版立绘）.png` 复制，规则 1；她的立绘此前**漏登**在「立绘尚无卡」清单里，本次直接转正、清单张数不变）——`assets/cards/` 现共 **48 张 PNG**、其中 **45 张被引用**（240×240 清晰版 **45 张**，另含 1 张非 240×240 的 `namazu.png` 与 1 张 448×488 的 `fairies.png`），**保留备用图仍 3 张**（博丽灵梦 `reimu.png` + 露米娅 `rumia.png` / 莉格露 `wriggle.png` 两张 100×100，后两张属“角色不在卡池”的保留项）。
